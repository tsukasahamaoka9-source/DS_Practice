# GLOSSARY

Kaggle日次ルーティンで登場した用語を蓄積する実用用語集。単なる用語リストではなく、手法・指標については「いつ・なぜ使うか、使うとどうなるか」まで書く。

## 評価指標

### Jaccard係数（Jaccard Index）
- **意味**: 2つの集合A, Bの類似度を `|A∩B| / |A∪B|` で表す指標。TP/(TP+FP+FN)の形でも書ける。
- **いつ使うか**: 検出・マッチング系のタスク（物体検出、セグメンテーション、グラフのノード対応付けなど）で、正解と予測の「重なり具合」を測りたいとき。
- **なぜ使うか**: 精度(Precision)と再現率(Recall)を同時に考慮でき、片方だけを最適化する不正なテクニックを抑制しやすい。
- **使うとどうなるか**: 予測数を過剰に増やす（FP増）と分母が増えてスコアが下がるため、闇雲な過剰検出へのペナルティが自然にかかる。ただしクラス数や希少イベント（例: 細胞分裂）には別途重み付けが必要になることが多い。
- **初出**: 2026-07-31 / Biohub - Cell Tracking During Development

### Balanced Accuracy
- **意味**: 各クラスごとのrecall（そのクラスを正しく当てられた割合）を計算し、それらの単純平均を取った指標。
- **いつ使うか**: 多クラス分類でクラスの出現頻度が偏っている（不均衡な）とき。
- **なぜ使うか**: 通常のaccuracyだと多数派クラスを当てるだけで高スコアが出てしまうが、balanced accuracyは少数派クラスの性能も平等に評価する。
- **使うとどうなるか**: 少数派クラスの予測精度を上げないとスコアが伸びないため、`class_weight="balanced"`のような不均衡対策や、少数派に特化した特徴量エンジニアリングが有効になりやすい。
- **初出**: 2026-07-31 / Predicting Student Health Risk (Playground Series S6E7)

### Sharpe比（Sharpe Ratio）
- **意味**: 金融のリスク調整後リターン指標。`(平均リターン) / (リターンの標準偏差)`で計算し、「どれだけ安定して稼げたか」を表す。
- **いつ使うか**: 投資戦略・トレーディングモデルの評価で、リターンの大きさだけでなくボラティリティ（振れ幅）も考慮したいとき。
- **なぜ使うか**: 単純なリターン最大化だけを目指すと、ハイリスク・ハイリターンな戦略が過大評価されてしまう。Sharpe比はリスクに見合ったリターンかを問う。
- **使うとどうなるか**: 派手だが不安定な予測より、地味でも安定した予測の方が評価されやすくなる。一方で、指標の計算方法（分母がゼロに近い場合の扱いなど）に境界条件があると、意図せず「メトリックハック」が可能になってしまうことがある（今回のHull Tacticalコンペでは、多数のnotebookが同一スコアに集中しておりこの兆候が見られた）。
- **初出**: 2026-07-31 / Hull Tactical - Market Prediction

### ROC-AUC（ROC曲線下面積）
- **意味**: 分類器が正例を負例よりも高くスコアリングできる確率を表す指標。閾値（0.5等）を固定せず、あらゆる閾値での真陽性率・偽陽性率の関係（ROC曲線）の下側面積で評価する。
- **いつ使うか**: 2値分類（または連続値のリスクスコア予測）で、特定の閾値に依存しない「ランキングの正しさ」を評価したいとき。アンサンブルの最適化目的にも使いやすい。
- **なぜ使うか**: accuracyのように特定の閾値に依存しないため、クラス不均衡や運用時の閾値未定な場面でも安定して評価できる。
- **使うとどうなるか**: 予測値の絶対的なキャリブレーション（確率としての正しさ）は評価されないため、「順位付けは正しいが確率値自体はズレている」モデルでも高スコアになりうる点に注意が必要。
- **初出**: 2026-08-03 / Predicting Smartphone Addiction (Playground Series S6E8)

### マクロ平均AUC（Macro-averaged / Multi-label AUC）
- **意味**: マルチラベル分類で、各ラベル（ターゲット）ごとに個別にROC-AUCを計算し、その単純平均を最終スコアとする評価指標。
- **いつ使うか**: 発生頻度が大きく異なる複数の独立したラベルを同時に予測するタスク（例: 膝の12種類の異常検出）で、頻度の多いラベルに評価が支配されないようにしたいとき。
- **なぜ使うか**: 全ラベルをまとめて1つのAUCとして計算すると、サンプル数が多いラベルの影響が強くなり、稀なラベルの識別力が正しく評価されない。ラベルごとに計算してから平均することで、稀なラベルにも多いラベルと同じ重みを与えられる。
- **使うとどうなるか**: 稀なラベルでも手を抜けなくなる一方、サンプル数が極端に少ないラベルでは1つのAUC値自体のばらつきが大きく、全体スコアが不安定になりやすい。
- **初出**: 2026-08-06 / RSNA Knee Abnormality Detection

### CTCスタイルのトラッキンググラフ評価（node_recall / edge_jaccard）
- **意味**: Cell Tracking Challenge（CTC）系のタスクで使われる、予測グラフと正解グラフを比較してノード検出率（node_recall）とエッジ（リンク）の一致度（edge_jaccard, 分裂イベントのTP/FP/FN）を算出する評価方法。
- **いつ使うか**: 3D+時間で細胞分裂・消失が起きる追跡タスクで、検出精度だけでなく「どのノードとどのノードが正しくつながっているか」というグラフ構造の一致度を測る必要があるとき。
- **なぜ使うか**: 検出モデルの精度とリンク（トラッキング）モデルの精度を分離して評価できる。
- **使うとどうなるか**: 座標範囲外に人工ノード/エッジを追加するような「メトリックハック」に対して脆弱になりうる点が実例（Biohubコンペ）から確認できる。
- **初出**: 2026-08-06 / Biohub - Cell Tracking During Development

### CVaR（Conditional Value at Risk、条件付き価値尺度・期待ショートフォール）
- **意味**: ある信頼水準（例: 下位5%）における「最悪ケースの平均損失」を表す指標。VaR（特定パーセンタイルの損失額）が「最悪の一点」しか見ないのに対し、CVaRはその一点より悪い領域全体の平均を取る。
- **いつ使うか**: 投資戦略やトレーディングモデルで、平均的なリターンだけでなく「大きく外れたときにどれだけ損するか」というテールリスクを評価・制御したいとき。
- **なぜ使うか**: Sharpe比のような分散ベースの指標は、上振れ・下振れを対称に扱ってしまい、めったに起きない大暴落のリスクを過小評価しやすい。CVaRは損失側の裾（tail）に焦点を当てて補完する。
- **使うとどうなるか**: 損失関数にCVaR正則化を加えると、リターンを多少犠牲にしてでも極端な損失を避けるような、より保守的で頑健な配分・予測になりやすい。
- **初出**: 2026-08-01 / Financially Guided Deep Portfolio Optimization

### Omega比（Omega Ratio）
- **意味**: リターン分布全体を使い、ある閾値を上回る利益の期待値と、下回る損失の期待値の比で表す指標。平均・分散だけを見るSharpe比と違い、分布の非対称性（歪み）も考慮する。
- **いつ使うか**: リターン分布が正規分布から大きく歪んでいる（非対称なリスクがある）投資戦略を評価したいとき。
- **なぜ使うか**: Sharpe比は上振れ・下振れを対称に罰してしまうが、Omega比は「閾値を超える良いリターン」と「下回る悪いリターン」を別々に扱うため、非対称なリスク選好をより正確に反映できる。
- **使うとどうなるか**: Omega比が1を超えていれば利益側が損失側を上回っていることを意味し、モデルの最適化目的として使うと、単純な平均リターン最大化よりも分布の形まで考慮した配分になりやすい。
- **初出**: 2026-08-01 / Financially Guided Deep Portfolio Optimization

### 微分可能サロゲート目的関数（Differentiable Surrogate Objective, softplus近似）
- **意味**: Sharpe比やOmega比のように非連続なmax関数や比率を含み本来は勾配計算できない評価指標を、`softplus(x) = log(1+e^x)`のような滑らかな関数で近似し、`-log(softplus(...))`の形でニューラルネットの損失関数として直接使えるようにする手法。
- **いつ使うか**: 評価指標そのもの（MSEのような汎用的な代理損失ではなく）をニューラルネットで直接最適化したいが、その指標が微分不可能な演算を含む場合。
- **なぜ使うか**: 代理損失（例: MSEでリターンを予測してから配分を決める）で学習すると、予測誤差が下流の意思決定に伝播してしまう。評価指標そのものを（近似的にでも）直接最適化すれば、この誤差伝播を避けられる。
- **使うとどうなるか**: 学習が評価指標に直接寄り添うため、代理損失を使う場合よりタスク本来のスコアが伸びやすい。一方で近似の滑らかさ（softplusの温度パラメータ等）の調整が必要で、正則化なしでは特定シナリオに過学習しやすい。
- **初出**: 2026-08-01 / Financially Guided Deep Portfolio Optimization

### HOTA / Cell-HOTA（Higher Order Tracking Accuracy / その細胞追跡拡張版）
- **意味**: 一般的なMOT（Multi-Object Tracking）分野の標準指標HOTAは、検出精度DetAと関連付け（トラッキング）精度AssAの幾何平均で、複数のIoU類似度閾値αで平均を取ってバランス良く評価する。Cell-HOTAはこれを細胞分裂に対応させた拡張版で、分裂精度DivAを新設し、AssAとDivAの幾何平均（AssDivA）とDetAをさらに幾何平均して算出する。分裂タイミングが正解の前後1フレーム以内なら許容する「flexible division」オプションも持つ。
- **いつ使うか**: 細胞追跡のように検出・関連付け・分裂という複数の異なる能力を同時に評価する必要があり、かつ従来のCTC系指標OPCTB（SEGとTRAの平均）ではセグメンテーション精度に評価が偏りがちで、細胞分裂タイミングのわずかなズレを過剰に罰してしまう問題を避けたいとき。
- **なぜ使うか**: OPCTBはSEGスコアへの依存が強くトラッキング性能の違いを反映しにくく、また分裂タイミングが1フレームずれただけで大きく減点されるため、正解自体が本質的に主観的（アノテーターごとにブレる）な分裂タイミングを厳しく罰しすぎる。HOTA系はDetA/AssA/DivAをそれぞれ独立して解釈できるため、モデルのどの能力が弱いかを切り分けやすい。
- **使うとどうなるか**: node_recall/edge_jaccardのような単一・複合スコアだけでは見えない「検出は強いが分裂判定が弱い」等の内訳が可視化でき、次に何を改善すべきかの優先順位付けがしやすくなる。一方でαの取り方や幾何平均の設計次第でスコアの感度が変わるため、指標自体の解釈にも注意が必要。
- **初出**: 2026-08-12 / Cell-TRACTR論文（Paper Digest）


### ベイズ最適AUC（Bayes-Optimal AUC / AUC天井の推定）
- **意味**: ターゲットが「滑らかな確率場からのベルヌーイ試行」であるとき、どんな完璧なモデルでも超えられないAUCの上限。キャリブレーション済みの予測確率 `p` から $\text{AUC}^{*} = \mathbb{E}[\mathbb{1}\{p_i>p_j\}p_i(1-p_j)] / (\mathbb{E}[p]\mathbb{E}[1-p])$ で推定できる。
- **いつ使うか**: リーダーボードが小数第4位で競っていて、「あと0.0002伸ばすために2週間かける価値があるか」を判断したいとき。合成データのコンペ（Playground Series等）は特に、生成プロセス由来のラベルノイズで天井が存在する。
- **なぜ使うか**: 天井までの距離が分かれば、**チューニングを続けるべきか、別のコンペに移るべきか**を根拠を持って決められる。実装は10行程度で、計算は10分もかからない。
- **使うとどうなるか**: S6E8では天井が約0.97006、当時のスタックが0.97001〜0.97009で、**伸びしろは5e-5かゼロ**と判明した（真の確率が0.05〜0.95の行が36%、0.4〜0.6が5.8%＝ほぼコイン投げ）。注意点は、(a) 事前にisotonic回帰などで**out-of-foldにキャリブレーションしておく必要がある**（生のスコアでは意味がない）、(b) 出るのは点推定なので、ブートストラップで区間を出さないと過信しやすい、(c) 素直に実装するとO(n²)なので、`p` をソートして累積和で O(n log n) に落とす。
- **初出**: 2026-08-17 / Playground Series S6E8

### Wilson スコア区間（Wilson Score Interval）
- **意味**: 二項比率（成功率）の信頼区間を求める方法のひとつ。`p ± z√(p(1-p)/n)` という正規近似ではなく、`(p + z²/2n ± z√(p(1-p)/n + z²/4n²)) / (1 + z²/n)` で計算する。
- **いつ使うか**: 「このビンでの陽性率は何%か」のような**比率に信頼区間を付けたい**とき。特にビンごとのサンプル数が少ない、あるいは比率が0や1に近いとき。
- **なぜ使うか**: 正規近似は p が0や1に近いと**区間が0未満や1超になる**という無意味な結果を出す。また n が小さいと被覆確率が名目より大幅に低くなる。Wilson区間はこの両方を起こさない。
- **使うとどうなるか**: 折れ線グラフに影として重ねると、「この山や谷はノイズか本物か」が一目で判断できるようになる。点推定だけの折れ線は読者に精度を誤解させるので、**比率を可視化するなら区間を付けるのが既定**と考えてよい。さらに強い証拠が欲しければ、データを2分割して同じ計算をし「独立な半分でも同じ形が出る」ことを示す（split-half）。
- **初出**: 2026-08-19 / Predicting Smartphone Addiction (Playground Series S6E8)

### Hanley-McNeil によるAUCの標準誤差（Hanley & McNeil 1982 SE of AUC）
- **意味**: 与えられた陽性数・陰性数のもとで、AUCの推定値がどれだけばらつくかを解析的に求める公式。`q1 = a/(2-a)`, `q2 = 2a²/(1+a)` を使い、`SE = √((a(1-a) + (n⁺-1)(q1-a²) + (n⁻-1)(q2-a²)) / (n⁺n⁻))`。
- **いつ使うか**: 指標がAUCのコンペで、**公開リーダーボードのスコア差に意味があるか**を判定したいとき。あるいはCVの改善幅が信じてよい大きさかを見積もりたいとき。
- **なぜ使うか**: 公開LBはテストセットの一部（例: 20%）でしか採点されない。行数が分かればこの公式で採点の揺らぎが計算でき、**「上位10チームの隣接スコア差より、採点の標準誤差のほうが大きい」**といった判定ができる。感覚ではなく数値で「その差は測れていない」と言える。
- **使うとどうなるか**: 意思決定が変わる。差が測定精度以下と分かれば、公開LBを0.00003上げるチューニングをやめてCVを信じる、という判断ができる。なお**単体スコアの標準偏差と、2つの提出の"差"の標準偏差は別物**で、同じテスト行で採点される以上2つは強く相関して動くため、差のほうがずっと精度良く測れる（`sd(gap) = sd(move)·√(2(1-ρ))`）。提出の選択は絶対スコアではなく**ペアの差**で考えるべき。
- **初出**: 2026-08-19 / Predicting Smartphone Addiction (Playground Series S6E8)

### tolerance正規化MAE（Tolerance-Normalized MAE / UMUD Score）
- **意味**: 単位もスケールも異なる複数の回帰ターゲットについて、各ターゲットのMAE（平均絶対誤差）を、そのターゲットごとに定めた「許容誤差（tolerance）」で割ってから合成した複合指標。小さいほど良い。UMUDコンペでは羽状角PA（度）・筋束長FL（mm）・筋厚MT（mm）の3変数に適用される。
- **いつ使うか**: 1つの提出で**複数の物理量を同時に予測**させ、それらを1つの順位にまとめたいとき。特に、変数ごとに典型的な大きさが桁違い（FLは数十mm、MTは十数mm、PAは十数度）で、単純な合計だと大きい変数の誤差だけが指標を支配してしまう場合。
- **なぜ使うか**: 正規化しないと「FLさえ合わせれば勝てる」指標になり、他の変数を予測する動機が消える。toleranceを「臨床的・実務的に許容できる誤差幅」に設定すれば、**指標がドメインの要求そのものを表現する**ようになる。RMSEでなくMAEを使うのは、画質不良などによる少数の大失敗に指標を支配させないため。
- **使うとどうなるか**: 各変数が対等に効くので、**最も伸びしろのある変数を探す**という戦略が有効になる。またMAEは外れ値に鈍いので、精度改善よりも**系統誤差（バイアス）の除去**のほうが効率よくスコアが動く（UMUDでは筋厚に一律 -0.4mm の補正をかけただけで 0.7768 → 0.7679 と改善した）。逆に言えば、「全体的に少しずれている」状態を放置すると恒常的に損をする。
- **初出**: 2026-08-25 / UMUD Challenge: Muscle Architecture in Ultrasound Data



### スピアマン順位相関（Spearman's Rank Correlation）
- **意味**: 2つの変数を順位に変換してから計算したピアソン相関。値そのものではなく**大小関係の一致度**を測る。
- **いつ使うか**: 評価指標がAUCのように**順位しか見ない**場合に、2つの予測がどれだけ似ているかを判定するとき。アンサンブルの前に「混ぜる価値があるか」を測る用途が典型。
- **なぜ使うか**: ピアソン相関は値のスケールと分布の形に引きずられる。片方が [0.01, 0.99] に広く散らばり、もう片方が [0.45, 0.55] に固まっていると、順位はほぼ同一でもピアソンは低く出る（逆もある）。AUC指標下で本当に知りたいのは「順位がどれだけ違うか」なので、スピアマンが本質的。
- **使うとどうなるか**: スピアマンが 0.99 を超えるならブレンドしても情報は増えず、スコアはほぼ動かない（2026-08-26 の S6E8 notebook がまさにこの状態で、ブレンド後のスコアが入力の片方と同一だった）。逆に 0.6 を下回るなら、片方が壊れている可能性を疑うべき。**係数だけでなくランク差分のヒストグラムも見る**とさらに良い——AUCに効くのは「上位1%での入れ替わり」であり、全体の緩やかなずれではないため。
- **初出**: 2026-08-26 / Playground Series S6E8

### 公式指標のローカル再実装による提出不要プロキシ（Official-Metric Reimplementation / Submission-Free Proxy Score）
- **意味**: コンペの公式評価指標を**自分で実装し直し**、テストに含まれない手元データ（多くは train の一部）で計算して、提出せずに変更の良し悪しを判断すること。Biohub の 0.934 notebook はタイトルに `PROXY_SCORE=0.9384` を掲げ、ハンガリアン法によるノード照合（7µmゲート）・Edge Jaccard・ノード数ペナルティ付き Adjusted Jaccard・Division Jaccard を全て自前実装している。
- **いつ使うか**: (1) 提出回数に上限がある（1日5回程度）。(2) 指標が複合的で、汎用の CV スコア（AUC や RMSE）では代用できない。(3) 探索したいパラメータが多い。細胞追跡・物体検出・セグメンテーションのように**指標が「集合の一致」や「グラフの一致」で定義される**コンペでは、ほぼ必須になる。
- **なぜ使うか**: 提出枠を消費して1パラメータずつ確かめるやり方では、1週間かけて数個しか試せない。手元で公式指標が再現できれば1日に数十通り試せる。さらに重要なのは、**公式指標と同じアルゴリズム（例: 貪欲な最近傍ではなくハンガリアン法）で測ること**。近似した代用指標だと、代用指標だけが改善して本番が動かない、という最悪の誤誘導が起きる。
- **使うとどうなるか**: 実験速度が一桁上がる。ただし proxy には必ず**楽観バイアス**が乗る（Biohub の例では proxy 0.9384 に対し LB 0.934 で約0.004）。train 分布での推定である以上これは避けられないので、**proxy の絶対値ではなく変更前後の差分（Δproxy）を信じる**のが正しい使い方。さらに一歩進めるなら、動画（サンプル）単位のブートストラップで Δproxy の信頼区間を出し、**バイアスと同程度の改善は誤差として棄却する**。関連: イベント認識バリデーション標本設計、最小検出可能差、ブートストラップ信頼区間。
- **初出**: 2026-09-02 / Biohub - Cell Tracking During Development

### 単調変換の無効性チェック（Monotone-Transform No-Op Check）
- **意味**: 順位ベースの指標（ROC-AUC、Spearman相関など）を使うコンペにおいて、**予測値への単調変換は理論上スコアを1ミリも動かさない**という事実を使って、後処理が本当に意味を持つかを検算すること。RSNA Knee の 0.936 notebook にある consensus power calibration（`z=(p−0.5)/0.5; out = 0.5 + 0.5·sign(z)·|z|^1.05`）は γ>0 の単調関数なので、**AUC に対しては原理的に no-op**（float の丸めで同着が分離される程度の影響しかない）。
- **いつ使うか**: 後処理・キャリブレーション・スケーリングを追加しようとするとき、**実装する前に**。特に「効いた気がする」小改善を評価するとき。
- **なぜ使うか**: 順位指標のコンペでは、単調変換を「効いた」と誤認する事故が非常に多い。実際には**別の乱数シードや別の丸め順序で同じ幅だけ動く**ノイズを見ているだけ、というケースがほとんど。この検算を先にやれば、無意味な後処理をパイプラインから排除でき、LBへの過学習も減る。逆に、単調変換が意味を持つのは (1) 指標が logloss や Brier のように**値そのもの**を見るとき、(2) **重み付き平均の前**にかけるとき（混合比が実質的に変わるため）、(3) しきい値で二値化するときの3つに限られる。
- **使うとどうなるか**: パイプラインが1段シンプルになり、実行時間とバグの表面積が減る。判定手順は「その変換は狭義単調か？ → Yes なら順位不変 → 指標は順位のみを見るか？ → Yes なら no-op と結論し、外して同スコアであることを確認する」。関連: ランクベースアンサンブル、Rank-01 パーセンタイル正規化、ROC-AUC。
- **初出**: 2026-09-02 / RSNA Knee Abnormality Detection

### fold別AUCとプールドOOF AUCの乖離（Fold-wise vs Pooled OOF AUC Divergence）
- **意味**: 交差検証でAUCを報告するとき、①各foldで測ったAUCの単純平均（`np.mean(fold_scores)`）と、②全foldのOOF予測を1本の配列にプールしてから1回だけ測ったAUC（`roc_auc_score(y, oof)`）の2通りがある。この2つは**一致しない**。差が大きいほど「foldごとに予測値のスケールがずれている」ことを意味する。
- **いつ使うか**: 交差検証を回すたび、**両方を必ず印刷する**。特にfold平均でテスト予測を作る場合や、この後スタッキング・ブレンドに進む場合は必須。
- **なぜ使うか**: AUCは順位しか見ないので、fold単体で測る限りスケールのズレは表に出ない。しかし**プールした瞬間に順位が壊れる**。fold Aが全体的に高い値を出し、fold Bが低い値を出していると、混ぜたときにfold Aのサンプルが一律で上位に来てしまい、真の順位が失われる。乖離はその現象の唯一の早期警告になる。
- **使うとどうなるか**: 乖離が小さければ（0.0005未満程度）fold平均でのブレンドは安全。大きければ、**fold内で順位正規化してから平均する**（`rank(pct=True)` してから平均）ことで解消できる。この差は「CVは良いのにLBが伸びない」「ブレンドすると単体より下がる」という症状の典型的な原因のひとつで、原因を知らないと延々とモデル側を疑うことになる。関連: fold内順位正規化してから平均、Honest OOF、ランクベースアンサンブル、モーメントマッチングによるロジット整合。
- **初出**: 2026-09-03 / Playground Series S6E9

## 手法

### DETRベースのend-to-end検出+追跡（Track Query / Object Query）
- **意味**: DETR（Detection Transformer）を追跡タスクに拡張したTrackFormer/MOTR系アーキテクチャの中核アイデア。最初のフレームでは「物体クエリ（object query）」が全物体を検出し、以降のフレームではその出力埋め込みが「トラッククエリ（track query）」としてそのまま次フレームに引き継がれ、新規出現物体の検出用object queryと合わせてデコーダのself-attentionで互いを認識し合うことで、二重検出を避けながら検出とトラッキングを1モデル・1パスで同時に行う。
- **いつ使うか**: 従来の「検出→（別モデル/後処理で）リンク」という2段階パイプラインの代わりに、検出とトラッキングを単一のend-to-endモデルに統合し、非最大抑制やILP最適化のような後処理を省きたいとき。
- **なぜ使うか**: 2段階パイプラインは検出誤差がリンク段階にそのまま伝播する上、後処理のチューニングにコストがかかる。track query方式は時間方向の一貫性をモデル自体の内部状態として持たせるため、後段の手作りルールへの依存を減らせる。
- **使うとどうなるか**: 学習・推論が1モデルに閉じるため後処理由来のバグや調整箇所が減る一方、既存の検出→リンクの多段パイプライン（各段を個別に改善できる）と比べてモデル全体の再学習コストが大きく、部分的な改善の積み増しがしにくくなるトレードオフがある。
- **初出**: 2026-08-12 / Cell-TRACTR論文（Paper Digest）

### Difference of Gaussians（DoG、差分ガウシアン）
- **意味**: 異なる標準偏差（ぼかしの強さ）でガウシアンフィルタをかけた2つの画像の差分を取ることで、特定サイズの明るい塊（blob）を強調する画像処理手法。
- **いつ使うか**: 顕微鏡画像や天体画像などで、特定サイズの「点状・塊状」の対象物（細胞、星など）を検出したいとき。
- **なぜ使うか**: 単純な閾値処理よりも、対象物のサイズに合わせた検出ができ、背景ノイズや大きすぎる/小さすぎる構造を除外しやすい。
- **使うとどうなるか**: `dog_small_um`/`dog_large_um`のような2つのスケールパラメータの選び方次第で感度が大きく変わる。小さすぎるとノイズを拾い、大きすぎると近接する細胞を1つに統合してしまう。
- **初出**: 2026-07-31 / Biohub - Cell Tracking During Development

### Hungarian法（線形割当問題、`scipy.optimize.linear_sum_assignment`）
- **意味**: 2つのグループ（例: 前フレームの点と次フレームの点）の間で、総コスト（例: 距離の合計）が最小になるような1対1の対応付けを求めるアルゴリズム。
- **いつ使うか**: フレーム間の物体追跡、マッチング問題など「誰と誰を対応付けるべきか」を最適に決めたいとき。
- **なぜ使うか**: 貪欲法（近い順に対応付ける）だと局所最適に陥りやすいが、Hungarian法は大域的に最小コストな組み合わせを保証する。
- **使うとどうなるか**: 計算量はO(n³)程度で、候補数が多いと重くなる。実務では「距離がゲート値を超えるペアは除外する」など事前にコスト行列を疎にする工夫が併用されることが多い。
- **初出**: 2026-07-31 / Biohub - Cell Tracking During Development

### クラス重み付け（`compute_sample_weight(class_weight="balanced")`）
- **意味**: 各サンプルに、そのクラスの出現頻度に反比例した重みを付けて学習する手法。少数派クラスのサンプルほど大きな重みが付く。
- **いつ使うか**: 分類問題でクラスの数が偏っている（不均衡）とき。特に評価指標がbalanced accuracyやmacro-F1のように各クラスを平等に扱う場合。
- **なぜ使うか**: 重み付けをしないと、モデルは多数派クラスばかり当てるように学習しがちで、少数派クラスの予測精度が犠牲になる。
- **使うとどうなるか**: 少数派クラスの再現率が上がる一方、多数派クラスの精度がやや下がることがある（トレードオフ）。SMOTE等のオーバーサンプリングと比べて、データを水増ししないため計算コストが小さいのが利点。
- **初出**: 2026-07-31 / Predicting Student Health Risk (Playground Series S6E7)

### Granger因果性検定（Granger Causality）
- **意味**: 時系列Xの過去の値が、時系列Yの将来の値を予測するのに役立つかどうかを統計的に検定する手法。「因果」という名前だが、実際には時間的な予測力の有無を見るもの。
- **いつ使うか**: 多数の時系列特徴量の中から、ターゲット（例: 市場リターン）の予測に本当に役立つものを絞り込みたいとき。
- **なぜ使うか**: 単純な相関係数だけでは、同時に動くが予測には使えない特徴量（同時相関はあるが先行性がない）を見分けられない。Granger因果性は「先行して動くか」を評価できる。
- **使うとどうなるか**: 特徴量選択の精度は上がるが、検定の多重比較問題（大量の特徴量に対して繰り返し検定すると偶然有意になるものが増える）に注意が必要。
- **初出**: 2026-07-31 / Hull Tactical - Market Prediction

### 構造変化検出（Structural Break Detection）
- **意味**: 時系列データの統計的な性質（平均、分散、特徴量とターゲットの関係など）が、ある時点を境に大きく変化していないかを検出する手法。
- **いつ使うか**: 金融データのように、市場のレジーム（強気相場・弱気相場など）が時間とともに変わりうるデータを扱うとき。
- **なぜ使うか**: 特徴量とターゲットの関係が全期間で一定だと仮定すると、レジームが変わった後のデータでモデルの性能が急に悪化することがある。事前に構造変化を把握しておくことで、頑健な特徴量選択やCV設計ができる。
- **使うとどうなるか**: 「どの特徴量が時期によらず安定して効くか」を優先的に選べるようになる。ただし検出手法自体にもパラメータ（変化点の感度など）があり、誤検出・見逃しのバランスに注意が必要。
- **初出**: 2026-07-31 / Hull Tactical - Market Prediction

### 整数線形計画法（ILP: Integer Linear Programming）によるグラフ最適化
- **意味**: 変数を整数（多くは0/1）に制限した線形計画問題。追跡グラフ構築では「このエッジを採用するか否か」を0/1変数とし、エッジスコアの合計を最大化しつつノードの入出次数などの制約（1つの細胞は同時に複数の親を持てない、等）を満たす解を求める。
- **いつ使うか**: Hungarian法のような単純な1対1マッチングでは表現しきれない、分岐（細胞分裂）や複雑な制約を含むグラフ構築問題を大域的に最適化したいとき。
- **なぜ使うか**: 局所的な貪欲法やスコア閾値だけでリンクを選ぶと、大域的に見て矛盾する解（1ノードに2つの親が付く等）が生まれやすい。ILPはこれらの制約を明示的な式として組み込み、厳密な最適解（または一定の最適性保証付きの近似解）を得られる。
- **使うとどうなるか**: Hungarian法より柔軟な制約を表現できる一方、問題規模（ノード・エッジ数）が大きくなるとソルバーの計算時間が急増しうる。実務ではまずスコア閾値で候補エッジを絞り込み、疎なグラフに対してILPを解くのが定石。
- **初出**: 2026-08-03 / Biohub - Cell Tracking During Development

### クロスアテンション（Cross-Attention）
- **意味**: Transformerのattention機構の一種で、ある系列（例: フレームtのノード群）の各要素が、別の系列（例: フレームt+1のノード群）の要素との関連度を学習して重み付けする仕組み。
- **いつ使うか**: 2つの異なる集合・系列間の対応関係（どの要素とどの要素が関連するか）をデータから学習したいとき。細胞追跡ではフレーム間のリンク候補スコアリングに使われる。
- **なぜ使うか**: 単純な距離ベースのマッチングと異なり、位置情報だけでなく特徴量の類似性（見た目や文脈）も考慮した対応付けを学習できる。
- **使うとどうなるか**: 距離だけでは区別できない近接した対象同士も、特徴量の違いで正しく対応付けられる可能性が上がる。一方で学習には十分な量のアノテーション付きペアデータが必要で、疎なアノテーションでは工夫（正例・負例のマスキング等）が必要になる。
- **初出**: 2026-08-03 / Biohub - Cell Tracking During Development

### Hill Climbing（貪欲法によるアンサンブル選択）
- **意味**: 複数モデルの予測をアンサンブルする際、(1)最良の単体モデルから始め、(2)残りのモデルを1つずつ試して最もスコアが上がる重みで追加、(3)スコアが改善しなくなるまで繰り返す、という貪欲法によるアンサンブル構築法。
- **いつ使うか**: 多数の学習済みモデルのOOF予測が揃っており、それらを組み合わせて評価指標を直接最大化する重みを探したいとき。
- **なぜ使うか**: 全モデルの重みを同時最適化する凸最適化と比べて実装が単純で、OOFへの過学習も比較的抑えられる。負の重みを許容すれば、特定モデルの系統的な誤差を打ち消す使い方もできる。
- **使うとどうなるか**: 単体最強モデルより高いスコアに到達しやすいが、探索の精度（precision）や局所最適に陥るリスクがあり、理論的にはスタッキング（メタモデル学習）の方が高い性能を出せる場合がある。
- **初出**: 2026-08-03 / Predicting Smartphone Addiction (Playground Series S6E8)

### オンライン学習・逐次学習（Online Learning / Incremental Learning、時系列での注意点）
- **意味**: モデルを一度学習して固定するのではなく、新しいデータが届くたびに再学習・更新し続ける学習方式。
- **いつ使うか**: 金融市場のように、データの統計的性質が時間とともに変化しうる（非定常な）時系列を扱うとき。
- **なぜ使うか**: 固定モデルは古い市場regimeに最適化されたままになり、直近のパターン変化に追従できない。オンライン学習は都度モデルを更新することで直近の傾向を反映できる。
- **使うとどうなるか**: 直近regimeへの適応力は上がるが、計算コストが増える（毎回の再学習が必要）。**特に注意が必要なのは検証データの取り方**で、通常の機械学習の感覚で大きめのvalidation_split（例: 0.25）を設定すると、時系列全体の何年分もの古いデータを検証に使ってしまい「直近にどれだけ適応できているか」を測れなくなる。オンライン学習の検証は直近のごく短いウィンドウ（全体の0.1%〜0.3%程度）に限定すべき。
- **初出**: 2026-08-03 / Hull Tactical - Market Prediction

### ターゲット・頻度エンコーディング（連続値を含む全カラムへの適用、ネストCV）
- **意味**: 数値・カテゴリを問わず全カラムを文字列レベルとして扱い、各水準ごとの「スムージングされたターゲット平均」と「出現頻度」を新しい特徴量として追加するエンコーディング手法。
- **いつ使うか**: 高カーディナリティ（水準が多い）な連続値・カテゴリ特徴量があり、かつ1水準あたりのサンプル数が十分（数百行以上が目安）にあるとき。木モデルが同じ非線形な関係を再現するのに何十回も分岐を要するような場合に特に有効。
- **なぜ使うか**: 木モデルは連続値を分岐で近似するが、target encodingなら1回の参照で目的変数との関係を直接引ける。ただし目的変数を使うため、素朴に計算するとCVスコアが不当に高くなる（リーク）。ネストしたクロスバリデーション（外側foldの学習データの中でさらに内側foldに分けて計算する）でこれを防ぐ。
- **使うとどうなるか**: 効果が大きい場合はモデルの主要な改善要因になりうる（今回のPlaygroundコンペでは他の全工夫を合わせたより効果が大きかった）。一方、水準あたりのサンプル数が少ない特徴量に無理に適用すると過学習を招くため、事前に「1水準あたりの行数」を確認してから適用すべき。
- **初出**: 2026-08-04 / Predicting Smartphone Addiction (Playground Series S6E8)

### ノイズフロアの測定（Repeated CV Noise Floor）
- **意味**: 1回のクロスバリデーションの中でのfold間スコアのばらつき（per-fold range）ではなく、分割の乱数シードを変えて複数回クロスバリデーションを繰り返し、その平均スコアの標準偏差を「測定誤差の下限（ノイズフロア）」として求める手法。
- **いつ使うか**: 新しい特徴量・手法を試してCVスコアが少し上がったとき、それが本当に効いているのか、それとも単なる分割の運なのかを判断したいとき。
- **なぜ使うか**: 1回のfold内のスコアのばらつきは、別の分割をしたときのスコア変動を大きく過大評価する。ノイズフロアを先に測っておけば、その後の「+0.001の改善」のような主張が本当に意味のある差なのかを定量的に判定できる。
- **使うとどうなるか**: 施策の採否を「なんとなく良さそう」ではなく「ノイズフロアの何倍の改善か」という基準で判断できるようになり、無駄な複雑化（効果のない特徴量やモデルの追加）を防げる。
- **初出**: 2026-08-04 / Predicting Smartphone Addiction (Playground Series S6E8)

### ロジットスケールでのスタッキング（Stacking on Logit Scale）
- **意味**: 各モデルの予測確率をそのまま合成するのではなく、`log(p/(1-p))`（対数オッズ、ロジット）に変換してからスタッキング（メタモデルによる合成）を行う手法。
- **いつ使うか**: 目的変数が特定の条件で確率0または1に近づく「飽和」領域を持つタスク（例: 明らかに依存/非依存と分かるケースが多い分類問題）で、複数モデルを組み合わせたいとき。
- **なぜ使うか**: 確率スケールでは0.99と0.999の差が小さく見えてしまい、モデル間の違いを合成に反映しにくい。ロジットスケールならこの差を引き伸ばして表現できるため、飽和領域でもモデルの違いを活かせる。
- **使うとどうなるか**: ライブラリ内のモデルの多様性が高いほど効果が大きい（今回の例では12モデルのライブラリで+0.00047、全てGBM系のライブラリでは+0.00008と効果が縮小した）。木モデル同士は飽和領域での挙動が似通いやすいため、ロジットスケールの恩恵を最大化するには非GBM系モデル（線形モデル・深層表形式モデル等）を混ぜるとよい。
- **初出**: 2026-08-04 / Predicting Smartphone Addiction (Playground Series S6E8)

### トップハット変換（Top-hat Transform）
- **意味**: 画像に対してモルフォロジー演算の一種である「グレースケール・オープニング」（`grey_opening`、局所的な明るい小さな塊を除去する処理）を適用した結果を元画像から差し引くことで、背景の緩やかな明るさムラを除去し、小さく明るい塊（細胞核など）だけを強調する画像前処理。
- **いつ使うか**: 顕微鏡画像のように、背景の明るさが場所によって不均一（照明ムラ、細胞密度の違いなど）で、対象物が背景よりわずかに明るい小さな塊として現れるとき。
- **なぜ使うか**: 単純な閾値処理は背景ムラの影響を受けやすく、明るい領域では過検出、暗い領域では見逃しが起きやすい。トップハット変換は局所的な背景を推定して差し引くため、画像全体で明るさが不均一でも対象物を一貫して強調できる。
- **使うとどうなるか**: オープニングのカーネルサイズ（対象物より少し大きいサイズが目安）の選び方次第で効果が変わる。小さすぎると対象物自体まで除去され、大きすぎると背景ムラを取りきれない。今回のBiohubコンペでは、トップハット変換ありのモデルとなしのモデルを両方学習し、ヒートマップを平均するアンサンブル戦略で頑健性を高めていた。
- **初出**: 2026-08-04 / Biohub - Cell Tracking During Development

### ウォークフォワード検証（Walk-Forward Cross-Validation）
- **意味**: 時系列データを「常に過去のデータで学習し、その直後の未来のデータでテストする」という順序を守りながら、テスト期間をずらして複数fold作るクロスバリデーション手法。
- **いつ使うか**: 金融データのような時系列予測タスクで、通常のランダムシャッフルk-foldクロスバリデーションを使うと未来の情報で過去を予測してしまうリークが起きる場合。
- **なぜ使うか**: 本番の推論では必ず「過去→未来」の順で予測が行われるため、検証の順序もそれに合わせないと、実運用時の性能を過大評価してしまう。
- **使うとどうなるか**: 各foldの学習データ量が不揃いになる（直近のfoldほど学習データが多い）ため、fold間のスコアのばらつきが大きくなりやすい。また短いテスト期間（例: 100〜200日）でのfold平均スコアと、全期間を通した総合スコアとで指標の性質上、値が大きく乖離することがある点に注意が必要。
- **初出**: 2026-08-04 / Hull Tactical - Market Prediction

### Test-Time Augmentation（TTA、テスト時データ拡張）
- **意味**: 推論時に入力画像を反転・回転などで複数パターンに変換し、それぞれに対するモデルの予測を平均・統合することで最終予測を安定化させる手法。
- **いつ使うか**: 画像・3Dボリュームなど、反転や回転をしても正解ラベルの意味が変わらない（回転不変・反転不変な）タスクで、推論時に多少計算コストを増やしてでも精度・安定性を上げたいとき。
- **なぜ使うか**: モデル単体の予測は入力のわずかな向き・位置ずれに敏感なことがある。複数の変換パターンで予測し平均を取ることで、そうしたノイズに強くなる。学習をやり直す必要がなく、推論時だけの工夫で効果が得られるのが利点。
- **使うとどうなるか**: 推論時間はTTAパターン数（例: 8方向）倍に増えるが、検出漏れ（FN）が減りやすい。パターン数を増やすほど改善は頭打ちになりやすく、どの変換の組み合わせが効くかはタスク依存（今回のBiohubコンペのコードには4種類のTTA実装が残されており、試行錯誤の跡が見られた）。
- **初出**: 2026-08-05 / Biohub - Cell Tracking During Development

### Honest OOF（正直なOut-of-Fold検証、fold整合性チェック）
- **意味**: 複数モデルのOOF（Out-of-Fold）予測をスタッキング・ブレンドする際、すべてのモデルが**厳密に同一のクロスバリデーション分割**（同じ`n_splits`・同じ`random_state`）で学習されているかを確認・徹底する検証姿勢。
- **いつ使うか**: 自分以外の参加者が公開したOOF予測を再利用してブレンドを組みたいとき、または複数の実験で作ったOOF同士を後から組み合わせたいとき。
- **なぜ使うか**: fold分割が微妙に異なるOOF予測を混ぜると、ある行が「自分のモデルの学習に使われていたのに、ブレンド全体としては未学習データ扱いされる」形でリークが発生し、ブレンドのCVスコアが不当に高く見えてしまう。この種のリークは配列の形状や値の範囲が正常に見えるため、統計的検定（KS検定など）でもほぼ検出できない。
- **使うとどうなるか**: `n_splits`と`random_state`をコードで確認する一手間を怠らないことで、見せかけのスコア向上を避けられる。逆に言えば、公開されたOOF配列は「一見健全に見えても信用できるとは限らない」という前提でコードを読む癖がつく。
- **初出**: 2026-08-05 / Predicting Smartphone Addiction (Playground Series S6E8)

### 汎化ギャップ最小化によるハイパーパラメータ選定（Generalization-Gap-Aware Trial Selection）
- **意味**: Optuna等でのハイパーパラメータ探索の結果を「検証スコアが最も高いTrial」ではなく、「本番（Public LB等）でのスコア低下幅（本番スコア−検証スコア）が最も小さいTrial」を基準に選ぶ、モデル選定の考え方。
- **いつ使うか**: 検証スコアが最高のパラメータ設定が、必ずしも本番でも良い結果を出すとは限らない（過学習気味な設定が偶然高い検証スコアを出しているだけかもしれない）と疑われるとき。
- **なぜ使うか**: 検証スコアの最大化だけを基準にすると、検証データにたまたま適合しただけの不安定な設定を選んでしまうリスクがある。本番との差が小さい設定は、未知のデータに対しても同様に安定した性能を発揮しやすいと期待できる。
- **使うとどうなるか**: 単純な「検証スコア最大」の設定より保守的な（＝スコアの絶対値はやや低くても）設定が選ばれやすくなる。ただし本番スコアを何度も参照して選定基準にすること自体が、本番リーダーボードへの間接的な過学習（LB probing）になりうる点には注意が必要。
- **初出**: 2026-08-05 / Hull Tactical - Market Prediction

### 非同質性グラフ（Non-homophilic Graph）と調整済み同質性（Adjusted Homophily）
- **意味**: グラフニューラルネットワーク（GNN）の多くは「隣接するノード（またはエッジ）同士はラベルが似ている」という同質性（homophily）を暗黙に仮定している。調整済み同質性`H_adj`は、この仮定がどれだけ成り立つかを0〜1で定量化した指標（0.5超で「同質的」とされる）。細胞追跡の候補グラフ（各検出をノード、フレーム間の近接ペアをエッジとするグラフ）は、同じノードを共有するエッジ同士の正解ラベルがほぼ無相関（`H_adj ≈ 0.01`、正解エッジに隣接するエッジのうち正解はわずか29%）という「非同質性グラフ」であることが実測で示されている。
- **いつ使うか**: GNNのメッセージパッシング（隣接ノード・エッジの特徴を集約する処理）を新しいタスクに適用する前に、そのグラフ構造がそもそも同質的かどうかを検証したいとき。
- **なぜ使うか**: 非同質性グラフにメッセージパッシングを適用すると、ラベルと無相関な（時にはラベルと逆相関する）隣接要素の情報を集約してしまい、表現がノイズで劣化する。同質性を事前に測定せずGNNを導入すると、既存のシンプルな手法を上回れない原因が「モデルの表現力不足」なのか「そもわそもグラフ構造がGNNに適さない」のか切り分けられない。
- **使うとどうなるか**: 候補グラフのようにトポロジーがラベル情報を持たない場合、GNN（GAT・FAGCNなど非同質性対応型を含む）は性能の上限がトポロジーそのものに縛られる。この場合、グラフの隣接関係ではなく要素間の幾何的・特徴的な関係（例: エッジ同士の距離）に基づいてattendするTransformer型アーキテクチャの方が有効な場合がある。
- **初出**: 2026-08-05 / Higher-Order Cell Tracking Transformer

### エッジ中心アテンション（Edge-centric Attention）
- **意味**: グラフの「ノード」ではなく「候補エッジ（リンク）」自体をトークンとして扱い、エッジ同士を互いに注意（attention）させるTransformerの設計。ノード対を結合してエッジ特徴を作った後、エッジ同士の幾何的な関係（例: 3D空間での線分間の最短距離）を学習可能なバイアスとして注意スコアに加える。
- **いつ使うか**: グラフのトポロジー（隣接関係）自体がラベル情報を持たない非同質性グラフ（上記参照）で、GNN的なメッセージパッシングではなく別の方法で要素間の関係を学習させたいとき。特に、複数の候補リンクが1つのノードを奪い合う（cell divisionのような分岐・競合が起きる）タスクで有効。
- **なぜ使うか**: ノード埋め込み方式は、分岐点（細胞分裂など）で複数の経路の埋め込みが融合してしまう問題を抱えるが、エッジそのものを独立したトークンとして扱うことでこの融合を回避できる。トポロジーではなく幾何的な距離でバイアスをかけることで、ラベルと無相関な隣接関係に惑わされない。
- **使うとどうなるか**: ノード数`|V|`に対しエッジ数`|E|`は多くなりやすく（近傍k件への制限をしても`O(|V|^2 k^2)`程度）、計算・メモリコストは純ノードベースの手法より大きく増える（論文中ではk=10で約100倍のメモリ増）。一方で、事前学習済みの大規模画像エンコーダなしでも高精度を達成でき、手作り特徴量のみで完結する軽量な設計にもできる。
- **初出**: 2026-08-05 / Higher-Order Cell Tracking Transformer

### 時空間サイン波位置エンコーディング（Sinusoidal Space-Time Positional Encoding）
- **意味**: 3D+時間のボクセル座標(z, y, x, t)を、Transformer系モデルに入力できる連続的な埋め込みベクトルに変換する手法。sin/cosの異なる周波数成分を使い、座標の絶対値と相対距離の両方を表現できるようにする。
- **いつ使うか**: 細胞位置のように連続値かつ4次元（3D＋時間）の座標を、距離関係をモデルが学習しやすい形でTransformerに渡したいとき。
- **なぜ使うか**: 単純なone-hotやスカラー入力では、座標間の距離関係をモデルが学習しにくいため。
- **使うとどうなるか**: エッジ（ノード間リンク）候補のスコアリングで、物理的・時間的に近いノード同士をモデルが自然に重視できるようになる。
- **初出**: 2026-08-06 / Biohub - Cell Tracking During Development

### ギャップクロージング＋短小トラック除去（Gap-Closing & Short-Track Pruning）
- **意味**: トラッキンググラフの後処理として、(1) 1フレーム欠損したリンクをHungarian法で物理距離をもとに補完し、(2) Union-Findで求めた弱連結成分のうち短すぎるトラックを除去する2段階の処理。
- **いつ使うか**: 検出・リンクモデルの出力に、一時的な検出漏れやノイズによる短い偽トラックが混在するとき。
- **なぜ使うか**: 後処理でグラフ全体の一貫性を高め、一時的な欠損によるトラック分断や、ノイズ由来の短小トラックを減らせる。
- **使うとどうなるか**: node_recallやedge_jaccardなどのグラフ評価指標が向上しやすい。
- **初出**: 2026-08-06 / Biohub - Cell Tracking During Development

### DINOv2（自己教師あり学習の視覚基盤モデル）
- **意味**: Meta社が公開した、正解ラベルを使わずに大量の自然画像から汎用的な視覚特徴を学習する自己教師あり学習(self-supervised learning)モデル。学習済みの重みをそのまま(または一部凍結して)特徴抽出器として転用できる。
- **いつ使うか**: 正解ラベルが極端に少ない(例: 数十件)画像タスクで、ゼロから畳み込みネットワークやTransformerを学習させると即座に過学習してしまう場合。
- **なぜ使うか**: 自然画像で学習した表現でも、汎用的なエッジ・テクスチャ・形状の特徴は医療画像にも一定程度転用できることが複数の研究で示されている。重みを凍結して使えば、少量データでも安定した特徴が得られる。
- **使うとどうなるか**: 学習対象のパラメータ数が大幅に減り過学習を抑えられる一方、医療画像特有の微細な所見を自然画像用の特徴だけで十分に捉えられるかは保証されず、タスクによって効果が大きく異なる。
- **初出**: 2026-08-06 / RSNA Knee Abnormality Detection

### ルールベース×テキスト分類器のハイブリッド擬似ラベリング
- **意味**: 正規表現などのルールベース手法(高精度だが再現率が低い)と、TF-IDF+ロジスティック回帰などの軽量な機械学習モデル(表現の揺れを拾えるが誤検出もある)を、それぞれの確信度に応じて重み付けブレンドし、正解ラベルがない大量サンプルに対する擬似ラベルを作る手法。
- **いつ使うか**: 画像ラベルに対応するテキスト情報(放射線レポート等)が存在し、正解ラベル数が少なすぎて弱教師あり学習が必要なとき。
- **なぜ使うか**: ルール単体では言い回しの多様性を拾いきれず、テキスト分類器単体では少量の正解データで学習した際に不安定になりやすい。両者を確信度で組み合わせることで片方だけよりも頑健な擬似ラベルを作れる。
- **使うとどうなるか**: ターゲットごとにルール/テキストモデルどちらが信頼できるかが異なるため、ターゲット単位で信頼性を評価し重みを変える設計が効果を発揮する。ただし擬似ラベル自体の質が低いターゲットでは、画像モデルの性能向上より先にこの段階の精度を上げる方が投資効果が高い。
- **初出**: 2026-08-06 / RSNA Knee Abnormality Detection

### 双方向調和平均マッチング（Bidirectional Harmonic-Mean Association Fusion）
- **意味**: フレーム間のリンク候補を「前向き（t→t+1）」と「後向き（t+1→t）」の2方向で独立にスコアリングし、2つのスコアの調和平均（`2ab/(a+b)`）を最終的なリンク確率とする融合手法。
- **いつ使うか**: 2つの時点・2つの視点から別々に計算した対応スコアを1つに統合したいとき。特に「両方向で高スコアな候補だけを信頼したい」場合。
- **なぜ使うか**: 算術平均は片方が高スコアなら全体も高くなりやすいが、調和平均は小さい方の値に強く引っ張られる性質があるため、片方向だけ高くもう片方が低い「怪しい」候補を自然に減点できる。
- **使うとどうなるか**: 前向き・後向き両方の推定が一致する頑健なリンクが優先され、単方向推定のノイズに弱い候補は抑制されやすい。一方で両方向のスコアリングを個別に計算する分、計算コストは単方向手法より増える。
- **初出**: 2026-08-07 / Biohub - Cell Tracking During Development

### 欠損パターン別メタスタッキング（Missingness-Regime-Aware Meta-Stacking）
- **意味**: サンプルを「どの特徴量が欠損しているか」というパターン（欠損レジーム）ごとにバケット分けし、レジームごとに得意なベースモデルの重みを変えてメタモデルで統合するスタッキング手法。
- **いつ使うか**: 特徴量の欠損パターンが複数存在し、レジームによって効くモデルが異なることが疑われるとき（アンケート・センサーデータ等で回答項目が人によって異なる場合など）。
- **なぜ使うか**: 全サンプルに同一のブレンド重みを使うと、欠損パターンごとの得意不得意が均されてしまい、レジーム特化の精度向上を取り逃す。
- **使うとどうなるか**: レジームごとに最適化された重みで精度が底上げされる一方、レジームの数が多すぎるとバケットあたりのサンプル数が減り、メタモデルの学習が不安定になりやすい。
- **初出**: 2026-08-07 / Predicting Smartphone Addiction (Playground Series S6E8)

### ターゲット別クエリAttentionヘッド（Target-Specific Query Attention Head, SlotHead）
- **意味**: マルチラベル分類タスクで、ラベル（診断項目）ごとに専用の学習可能なクエリベクトルを持つAttentionヘッドを用意し、各ラベルが入力のどの部分（例: どのMRIシーケンス）に注目すべきかを個別に学習させる設計。
- **いつ使うか**: 1つの入力（複数シーケンスの画像等）から複数の独立したラベルを予測するタスクで、ラベルごとに重要な入力部分が異なると考えられるとき。
- **なぜ使うか**: 全ラベル共通の特徴量プーリングでは、あるラベルにしか効かない部分特徴が他のラベルの予測に埋もれてしまう。ラベルごとに専用クエリを持たせることで、ラベルに特化した注目箇所を学習できる。
- **使うとどうなるか**: ラベルごとの解釈可能性（どのシーケンスに注目したか）が得られやすくなる一方、ラベル数に比例してクエリ・パラメータ数が増える。
- **初出**: 2026-08-07 / RSNA Knee Abnormality Detection

### ランクベースアンサンブル（Rank-based Ensembling / Rank Blending）
- **意味**: 複数モデルの予測値をそのまま平均するのではなく、各モデル内で予測値を順位（ランク）に変換してから順位を平均・統合するアンサンブル手法。
- **いつ使うか**: 評価指標がAUCのように「絶対値ではなく順位（ランキング）の正しさ」を問う場合、またはモデルごとに予測値のスケール・キャリブレーションが大きく異なる場合。
- **なぜ使うか**: 生の予測値をそのまま平均すると、出力スケールが大きいモデルが結果を支配してしまうことがある。ランクに変換すれば全モデルが同じ0〜1のスケールで扱われ、スケール差の影響を受けずに統合できる。
- **使うとどうなるか**: モデル間のキャリブレーション差に頑健になる一方、順位に変換する際に予測値同士の「差の大きさ」の情報は失われる。
- **初出**: 2026-08-07 / RSNA Knee Abnormality Detection

### 拡散モデル中間表現による特徴抽出（Diffusion-based Representation Learning, DDAE-style Feature Extraction）
- **意味**: 画像生成用の拡散モデル（ノイズ除去=denoisingを学習するモデル）を、生成目的ではなく表現学習として使う手法。ラベルなし画像で拡散モデルを事前学習し、ノイズ除去過程の特定のtimestep・特定の中間層（bottleneck）から取り出した特徴量を、分類・セグメンテーションなど下流タスクの入力として転用する。
- **いつ使うか**: 対象ドメイン（例: 特定部位のMRI）にラベルなし画像は大量にあるがラベル付き画像が少ないとき。DINOv2等の対照学習・自己蒸留系の基盤モデルと並ぶ、もう一つの自己教師あり事前学習の選択肢。
- **なぜ使うか**: ノイズ除去という生成タスクを解く過程で、画像を多段階の抽象度で捉える階層的な特徴が自然に学習される。どのtimestep・層を使うかをvalidationで選べば、タスクに合った抽象度の特徴を凍結/軽微調整だけで取り出せる。
- **使うとどうなるか**: ラベルが少ない設定（例: 訓練ラベル10%）でも教師ありCNNより性能劣化が小さい「ラベル効率」の高さが報告されている。一方、拡散モデル自体の事前学習コストは高く、どのtimestep/層が最適かを探索する追加の手間がかかる。
- **初出**: 2026-08-08 / Paper Digest（OrthoDiffusion, arXiv:2602.20752） — RSNA Knee Abnormality Detectionコンペで使われているDINOv2凍結特徴抽出（対照学習系）と対比できる、拡散モデル系の自己教師あり特徴抽出の実例。

### ローカル関連性ランカー（Local Association Ranker、22特徴量による候補リンク再スコアリング）
- **意味**: フレーム間の候補リンク（エッジ）ごとに、エッジ確率・ノードの入出次数・局所密度・候補順位・距離・速度など22個の特徴量を計算し、それらを入力とする軽量な学習済みモデルで「このリンクが本物らしいか」を再スコアリングする手法。
- **いつ使うか**: 距離やHungarian法のような単純な幾何的マッチングだけでは、周囲の混雑度や候補の相対順位といった文脈情報を捉えきれない追跡・マッチングタスクで、候補選択の精度をさらに上げたいとき。
- **なぜ使うか**: 生の距離・確率だけでは「近いが実は別の細胞」「遠いが実は同じ細胞」のような紛らわしいケースを区別しにくい。22個の特徴量に文脈情報（周辺密度、候補の相対順位など）を含めることで、単純な幾何指標より柔軟な判断ができる。
- **使うとどうなるか**: 既存の検出・グラフ最適化パイプラインを変更せずに、リンク選択の一段階だけを差し替えられるため、既存パイプラインとの統合コストが低い。一方、公開モデルを別の実行環境で使う際は、学習時と同じ特徴量名・スケールが再現されているかを慎重に検証する必要がある（表記ゆれを吸収するエイリアス機構が必要になることが多い）。
- **初出**: 2026-08-10 / Biohub - Cell Tracking During Development

### レポート由来の弱教師あり学習（Report-Derived Weak Supervision）
- **意味**: 画像に直接付いた正解ラベルの数が極端に少ない（例: 4,407件中58件）場合に、同じ学習データに付随するテキスト（放射線科医の読影レポートなど）をルールベースまたは学習済みモデルで解析し、連続値・グレード付きの代理ターゲットへ変換した上で、そのターゲットに対して画像だけを使うモデルを学習する手法。推論時にテキストが手に入らない（テストデータにレポート列がない）タスクで特に有効。
- **いつ使うか**: 正解ラベルの絶対数が少ないが、同じデータに別モダリティ（テキストなど）の情報が豊富に存在し、かつ推論時にはそのモダリティが使えない制約があるとき。
- **なぜ使うか**: 58件では画像モデルの学習に足りないが、4,407件のテキストから抽出した代理ラベルなら十分な学習データ量を確保できる。抽出したラベルの質は、少数の正解ラベルとのAUC一致度で検証できる。
- **使うとどうなるか**: ラベル数が大幅に増える一方、テキスト抽出の精度がボトルネックになる。「言及なし＝陰性」という仮定が所見によっては妥当でないことがあり、グレード評価（強く肯定・弱く肯定・言及なし・明示的に否定）のように段階的に扱うと、コンペの評価指標が確率の絶対値ではなく順位のみを見る場合（AUCなど）と特に相性が良い。
- **初出**: 2026-08-10 / RSNA Knee Abnormality Detection

### モデルの指紋照合（Model Fingerprinting）
- **意味**: 固定シードから生成した合成入力をモデルの前処理からフォワードパス全体に通し、その出力を「指紋」として記録・比較することで、学習時と推論時で前処理（正規化、リサイズ順序など）が一致しているかを検証する手法。
- **いつ使うか**: 学習済み重みを、学習時とは別のノートブック・別の実行環境で読み込んで推論する場合。特に前処理の順序やスケーリングを間違えても例外が発生せず、もっともらしい誤った予測が出力されてしまうリスクがあるとき。
- **なぜ使うか**: 前処理のズレはエラーにならず「一見正常だが精度が落ちている」結果を生むため、通常のテストでは気づきにくい。合成入力に対する固定の出力を照合基準にすれば、実データを使わずに前処理の一致を機械的に検証できる。
- **使うとどうなるか**: GPU間の数値誤差（1e-5程度）より大きい許容誤差を設定することで、無関係な誤差要因を無視しつつ本質的な不一致だけを検知できる。導入コストは小さいが、モデルのアーキテクチャが変わるたびに指紋を取り直す必要がある。
- **初出**: 2026-08-10 / RSNA Knee Abnormality Detection

### GPU実行プローブ（CUDA Execution Probe）
- **意味**: `torch.cuda.is_available()`のようなAPIレベルの確認だけでなく、実際に小さなモデル（畳み込み層など）をGPU上で1回動かしてみることで、現在のライブラリのビルドがそのGPU世代の演算カーネルに実際に対応しているかを検証する手法。
- **いつ使うか**: クラウド/共有GPU環境（Kaggle等）で、割り当てられるGPUの世代が実行のたびに変わりうり、かつ一部の古い世代（P100等）が現在のライブラリビルドと非互換になりうるとき。
- **なぜ使うか**: `is_available()`はGPUの「存在」しか確認せず、「そのGPUで今のカーネルが実際に動くか」までは保証しない。非互換なGPUで学習・推論を強行すると、原因不明のエラーで長時間の実行が無駄になる。
- **使うとどうなるか**: 実行の冒頭で問題を検知し、CPUへ確実にフォールバックしたり、別の実装（異なるバッチ構成・軽量スケジュール）に切り替えたりする判断が早期にできる。
- **初出**: 2026-08-10 / RSNA Knee Abnormality Detection

### 動的べき乗重み付けアンサンブル（Dynamic Power-Weighted Non-Linear Blending）
- **意味**: 複数モデルの予測を混ぜる際、各モデルのスコアを[0,1]に正規化した上でべき乗（例: 4乗）して重みとする手法。スコア差をべき乗で誇張することで、最高スコアのモデルに重みをほぼ集中させつつ、他のモデルは僅かな重みでタイブレーカーとして働かせる。
- **いつ使うか**: 複数の公開notebookの予測がほぼ同じ内容で強く相関しており、単純な線形平均では新しい情報が増えないとき。
- **なぜ使うか**: 線形の重み付け（スコア差にそのまま比例）では僅差のモデル同士がほぼ同じ扱いになってしまうが、べき乗を使うと「わずかに良いモデル」により強く票を集められる。
- **使うとどうなるか**: 最高スコアのモデルへの依存度が非常に高くなるため、そのモデル自体が過学習気味だとブレンド全体もそれに引きずられるリスクがある。ランク変換とlogit変換（後述）を組み合わせて予測の「裾」を引き伸ばしてから使うと、AUC系の指標でさらに効果が出やすい。
- **初出**: 2026-08-11 / Predicting Smartphone Addiction (Playground Series S6E8)

### デュアルGPUワークキューとフェイルセーフ部分保存（Dual-Device Work Queue with Banked Partial Submissions）
- **意味**: 複数モデル（アンサンブルのメンバー）による推論を、複数GPUが共有のキューから空いた順に取り出して並列実行し、1メンバーの推論が終わるたびに提出ファイルを都度上書き保存する設計。あるメンバーが片方のGPUで失敗した場合はもう片方で再試行し、それでも失敗したメンバーは1票を失うだけで全体の実行は継続する。
- **いつ使うか**: 実行時間に制限があるKaggle Notebook環境で、多数のモデルを使ったアンサンブル推論を複数GPUで行うとき。
- **なぜ使うか**: 1台のGPUで順番に処理すると時間切れで後半のモデル・TTAウィンドウを丸ごと諦めることになりやすい。キュー方式で両GPUを常に稼働させることで、同じ時間内により多くのモデルを推論できる。また部分保存により、実行が途中で打ち切られても「それまでに完了した分だけのアンサンブル」が確実に提出される。
- **使うとどうなるか**: 単一GPU・逐次処理と比べて同じ時間内でより多くのメンバーを使い切れる（元notebookでは単一GPU実行の公開スコア0.847に対し、この手法で0.891相当のパッケージを使用）。実装はやや複雑になり、GPU間の競合状態（race condition）を避けるためのロック機構が必要になる。
- **初出**: 2026-08-11 / RSNA Knee Abnormality Detection

### ターゲット別TTAプーリング（Per-Target TTA Pooling Strategy）
- **意味**: Test Time Augmentation（TTA）で得られた複数の予測（例: 重複するスライスウィンドウごとの予測）を統合する際、ラベル（診断項目）の性質によって統合方法を変える手法。局所的で見逃しやすい所見（骨折など）は最大値や上位k件の平均でプールし、広がりのある所見（変形性関節症など）は単純平均でプールする。
- **いつ使うか**: マルチラベルのタスクで、ラベルごとに「病変の広がり方」の性質が大きく異なるとき。
- **なぜ使うか**: 単純平均だけだと、1つのウィンドウにしか写っていない小さな病変の兆候が、写っていない他のウィンドウの予測で薄められて見逃されてしまう。ラベルの性質に応じてプール方法を変えることで、局所的な所見の感度を保ちつつ、広範囲な所見は安定した平均値で評価できる。
- **使うとどうなるか**: 局所所見の再現率（recall）が向上しやすい一方、どのラベルにどのプール方法が適切かは分析・実験が必要で、汎用的な自動選択は難しい。
- **初出**: 2026-08-11 / RSNA Knee Abnormality Detection

### 因子分解機（Factorization Machine, FM）
- **意味**: 各特徴量の値（フィールド, 値）ごとに低次元の潜在ベクトルを1つ割り当て、2つの特徴量間の交互作用をそのベクトル同士の内積で表現するモデル。`0.5*(sum(v)^2 - sum(v^2))`という恒等式を使うことで、全ペアを列挙せず1回のパスで全交互作用項を計算できる。
- **いつ使うか**: カテゴリ・数値をひっくるめた高カーディナリティな特徴量同士の組み合わせ（ペア）が予測に効くが、素朴なペアのターゲットエンコーディングでは組み合わせの格子が疎になりすぎて（1セルあたりのサンプル数が少なすぎて）うまく学習できないとき。
- **なぜ使うか**: 決定木モデルは連続値の非線形な関係を分岐の繰り返しで近似するが、値ごとの細かい対応関係（ルックアップキー的な構造）を持つ合成データでは学習効率が悪い。FMは値ごとに直接ベクトルを持つため、似た値同士でベクトルを共有（情報を借用）でき、疎なデータでも頑健に学習できる。
- **使うとどうなるか**: 木モデル中心のアンサンブルに対して相関の低い（decorrelatedな）予測を追加でき、スタッキングの多様性向上に貢献しやすい。一方、埋め込み次元kや正則化（重み減衰）の調整を誤ると、疎な値に対応するベクトルが過学習しやすい。
- **初出**: 2026-08-12 / Predicting Smartphone Addiction (Playground Series S6E8)

### 部分的ファインチューニング（Partial Fine-tuning / Selective Layer Unfreezing）
- **意味**: 事前学習済みモデルの大部分（特に浅い層）のパラメータを凍結（`requires_grad=False`）したまま、最後の数ブロックだけを学習可能にして新しいタスクに適応させる転移学習の手法。
- **いつ使うか**: 下流タスクのデータ量が、事前学習済みモデル全体を再学習するには不十分（過学習・表現崩壊のリスクが高い）が、事前学習された表現をある程度タスク特化させたいとき。
- **なぜ使うか**: 自己教師あり学習された画像モデルの浅い層は汎用的なエッジ・テクスチャ検出器であることが多く、深い層ほどタスク特有の意味理解を担う。浅い層まで学習を許すとデータ不足で汎用表現そのものを壊してしまうリスクがあるが、深い層だけ開放すれば汎用性を保ちながらタスクに適応できる。
- **使うとどうなるか**: 全層ファインチューニングよりパラメータ数が少なく過学習しにくい一方、「どこまで開放するか（`unfreeze_last`）」の選び方次第で性能が変わり、タスク・データ量に応じた検証が必要。
- **初出**: 2026-08-12 / RSNA Knee Abnormality Detection

### スコア自動検出ブレンダー（Future-Proof Score-Autodiscovery Ensembling）
- **意味**: ディレクトリを再帰的に走査し、ファイル名に含まれるスコアらしき数値パターン（例: `0.97092`）を正規表現で検出することで、新しく追加された提出済みCSVファイルを自動的にアンサンブルの入力として取り込む設計。どのファイルを使うかをコード内にハードコードしない。
- **いつ使うか**: 複数の学習済みモデルの提出ファイルをKaggle Dataset等に追加していきながら、都度アンサンブル用のコードを書き換えずに新しいモデルを組み込みたいとき。
- **なぜ使うか**: ファイルパスやモデル名を決め打ちで書くと、モデルを追加・入れ替えるたびにコード修正が必要になり、ヒューマンエラー（古いファイルの参照残り等）の原因になる。ファイル名の命名規則自体を「発見可能な情報源」として使うことで、この保守コストをなくせる。
- **使うとどうなるか**: 新モデルの追加がデータセットの更新だけで完結し、コード変更・再デバッグの手間が減る。一方、命名規則が崩れたファイル（スコアの記載形式が違う等）は自動検出から漏れるため、命名規則自体の一貫性の維持が前提条件になる。
- **初出**: 2026-08-13 / Predicting Smartphone Addiction (Playground Series S6E8)

### 幾何平均によるアンサンブルブレンド（Geometric Mean Blending, `scipy.stats.gmean`）
- **意味**: 複数モデルの予測確率を単純な算術平均ではなく、各値の積のN乗根で求める幾何平均で統合する手法。0に近い極端な低確率が1つでも混じると、全体の幾何平均も強く引き下げられる性質を持つ。
- **いつ使うか**: ROC-AUCのように「自信満々な誤り」に対して敏感な評価指標で、複数モデルの予測を統合したいとき。
- **なぜ使うか**: 算術平均は極端な値の影響が薄まりやすいが、幾何平均は「どれか1モデルが強く否定した」場合にその情報を統合結果に強く反映できるため、over-confidentな誤り同士が算術平均で相殺されてしまう事態を避けやすい。
- **使うとどうなるか**: 極端な低確率を出したモデルが1つでもあると全体のスコアが大きく下がるため、事前に`np.clip`で確率を[1e-6, 1-1e-6]程度に丸めておくなど、0除算・極端値への対策が必須になる。
- **初出**: 2026-08-13 / Predicting Smartphone Addiction (Playground Series S6E8)

### 整数計画法によるフレーム間対応付け（ILP-based Assignment）
- **意味**: 前フレームと次フレームの候補ペア（エッジ）それぞれにコスト（または報酬）を割り当て、「各ノードの入出次数の制約（例: 分裂は出力2本まで）」を満たしつつ総コストが最適になるエッジの組み合わせを、整数計画法（Integer Linear Programming）のソルバーで厳密に求める手法。
- **いつ使うか**: 単純な1対1マッチング（Hungarian法）では表現できない「1つの親細胞が2つの娘細胞に分裂する」といった1対多の対応関係を含むトラッキング問題。
- **なぜ使うか**: 貪欲法や単純な最近傍探索では、局所的には良く見えても大域的には矛盾した対応付け（同じノードに二重にリンクする等）が生じやすい。ILPは出現・消失・分裂といった複数種類のイベントを同じ目的関数の中で明示的にコスト化し、制約を満たす大域最適解を保証する。
- **使うとどうなるか**: 出現(appearance)・消失(disappearance)・分裂(division)それぞれの重み付けを変えるだけでトラッキングの挙動（分裂を検出しやすくする／トラックの継続を優先する等）を調整できる一方、候補エッジ数が多いと計算コストが増えるため、事前のゲーティング（距離が離れすぎたペアを候補から除外する等）が併用されることが多い。
- **初出**: 2026-08-14 / Biohub - Cell Tracking During Development

### ギャップクロージング（Gap Closing / Track Stitching）
- **意味**: 検出漏れ（該当フレームで対象が検出されなかった）によって途切れたトラックを、数フレーム先の再出現ノードと距離条件付きで再接続する後処理。
- **いつ使うか**: 検出モデルの性能が完璧でなく、密集・オクルージョン・ノイズなどで一部フレームだけ検出が抜け落ちるトラッキングタスク全般。
- **なぜ使うか**: 検出漏れをそのまま放置すると1本の実際のトラックが複数の短い断片として評価されてしまい、エッジベースの指標（例: edge Jaccard）を大きく損なう。許容ギャップ数と距離しきい値を設定して再接続することで、検出モデル側を再学習しなくてもトラッキング指標を底上げできる。
- **使うとどうなるか**: 許容ギャップや距離のしきい値を緩くしすぎると、本来は別々の物体を誤って接続してしまう（偽陽性エッジの増加）リスクとのトレードオフになる。密度に応じてしきい値を動的に変える「adaptive」な設計もよく使われる。
- **初出**: 2026-08-14 / Biohub - Cell Tracking During Development

### Nested Honest Cross-Validation（入れ子の誠実な交差検証）
- **意味**: アンサンブルの重みやスタッキングのメタモデルを「学習用fold」だけで学習し、それ以外の「held-outなfold」だけでスコアを測る、という手順をfoldを入れ替えながら繰り返す検証方法。特徴量やスケーラの統計量（平均・標準偏差など）も学習用foldだけで再計算し、held-out側の情報が一切学習側に漏れないようにする。
- **いつ使うか**: 複数モデルのOOF（out-of-fold）予測を混ぜ合わせる際に、「本当にheld-outデータで効果があるのか」を、公開リーダーボードに頼らず自分のCVだけで確かめたいとき。
- **なぜ使うか**: 単純に全データでメタモデルを学習してから同じ全データでスコアを測ると、メタモデルがその特定のデータに適合しているだけの「in-fold過適合」を見逃してしまう。nested CVは「学習に使っていないデータでどれだけ一般化するか」を測るため、公開LBとの整合性が高い、より信頼できる評価になる。
- **使うとどうなるか**: 計算コストは通常のCVより増える（fold数だけ特徴量やスケーラの再計算が必要）が、「本当に効く工夫」と「見かけ上のノイズ」を統計的に(t値や標準誤差で)区別できるようになり、公開LBの僅かなスコア差に振り回されにくくなる。
- **初出**: 2026-08-14 / Predicting Smartphone Addiction (Playground Series S6E8)

### Rank/Logitデュアル表現によるスタッキング（Dual Rank-Logit Representation for Stacking）
- **意味**: アンサンブルのメタモデルに各メンバーの予測を渡す際、正規化ランク（`norm_rank`、スケールに頑健）とロジット変換値（`logit(clip(p, 1e-6, 1-1e-6))`、極端な確信度の強弱を保持）の2列を両方与える表現方法。
- **いつ使うか**: ROC-AUCのようにランキングの正しさを評価する指標のもとで、複数モデルの予測を1つのメタモデル（ロジスティック回帰など）でブレンドしたいとき。
- **なぜ使うか**: ランクだけだとメンバー間のスケールの違いを気にせず安定して扱えるが、「どれだけ極端に自信を持っているか」というテール情報が失われる。ロジット値はそのテール情報を保持するため、両方を与えることでメタモデルが状況に応じて使い分けられる。
- **使うとどうなるか**: 特徴量の次元がメンバー数の2倍になるため多少の計算コスト増はあるが、nested CVで検証する限り単一表現より安定して（かつ全foldで） AUCが改善しやすい、比較的低コストな改善レバーとなる。
- **初出**: 2026-08-14 / Predicting Smartphone Addiction (Playground Series S6E8)

### 臨床否定表現検出（Clinical Negation Detection）
- **意味**: 放射線科レポートなどの自由記述テキストから、「no evidence of tear」のように所見を打ち消す否定語（前置・後置）を検出し、対象の病理表現がその否定語の作用範囲（一定の文字数ウィンドウ内）に入っているかどうかで、その節が陽性か陰性かを判定する自然言語処理の手法（英語圏ではNegExというアルゴリズムが有名）。
- **いつ使うか**: 医療テキストのように「正常」の記載が圧倒的多数を占め、かつ否定の言い回しが多言語・多様な文体で書かれるデータから、構造化されたラベル（所見あり/なし）を抽出したいとき。
- **なぜ使うか**: 単純なキーワードマッチ（「tear」という単語があれば陽性、とみなす）では、「no tear」のような否定文を誤って陽性と判定してしまう。否定表現とその作用範囲を明示的にモデル化することで、この典型的な誤りを防げる。
- **使うとどうなるか**: 否定語と対象語の間に"but/however"のような転換の接続詞がある場合は否定を打ち消す、といった細かい例外処理が必要になり、ルールベースのシステムは徐々に複雑化しやすい。一方、正しく設計できれば大規模言語モデルを使わずとも高精度な構造化ラベル抽出が可能になる。
- **初出**: 2026-08-14 / RSNA Knee Abnormality Detection


### rank-gauss変換（Rank-Gauss / Gauss-Rank Transform）
- **意味**: 値を順位に変換し、[0,1]に正規化してから正規分布の分位点関数 `norm.ppf` に通す変換。`gauss(v) = norm.ppf(clip((rank(v)-0.5)/n, 1e-7, 1-1e-7))`。
- **いつ使うか**: 出力スケールがバラバラな複数モデルの予測をスタックの入力にするとき。特に「あるライブラリは確率[0,1]、別のライブラリは生のFMスコア[-14,+48]」のように、単位が揃わない配列を混ぜる場合。ニューラルネットに歪んだ分布の数値特徴を入れる前処理としても使う。
- **なぜ使うか**: 単なる順位（rank）だけだと分布の端（極端に高い/低い値）の解像度が失われる。ガウス分位点に写すと**端が引き伸ばされて解像度が戻る**ため、ターゲットが飽和する領域の識別力が保たれる。
- **使うとどうなるか**: S6E8では logit空間に対して **+0.00008** の改善（小さいが安定して正）。副作用として、変換後は必ず `StandardScaler` を挟むこと（挟まないとlbfgsが収束しない）。またこれは**単調変換なのでAUCは変わらない** — 効くのはあくまで「メタモデルの入力として扱いやすくなる」経路である点に注意。
- **初出**: 2026-08-17 / Playground Series S6E8

### Isotonic回帰によるOOFキャリブレーション（Isotonic Calibration）
- **意味**: 「単調増加であること」だけを仮定して、モデルの出力スコアを実際の確率に写すノンパラメトリックな校正手法。`sklearn.isotonic.IsotonicRegression`。
- **いつ使うか**: (a) 指標がlog-lossやBrier scoreで確率の絶対値が問われるとき、(b) AUC指標でも**ベイズ最適AUCの推定など、確率の絶対値を必要とする分析**をしたいとき、(c) 予測を閾値で切って業務判断に使うとき。
- **なぜ使うか**: Platt scaling（シグモイド当てはめ）より柔軟で、S字でない歪みも補正できる。データが数万行以上あれば過学習の心配は小さい。
- **使うとどうなるか**: 十分位ごとの「予測平均 vs 実測平均」（reliability table）がほぼ対角線に乗るようになる。**必ずout-of-foldで当てはめること** — 同じ行で当てはめて同じ行を校正すると、確率が過度に自信過剰になり、後段の分析（天井推定など）が壊れる。またAUC指標のコンペでは、校正そのものはスコアを1ミリも動かさない（単調変換だから）。
- **初出**: 2026-08-17 / Playground Series S6E8

### Leave-One-Author-Out アブレーション（寄与の帰属分析）
- **意味**: 共有プールをスタックする際、「ある投稿者が公開した予測配列を全部抜いてスタックを組み直し、スコアがどれだけ落ちるか」を測る実験。
- **いつ使うか**: 公開OOFライブラリを寄せ集めてスタックしているコンペで、**プールのどの部分が実際に価値を持つのか**を知りたいとき。自分の追加モデルが公開プールに対して何の上乗せをしているかを測るときにも使う。
- **なぜ使うか**: 「入れたらスコアが上がった」は交絡が多いが、「抜いたら下がった」は寄与の証明として強い。またメンバー数の多寡と寄与は比例しない（67配列で+0.000016、22配列で+0.000057という逆転が実際に起きる）ことが分かる。
- **使うとどうなるか**: どこに追加投資すべきかが決まる。**2つの落とし穴**: (1) 帰属はメンバー名でなく**読み込み元のデータセットのスラッグ**で行う（配列名は `catnative` のように素っ気なく、モデル名で命名されたライブラリもあるため、名前で判断すると別人にクレジットする）。(2) 69万行のnested AUCで1e-6の差はfold分割の揺らぎに埋もれるので、下位群は**順位ではなく「ゼロと区別がつかない帯」として報告する**。計算コストは「投稿者数 × フルスタック学習」で、S6E8では9回で約14分。
- **初出**: 2026-08-17 / Playground Series S6E8

### 注意機構つきMultiple Instance Learning（Attention-based MIL）
- **意味**: 「袋（bag）にラベルが付いているが、袋の中のどの要素（instance）がその原因かは分からない」という設定の学習法。アテンション重みで「どの要素が重要か」をモデル自身に学習させる。
- **いつ使うか**: 医用画像で「この検査に病変がある」というstudy単位のラベルしかなく、**どのスライスに写っているかの注釈が無い**とき。病理のWSI（全スライド画像）、複数フレームの動画分類なども同じ構造。
- **なぜ使うか**: スライス単位の注釈は取得コストが極めて高い。MILならstudy単位のラベルだけで学習でき、副産物として「どのスライスを見たか」の可視化（説明性）が得られる。
- **使うとどうなるか**: max型プーリングは「1枚でも異常があれば陽性」という仮定に対応し、ACL断裂のように1〜2枚にしか写らない所見に強い。mean型は変形性関節症や関節液貯留のような広範囲の変化に強い。**両方を連結して分類器に選ばせるのが安全**。注意点として、(a) 陽性instanceが極端に少ないとアテンションが学習しにくい、(b) 関連する所見をグループ化してパラメータを共有すると、症例の少ない所見が多い所見から学習を借りられる、(c) 学習中にシリーズを丸ごとランダムに落とす `series_dropout` を入れると、欠損シリーズのあるstudyに強くなる。
- **初出**: 2026-08-17 / RSNA Knee Abnormality Detection

### Seed混合による検出場のアンサンブル（Multi-Seed Logit Blending of Detection Fields）
- **意味**: 同じアーキテクチャを初期化seedだけ変えて複数学習し、**最終予測ではなく中間の検出ヒートマップ（ロジット）の段階で**混ぜる手法。`d_shared = (1−α)d_A + α·d̃_B`。
- **いつ使うか**: 検出→後処理という多段パイプラインで、後処理が重い（ILPやグラフ最適化など）ために、後処理を複数回走らせたくないとき。
- **なぜ使うか**: 最終出力を平均する通常のアンサンブルと違い、**混合後に1回だけ後処理を走らせればよい**ので計算コストが1本分で済む。かつ、しきい値付近で不安定な検出（片方のseedだけが拾ったノイズ）が薄まり、両方が支持する検出だけが残る。
- **使うとどうなるか**: 検出の再現率をほぼ保ったまま精度が上がる。注意点は、(a) **ロジット空間で混ぜること**（確率空間で平均すると、片方が0.99・片方が0.01のとき0.5になって「迷い」が消えない）、(b) 2つのモデルの出力が空間的に位置合わせされている必要がある、(c) 混合係数 α は交差検証で決める（Biohubでは「near-balanced」=0.5付近）。
- **初出**: 2026-08-17 / Biohub - Cell Tracking During Development

### PLR（Periodic-Linear）数値埋め込み / 周期的数値埋め込み
- **意味**: 数値スカラー `x` を、学習可能なフーリエ周波数 `f` で `sin(2πfx)`, `cos(2πfx)` に展開してから線形変換し、埋め込みベクトルに変える手法。表形式データ×ニューラルネットで数値特徴を扱うための標準的な武器。
- **いつ使うか**: テーブルデータをTransformer/MLPで扱うとき。特に数値列に細かい非線形性があり、生のスカラーを1本だけ入れても表現しきれないとき。
- **なぜ使うか**: 生の `x` を線形層に入れると `wx+b` という直線しか作れず、細かい非線形性の表現に多くの層を要する。sin/cosを複数周波数で並べると**フーリエ級数のように任意の滑らかな関数を効率よく近似**でき、しかも周波数自体が学習可能なので**必要な解像度をモデルが自分で選べる**。NeRFのpositional encodingと同じ発想。
- **使うとどうなるか**: 少ない層数で滑らかな傾向を捉えられ、GBDTに対する表形式NNの弱点が埋まる。周波数の数 `k` が過大だと高周波成分に過学習し、過小だと表現力不足になるので、埋め込み次元との比率込みで調整が要る。カテゴリ用の埋め込みテーブルと**足し合わせる**構成にすると、離散的な暗記と連続的な傾向を列ごとに自動で使い分けられる。
- **出典**: Gorishniy et al., "On Embeddings for Numerical Features in Tabular Deep Learning" (arXiv 2203.05556)
- **初出**: 2026-08-18 / Playground S6E8（Predicting Smartphone Addiction）

### 折半法による信号検出（Split-Half Reliability / 残差の半分割相関）
- **意味**: データを独立な2つの半分に分け、それぞれで計算した統計量（ここでは「値ごとの残差平均」）の相関を取ることで、**ノイズと実在するシグナルを分離する**手法。
- **いつ使うか**: 「この特徴量の、この値には本当に意味があるのか？」を確かめたいとき。特に合成データで「生成器が値→ラベル対応を暗記していないか」を検証したいとき。単変量AUCが0.5付近なのに何か効いている気がする、という場面。
- **なぜ使うか**: 素朴に「値ごとのラベル平均」を見ると**構成効果**（その値を持つ行がたまたま別の属性に偏っている）に騙される。そこで①まずモデルを噛ませて残差を取り（モデルが説明できる分＝構成効果を除去）、②残差を独立な2つの四半分で集計して相関を見る。ノイズなら相関は0（帰無仮説）なので、**0でない相関は両方に共通して存在する実在の信号**の証拠になる。
- **使うとどうなるか**: 「効いている気がする」を統計的な証拠に変えられる。相関0.72のような値が出れば、その列に**値レベルの埋め込み（ルックアップ表）や target encoding を入れる**という設計判断が正当化される。注意点として、サンプル数の少ない値を除外するフィルタ（例: 各半分で150件以上）を必ず入れないと、少数サンプル由来の偶然の一致が相関を膨らませる。
- **初出**: 2026-08-18 / Playground S6E8

### パラメータ群別 weight decay（Parameter-Group-Specific Weight Decay）
- **意味**: モデルの部位ごとに異なる強さの weight decay（L2正則化）を設定すること。例: 埋め込みテーブルだけ `3e-4`、それ以外は `1e-5`（30倍差）。
- **いつ使うか**: モデル内に**過学習しやすさが極端に違う部位**があるとき。典型は埋め込みテーブル（値ごとに独立したパラメータを持つため、レアな値の埋め込みが数行のノイズを丸暗記する）。
- **なぜ使うか**: モデル全体に一律の正則化をかけると、埋め込みに合わせれば他が過剰に縛られ、他に合わせれば埋め込みが過学習する。部位ごとに分ければ**それぞれ最適な強さ**を選べる。
- **使うとどうなるか**: サンプル数の少ない値の埋め込みは0に引き戻され、十分なデータがある値だけが独自の表現を保持する。これは実質的に **target encoding の平滑化（smoothing）と同じ効果を、勾配降下で自動的に実現**していることになる。強すぎると暗記が効く列でも情報が消えるため、**最も効くハイパーパラメータの1つ**として探索する価値が高い。PyTorchでは `optim.AdamW([{'params': A, 'weight_decay': x}, {'params': B, 'weight_decay': y}])` で指定する。
- **初出**: 2026-08-18 / Playground S6E8

### 重みのEMA（Exponential Moving Average of Weights）
- **意味**: 学習中のモデル重みの指数移動平均を別に保持し、**検証・推論にはその平均側を使う**手法。
- **いつ使うか**: SGD/Adam系で学習するほぼすべての場面。特にバッチサイズが小さい、学習率が高い、データが少なくて振動しやすいとき。
- **なぜ使うか**: 確率的最適化は最適解の周りを振動し続ける。その軌跡を平均すると振動が打ち消され、**より平坦で汎化しやすい解**に近づく。「タダで手に入る軽量なアンサンブル」と考えると理解しやすい。
- **使うとどうなるか**: 検証スコアが安定し、多くの場合わずかに向上する。追加コストは重みのコピー1つ分のメモリと更新の計算だけ。**必ずEMA側で検証する**こと（本番で使う重みで選ぶのが筋）。学習の最終盤にしか効かないので、エポック数が極端に少ないと恩恵が薄い。OneCycleLRなど学習率を最後に下げるスケジュールと相性が良い。
- **初出**: 2026-08-18 / Playground S6E8

### Mean-Max デュアルプーリング（Mean-Max Pooling）
- **意味**: 系列やインスタンス集合を集約する際、平均プーリングと最大プーリングの**両方を計算して連結**し、どちらを使うかを後段のヘッドに学習させる手法。
- **いつ使うか**: マルチラベル問題で、**局所的な所見とびまん性の所見が混在**しているとき。医用画像のMILが典型例。
- **なぜ使うか**: max は「**どれか1つに強い証拠があれば陽性**」を表現する（骨折・靭帯断裂のように1スライスにしか写らない局所的な所見に適する）。mean は「**全体的にどうか**」を表現する（関節液貯留・滑膜炎のように広範囲に薄く現れる所見に適する）。片方だけだと、局所所見は平均で薄まって消え、びまん性所見は最大値がノイズの1点に引っ張られる。
- **使うとどうなるか**: ラベルごとに適した集約をモデルが選べるようになり、特に稀で局所的なラベルのAUCが改善しやすい。特徴次元が2倍になるのでヘッドのパラメータが増える点だけ注意。
- **初出**: 2026-08-18 / RSNA Knee Abnormality Detection

### ターゲットのグループ化による表現共有（Target Grouping / Grouped Multi-label Heads）
- **意味**: マルチラベル分類で、意味的に近いラベルをグループにまとめ（例: 靭帯 / 半月板 / OA / 炎症 / 骨）、**グループ内で中間表現を共有**させる設計。
- **いつ使うか**: ラベル間の陽性数の差が大きく、かつラベル間に**画像所見・生成機序の類似性**があるとき。macro平均で評価される場合は特に効果が大きい。
- **なぜ使うか**: 陽性が数十件しかないラベル（例: Fracture）は単独では良い表現を学習できない。同じグループの陽性が多いラベル（例: Contusion）と表現を共有すれば、「骨の異常はこう見える」という表現を**借りられる**。転移学習をラベル間で行っているようなもの。
- **使うとどうなるか**: 稀なラベルのAUCが目立って改善する。macro-AUCは稀ラベルにも等しい重みを与えるため、全体スコアに直結する。一方でグループ分けが不適切だと**関係ないラベル同士が干渉**して両方悪化するため、グループはドメイン知識に基づいて決めること。
- **初出**: 2026-08-18 / RSNA Knee Abnormality Detection

### エッジポリシーveto（非対称閾値による保守的なグラフ修復）
- **意味**: 学習済みのエッジ分類器（ポリシー）を、グラフのエッジを**追加するためには使わず、削除するためだけに使う**設計。しかも削除の閾値をOOFのF1最適閾値より**意図的に低く**設定する（例: τ=0.35）。
- **いつ使うか**: 主モデルが既に最も強い信号を持っており、補助モデルは「明らかな誤りの検出」にしか信用できないとき。トラッキングやグラフ構築の後処理全般。
- **なぜ使うか**: 補助ポリシーに追加もさせると、主モデルが持っていない誤ったエッジを増やしてPrecisionを壊す。逆に削除閾値を高く（＝積極的に削除）すると正しいエッジまで消えてRecallが落ち、F1が下がる。**「よほど自信を持って否定するときだけ消す」**という非対称性が、F1に対して最も安全な使い方になる。
- **使うとどうなるか**: Recallをほぼ維持したままPrecisionだけが上がる。加えて「既に2本の出力エッジを持つノード（＝分裂と判定済み）は保護対象から外さない」といった構造的な例外を併用すると、稀で重要なイベントを誤って壊さずに済む。閾値が恣意的になるのが弱点で、ポリシー確率をILPの目的関数の係数に組み込めば一貫性は上がる。
- **初出**: 2026-08-18 / Biohub - Cell Tracking During Development

### no-regressionゲート（No-Regression Gate for Ensembling）
- **意味**: アンサンブルに新しいモデル/アームを追加する際、**ターゲットごとにスコアが悪化しないことを自動判定**し、悪化するなら追加を採用しない仕組み。失敗時は追加前の提出をバイト単位で保持する。
- **いつ使うか**: 段階的にアームを積み上げる大規模アンサンブル。特にマルチラベルでmacro平均が指標のとき。
- **なぜ使うか**: アンサンブルは常に効くとは限らず、弱いアームを足すと悪化することがある。macro-AUCでは12ラベル中3つが改善して9つが悪化すれば全体は下がる。**全体平均だけ見ていると、この内訳が見えない**。
- **使うとどうなるか**: 「足したら下がった」を構造的に防げる。既に動いている良い結果を新しい試みが破壊しないという保証が得られるので、**安心して実験を積み上げられる**ようになる。閾値を厳しくしすぎると本来有効な追加も弾かれるため、ノイズフロアを測ってからゲートの許容幅を決めるのが望ましい。
- **初出**: 2026-08-18 / RSNA Knee Abnormality Detection

### 条件付き特徴量重要度（Conditional Feature Importance / 条件付けによる反証）
- **意味**: ある特徴量の価値を、**他の特徴量の値を固定した集団の中だけで**測り直す手法。単変量AUC（marginal）が「この列しか無かったら」を答えるのに対し、こちらは「他の列が既にあるとして、この列は**新しいこと**を言うか」を答える。
- **いつ使うか**: 単変量の重要度ランキングで中位に来た特徴量が、本物の信号なのか交絡による影なのかを判定したいとき。特徴量が互いに相関しているテーブルデータ全般。
- **なぜ使うか**: 特徴量Aが効いて見える理由が「本当に効く特徴量Bと相関しているから」であることは非常に多い。Bを固定してもなおAに予測力が残れば本物、0.500に落ちるなら影。**実装は素朴でよく**、主要変数の値でデータをセルに区切り、各セル内でAUCを測って平均するだけでも判定できる（各セルは十分なサンプル数を確保すること。少ないセルの推定は暴れる）。
- **使うとどうなるか**: 「効いて見えるが何も足していない」特徴量を捨てられる。ただし**この手法は組み合わせに対して構造的に盲目**であり、複数変数から作られる特徴量（残差など）は原理的に検出できない。また相関が高い状況では重要度を過小評価しうる。だから**条件付けの結論は必ずアブレーションで検証する**こと。両者が食い違ったら、それ自体が発見（例: 「価値の46%は欠損補完由来だった」）。
- **初出**: 2026-08-19 / Predicting Smartphone Addiction (Playground Series S6E8)

### 敵対的検証（Adversarial Validation）
- **意味**: 「この行はtrainかtestか」を当てる分類器を作り、そのAUCで train/test の分布の違いを測る手法。AUCが0.5なら区別不能（同じ分布）、明確に0.5より高ければ分布シフトがある。
- **いつ使うか**: CVの成績がLBに転移しないとき、あるいは転移するか事前に確認したいとき。コンペ序盤の定番チェック。
- **なぜ使うか**: 分布シフトがあるとCVは当てにならない。またシフトの強い行を検証データに寄せる、シフトを生む特徴量を落とす、といった対策の判断材料になる。
- **使うとどうなるか**: **重要なのは「特徴量をまとめて放り込まない」こと。** 「欠損パターンだけ」「観測値だけ」のように群を分けて複数回走らせると、**差異がどこに由来するかまで特定できる**。さらに、シフトを見つけただけでは行動できない点に注意。「train/testは区別できるが、その違いを生む欠損パターンはターゲットについて何も語らない」なら、シフトはあっても**予測には無関係**なので対策不要。**見つけた差異をターゲットとの関係まで追いかけて初めて意思決定できる**。
- **初出**: 2026-08-19 / Predicting Smartphone Addiction (Playground Series S6E8)

### 単調性制約（Monotone Constraints）
- **意味**: 勾配ブースティング木の分割に「この特徴量が増えたら予測値は下がってはいけない（またはその逆）」という制約を課す機能。LightGBM/XGBoost/CatBoost が `monotone_constraints=[1, 0, -1, ...]` の形で対応（1=単調増加、-1=単調減少、0=制約なし）。
- **いつ使うか**: ドメイン知識から単調性が確実に言える特徴量があるとき（利用時間が増えれば依存確率は上がる、面積が増えれば価格は上がる、等）。
- **なぜ使うか**: 木は柔軟すぎて、データのノイズが作った非単調な凹凸まで学習してしまう。制約を入れると仮説空間が狭まり、**過学習が減って汎化性能が上がる**ことがある。
- **使うとどうなるか**: 効果は小さいが安定して出やすい。**検証は「同じfold・同じシード・同じ学習回数で、変えたのはフラグ1つだけ」という対応のある比較（paired comparison）で行うこと**。差はシード間のばらつきと比較して初めて意味を持つ。なお、条件付けると実際には非単調な領域が存在する場合でも制約を入れて改善するなら、それは「その非単調性はノイズであり、モデルはそれを学習して過学習していた」という解釈になる。**発見をそのまま特徴量にせず、制約として入れる**という選択肢を持っておくとよい。
- **初出**: 2026-08-19 / Predicting Smartphone Addiction (Playground Series S6E8)

### 制約由来の残差特徴量（Constraint-Derived Residual Feature）
- **意味**: ドメインの物理的・会計的な制約（時間の合計は総時間以下、内訳の合計は総額に等しい、等）から導かれる引き算・割り算で作る特徴量。例: `daily_screen_time − (social + gaming + work) = その他の用途に使った時間`。
- **いつ使うか**: 「全体量」と「その内訳」が別々の列として与えられているデータ全般（時間配分、家計、在庫、栄養素など）。
- **なぜ使うか**: 個々の内訳は「何時間ゲームしたか」しか言わないが、残差は「**総量のうち説明のつかない部分がどれだけあるか**」を表す。多くのタスクで後者のほうがターゲットに直接的で、実際に単変量AUCが内訳の各列を大きく上回ることがある。
- **使うとどうなるか**: 単なる引き算なのに、交互作用項や高度な特徴量エンジニアリングより強いことがある。**そして条件付き重要度の分析では原理的に見つからない**（複数変数の組み合わせだから）。作ったら必ず**物理的にありえない値（残差が負など）が出ていないか件数を数える**こと。負の行が多ければ、データ生成過程が制約を守っていないという別の重要な発見になる。
- **初出**: 2026-08-19 / Predicting Smartphone Addiction (Playground Series S6E8)

### 外見コスト付きデータ対応付け（Appearance-Cost-Augmented Assignment）

- **意味**: フレーム間で物体（細胞・人・車など）を対応付けるとき、位置の距離だけでなく**「見た目の似ていなさ」をコストに加算**する手法。Biohub M001 では `cost += 2.0 * |logit(score_prev) - logit(score_curr)|`（µm単位）として検出スコアの急変にペナルティをかけている。
- **いつ使うか**: 追跡対象が密集していて、距離だけでは「どちらとつなぐべきか」が曖昧になる場面。特に、対象ごとに明るさ・サイズ・確信度といった**時間的に連続していそうな属性**が取れるとき。
- **なぜ使うか**: 近接した2物体がすれ違うと、位置だけのコストでは**IDの入れ替わり（ID switch）**が起きる。見た目が連続しているという事前知識を足すと、この乗り移りを抑えられる。距離コストと同じ単位（µm）にスケールを揃えて足すのがコツ。
- **使うとどうなるか**: Biohubの実測で +0.0016〜0.0022（VAL-24）。効果は小さいが安定。ただし**重み（ここでは2.0）はデータ依存のハイパーパラメータ**で、大きくしすぎると「見た目が似ているだけの遠い物体」を結んでしまう。検証データでの探索が必須。
- **初出**: 2026-08-20 / Biohub - Cell Tracking During Development

### 相互最近傍による細胞分裂の復元（Mutual-Nearest-Orphan Division Recovery）

- **意味**: 1対1の割当アルゴリズム（ハンガリー法など）では原理的に検出できない**1親→2娘の分裂**を、リンク後のグラフに対する後処理で救済する手法。「親の唯一の子 c1」と「親を持たない孤児ノード q」が**互いに最近傍**で、かつ**次フレームで距離が広がる**（`d(succ(c1),succ(q)) - d(c1,q) ≥ 閾値`）ときだけ、q を2人目の娘として追加する。
- **いつ使うか**: 追跡アルゴリズムが1対1割当ベースで、かつ評価指標が分裂イベントを含むとき。「分裂の検出数が0のまま増えない」という症状が出たら、モデルではなくこの構造的制約を疑う。
- **なぜ使うか**: 線形割当は数学的に必ず1対1なので、**モデルをいくら強くしても2人目の娘は絶対にリンクされない**。原因がモデルではなくアルゴリズムの形にあるため、学習側の改善では解決しない。
- **使うとどうなるか**: Biohub実測で分裂 TP/FP/FN が 0/0/14 → 6/31/8、総合 0.8387 → 0.8516。**FPが0→31に激増している**点に注意（精度は約16%）。それでもFNの減少が勝って正味プラスになるかは指標次第なので、**必ず副作用込みで検証してから採用する**。「分裂後に離れていく」という未来の情報を使う条件が、偽陽性を抑える鍵。
- **初出**: 2026-08-20 / Biohub - Cell Tracking During Development

### 疑似公開リーダーボード・シミュレーション（Pseudo-Public Leaderboard Simulation）

- **意味**: 手元のOOF予測から**実際の公開LBと同じ行数**をランダム抽出して「偽の公開LB」を作り、そこで最良の候補を選んだうえで、**選択に使わなかった残りの行**でその選択を評価する実験。選択による見かけの利得と、実際の利得の差を直接測る。
- **いつ使うか**: 公開LBの5桁目を追いかけ始めたとき。上位が僅差でひしめいていて、「この0.00005の改善は本物か」を判断したいとき。OOF予測と真のラベルが手元にあれば、どのコンペでも実施できる。
- **なぜ使うか**: 「公開LBが上がった」は**選択に使ったデータでの成績**であり、定義上、上振れしたものが選ばれる（**勝者の呪い**）。この実験は、その上振れ量を**推測ではなく実測**する唯一の方法。理屈で説得するより、自分のデータで数字を出すほうが強い。
- **使うとどうなるか**: S6E8の実測では、選択に使った59,260行では平均で正の利得が出る一方、**使わなかった行では平均でマイナス**になった。注意点として、候補数を少なくすると過学習量を**過小評価**する（多重検定の負荷が軽くなるため）。原著者自身「11候補・1信号は意図的に甘い設定であり、現実のLB探索はもっと過学習する」と断っている。層化抽出（`StratifiedShuffleSplit`）で陽性率を保つこと。
- **初出**: 2026-08-20 / Playground S6E8 (Predicting Smartphone Addiction)

### 決定論的タイブレーク付き厳密ランク化（Strict Rank with Deterministic Tie-Breaking）

- **意味**: 予測値を順位に変換する際、**同点の並べ方まで固定**する手法。`np.lexsort((tie_breaker, x))` で第2キーを与え、全行がユニークな順位（`(i+0.5)/n`）を持つようにする。
- **いつ使うか**: 評価指標がAUCなど**順位ベース**で、上位が僅差で競っているとき。また、複数モデルをランクブレンドする前処理として。
- **なぜ使うか**: 同点をどう並べるかで、AUCの5桁目は簡単に動く。ここが実行のたびに変わると、**測定したい改善幅より測定誤差のほうが大きくなり、検証そのものが成立しない**。「まず測定器を固定する」という順序が重要。
- **使うとどうなるか**: 再現性が保証され、`submission[TARGET].nunique() == len(submission)` のようなassertで検証可能になる。副作用として、タイブレーカーの選び方が実質的に微小な事前情報になる（S6E8では teacher の順位を第2キーにしている＝同点なら teacher を信じる）。この選択は明示しておくべき。
- **初出**: 2026-08-20 / Playground S6E8 (Predicting Smartphone Addiction)

### 教師–生徒の残差ブレンド（Teacher–Student Residual Blending）

- **意味**: 強い大モデル（teacher）の出力を小さなモデル（student）に蒸留したうえで、studentを置き換えるのではなく、**teacherに対する補正項**として `(1-w)*teacher + w*student` の形で少量混ぜる手法。
- **いつ使うか**: 単体では teacher に勝てない小モデルでも、**teacher とは違う間違え方をしている**と期待できるとき。特徴量を絞った解釈可能なモデルを student にすると、残差の意味も読みやすい。
- **なぜ使うか**: student は teacher の劣化コピーだが、変数を絞ることで**teacherが過学習している成分を含まない**ことがある。その差分（残差）が正則化として働く可能性がある。
- **使うとどうなるか**: S6E8では、OOF上は w=+0.12 付近で改善する一方、**公開LBは w=-0.08（符号が逆）を好んだ**。この符号の逆転こそが「公開LBに過学習している」ことの強い証拠になった。**混合重み w を公開LBで選ぶのは検証ではない**——OOFで決めて固定するのが正しい使い方。
- **初出**: 2026-08-20 / Playground S6E8 (Predicting Smartphone Addiction)

### トポロジー不変の軌跡平滑化（Topology-Preserving Trajectory Smoothing）
- **意味**: 追跡グラフの**接続関係（誰と誰がつながっているか）を一切変えず**、ノードの座標だけを時間方向の直線あてはめで滑らかにする後処理。分岐（分裂）点をまたぐ平滑化は行わない。
- **いつ使うか**: 評価指標が「ノードの位置が正解から一定距離以内か」で判定され、かつ検出座標にフレーム独立のノイズが乗っているとき。細胞追跡、物体追跡、姿勢推定の後処理全般。
- **なぜ使うか**: 座標の推定誤差は毎フレーム独立に乗るので、時間方向に平均すれば期待値として真の中心に近づく。何より、**エッジのスコアに一切副作用が出ない**のが最大の利点。後処理はたいてい「片方を良くして片方を悪くする」トレードオフになるが、これは一方的に得をする数少ない改善。
- **使うとどうなるか**: マッチング半径が厳しいほど効果が大きい（Biohubでは7.0µm）。ただし**分裂の直前・直後を平滑化してはいけない**。分裂点は座標が本来急変する場所なので、均すと精度が落ちる。実装上は「前後に分岐のない直線区間だけを辿る（`len(prev_ids) != 1` で打ち切る）」ことで自然に回避できる。窓幅と混合重み（例: 窓3・重み0.85）は検証データで探索する価値が高い。
- **初出**: 2026-08-21 / Biohub - Cell Tracking During Development

### 独立検出器による修復の拒否権（Independent-Detector Veto for Graph Repair）
- **意味**: 後処理でノードやエッジを「復元・追加」しようとするたびに、**その復元とは独立に学習された別モデル**（例: 中心検出U-Net）に問い合わせ、支持が得られなければ復元を却下する仕組み。
- **いつ使うか**: 後処理で欠損を補う工程が複数ある（ギャップ埋め、分裂復元、短小トラック救済など）タスク。指標がFNとFPの両方を減点するとき。
- **なぜ使うか**: 後処理で補完を強めれば必ずFNは減るが、同時にFPも増える。**補完側だけを強化してもスコアは伸びない**。ブレーキを主モデルとは別系統のモデルで持つことで、主モデルと同じ誤りを共有せずに済む（同じ思い込みを持つ検証は検証にならない）。
- **使うとどうなるか**: 補完の再現率をあまり落とさずに精度だけ上げられる。設計上の要点は**検出器が使えなかったときの既定動作**で、「通す（fail-open）」にすると環境差で静かにブレーキが外れ、「却下する（fail-closed）」にすると提出が空になり得る。どちらに倒すかは意識して選ぶこと。似た概念の「エッジポリシーveto」が**削除**に使うのに対し、こちらは**追加の可否**を判定する点が違う。
- **初出**: 2026-08-21 / Biohub - Cell Tracking During Development

### 輝度重み付き重心リファイン（Intensity-Weighted Centroid Refinement）
- **意味**: 補間で合成した座標（例: ギャップを埋めるための両端の中点）を、実画像の局所窓を切り出して**背景を差し引いた輝度で重み付けした重心**へ寄せる補正。移動量が上限を超えたら補正自体を破棄する。
- **いつ使うか**: 位置が距離しきい値で採点され、かつ補間で座標を作る工程があるとき。細胞・粒子追跡、キーポイント補間。
- **なぜ使うか**: 中点は「2点の間のどこか」でしかなく、実際の対象の位置とは限らない。画像には実際の輝度分布があるのだから、それを使わない手はない。背景として `np.percentile(patch, 20)` 程度を引いてから重み付けするのがコツで、これをしないと背景の広い領域に重心が引っ張られる。
- **使うとどうなるか**: マッチング判定を通る率が上がる。**上限（max shift）を設けて暴れたら却下する**のが重要で、これがないと近くの別の対象に吸い寄せられて逆効果になる。なお「却下して中点に戻す」より「上限までクリップして採用する」ほうが有利な場合があるので、却下件数の統計を取って比較するとよい。
- **初出**: 2026-08-21 / Biohub - Cell Tracking During Development

### 連続値の文字列化トリック（Stringify Trick for Synthetic Tabular Data）
- **意味**: 合成表形式データにおいて、ユニーク値の少ない「連続値」列を `.astype(str)` でカテゴリに変換し、ターゲットエンコーディングをかけて**値→陽性率のルックアップ表**に置き換える前処理。
- **いつ使うか**: Kaggle Playground のように、小さな実データから生成された合成データ。特に、連続値のはずの列が実際には数十〜数百のユニーク値しか持たない（値が格子状に並ぶ）場合。
- **なぜ使うか**: 木モデルは「x < 4.5 なら左」という**大小関係の分割**しか作れない。値が格子状で各格子点ごとにラベルの傾向がバラバラなら、大小関係には意味がなく、木は多数の分割を費やしても表現しきれない。カテゴリ化してターゲットエンコードすれば、**生成器のルールを1つの数値列に圧縮**できる。
- **使うとどうなるか**: 合成データでは大きく効く。**ただし本物の測定値に使うと汎化性能は落ちる**（未知の値が全部未知カテゴリになる）。「値の格子構造」を持つデータかどうかを先に確認すること。実装では、対象列を手で列挙するより `df[col].nunique() < 閾値` で機械的に選ぶほうが見落としがない。エンコード時は必ずfold内で `fit` すること。
- **初出**: 2026-08-21 / Predicting Smartphone Addiction (Playground Series S6E8)

### スロットの確率的ドロップアウト（Stochastic Slot Masking）
- **意味**: 複数の入力源（撮像シーケンスのスロット、センサ、モダリティ）を持つモデルで、**学習時にランダムに一部の入力源を欠損させる**データ拡張。
- **いつ使うか**: 推論時に一部の入力源が欠けることが常態化しているとき。医用画像（施設によって撮る系列が違う）、マルチセンサ、マルチモーダル学習全般。
- **なぜ使うか**: 全入力源が揃った状態でしか学習していないモデルは、1つ欠けただけで予測が大きく崩れることがある。**学習時に本番の欠損パターンを模擬**しておけば、欠損に対して滑らかに劣化するモデルになる。副次的に、特定の入力源への過度な依存も抑えられる（1つの系列にだけ頼るとその系列が落ちたとき損をするので、モデルは冗長な手がかりを学ぶ）。
- **使うとどうなるか**: 欠損時の性能が安定する一方、全部揃っている場合の性能はわずかに下がることがある。ドロップ率は、**実データにおける実際の欠損率に合わせる**のが出発点として妥当。欠損を「ゼロ埋め」ではなく明示的なマスクで伝えることとセットで使う。
- **初出**: 2026-08-21 / RSNA Knee Abnormality Detection

### 非負制約付きブレンド重み（NNLS / Non-Negative Least Squares Blending）
- **意味**: 複数モデルのOOF予測を線形結合する際、重み係数を `w_i >= 0` に制限して最小二乗を解く方法。制約なしのリッジ回帰と対比される。
- **いつ使うか**: 混ぜる予測配列が多く（数十本以上）、かつ互いの相関が高いとき。通常のリッジを当てると `+1.30` と `-1.01` のように大きさが1を超えて打ち消し合う係数ペアが出る場合。
- **なぜ使うか**: 打ち消し合う係数は実質「2モデルの差分」を特徴量にしている状態で、その差分はOOFのノイズであることが多い。非負制約は**この構造そのものを禁止**するため、正則化の強さを手探りするより確実に効く。hill climbing（逐次追加・非負）が実質的に同じ制約を課しているのも同じ理由。
- **使うとどうなるか**: OOFスコアは制約なし版より必ず下がる（制約付き最適解は制約なし最適解以下）。しかしprivateでの順位保持は改善しやすい。「OOFが下がったから悪い変更」と判断してはいけない典型例で、OOF−LBギャップを併せて見る必要がある。
- **初出**: 2026-08-22 / Playground Series S6E8

### 1軸キャリブレーションプローブ（One-Axis Calibration Probe）
- **意味**: 基準となる設定（アンカー）を固定し、**ハイパーパラメータを1つだけ**動かして効果を測る実験設計。
- **いつ使うか**: 後処理パラメータが10個以上あり、かつ1回の評価コスト（提出回数制限や実行時間）が高いとき。特にCVが信頼できず公開LBでしか測れない状況。
- **なぜ使うか**: 複数を同時に動かすと、スコアが上下しても**どの変更が効いたか特定できない**。1軸に絞れば、その1回の提出が「このパラメータの偏微分」という明確な情報を返す。ランダムサーチやOptunaが使えるのは評価が安価なときだけで、提出回数が1日5回といった環境では原理的に成立しない。
- **使うとどうなるか**: 探索は遅くなるが、得られた知見が蓄積する（「この閾値は0.9〜0.98で平坦」といった形状情報が残る）。応答が平坦なパラメータが見つかった場合、それは「この機構自体があまり効いていない」という重要な陰性情報。逆に鋭く反応する場合は過剰適合を疑う材料になる。パラメータ間の交互作用は原理的に拾えないのが弱点。
- **初出**: 2026-08-22 / Biohub - Cell Tracking

### 面間アテンション（Co-plane Attention）
- **意味**: 複数の撮像面（矢状断・冠状断・横断）や複数シーケンスの特徴量を、**面同士が相互にattendする**形で統合する機構。所見クエリ→スロットの1段attention（SlotHead）の発展形。
- **いつ使うか**: 同一被験者に対して複数の視点・モダリティが揃っており、かつ「ある面で見えた所見を別の面で確認する」という診断プロセスが臨床的に存在するとき。
- **なぜ使うか**: 1段のプーリングでは、各面から独立に情報を吸い上げて足すことしかできない。面同士を相互作用させれば「矢状断で靭帯の不連続を見つけたので、冠状断の同じ高さを重点的に見る」型の条件付き推論が表現できる。Nature Communications の膝MRI12所見研究がこの構成でAUC 0.812を報告。
- **使うとどうなるか**: パラメータが増えるので、**教師信号がstudy単位（粗い）の場合は過剰適合しやすい**。「ラベルが粗いなら集約機構も粗く」という原則と正面から衝突するため、導入時はholdoutで厳密に検証し、annotation checkのような少数サンプルの指標で判断しないこと。
- **初出**: 2026-08-22 / RSNA Knee Abnormality Detection

### 残差ブロック / スキップ接続（Residual Block / Skip Connection）
- **意味**: `out = f(x) + x` の形で、変換結果に入力そのものを足し戻す構造。ResNetで導入され、表形式向けMLPにも広く使われる。
- **いつ使うか**: ネットワークを3層以上に深くしたいとき。表形式データでは残差ブロック2つ程度（隠れ次元128前後）が実用的なライン。
- **なぜ使うか**: ①逆伝播の勾配が「変換の経路」と「そのまま足す経路」の両方を通れるため勾配消失しにくい。②そのブロックが「何もしない」のが最適なら内部の重みをゼロにすれば済むので、深くしても性能が悪化しにくい（恒等写像を学ぶのが容易）。
- **使うとどうなるか**: 深くしても学習が進むようになる一方、表形式データで深くしすぎると過学習が早く来る。BatchNorm・Dropoutと組み合わせるのが定石。足す前後のテンソル形状が一致していないとエラーになるので、隠れ次元を揃える必要がある。
- **初出**: 2026-08-24 / Playground S6E8（Predicting Smartphone Addiction）

### 最良チェックポイント復元による手動early stopping（Best-Checkpoint Restoration）
- **意味**: 学習は最後まで回しつつ、エポックごとに検証指標を測り、最良だった時点の重みを保存しておいて最後に復元する手法。
- **いつ使うか**: 評価指標が損失関数と異なるとき（例: 損失はBCE、評価はAUC）や、エポック数を事前に決めきれないとき。
- **なぜ使うか**: AUCのような微分不可能な指標は損失として直接最適化できない。しかし「各エポックで測って最良を選ぶ」ならば、実質的にその指標で最適な状態を選べる。打ち切り型のearly stoppingと違い、一度悪化してから再度改善するケースも取りこぼさない。
- **使うとどうなるか**: 過学習後の重みを採用してしまう事故を防げる。**重要な落とし穴**: `model.state_dict()` が返すテンソルはモデルの重みと同じメモリを参照するため、`copy.deepcopy()` せずに保持すると次のエポックで上書きされ、「保存したはずの最良重み」が最新の重みに化ける（エラーが出ないので気づきにくい）。
- **初出**: 2026-08-24 / Playground S6E8（Predicting Smartphone Addiction）

### AdamW（デカップルド重み減衰つきAdam）
- **意味**: 重み減衰（L2正則化）を勾配計算とは切り離して直接パラメータに適用するAdamの改良版。
- **いつ使うか**: ニューラルネットの学習時、素のAdamの代わりに。表形式・画像・言語のいずれでも現在の標準的な第一選択。
- **なぜ使うか**: 素のAdamで `weight_decay` を指定すると、その項も適応的学習率でスケールされてしまい、パラメータごとに正則化の強さがばらつく。AdamWはこれを分離するため、`weight_decay` が意図どおりの正則化として効く。
- **使うとどうなるか**: 過学習が抑えられ、汎化が改善することが多い。値は `1e-2`〜`1e-4` あたりを探索するのが一般的（本notebookは `1e-3`）。強すぎると学習不足（underfit）になるので、学習曲線で訓練損失が下がりきらない場合は緩める。
- **初出**: 2026-08-24 / Playground S6E8（Predicting Smartphone Addiction）

### BCEWithLogitsLoss（シグモイド一体型の二値交差エントロピー）
- **意味**: シグモイド関数と二値交差エントロピーを1つの演算として計算するPyTorchの損失関数。モデルはシグモイドを通す前の生スコア（ロジット）を出力する。
- **いつ使うか**: 二値分類・マルチラベル分類の学習時。
- **なぜ使うか**: 数値的安定性のため。シグモイドを掛けてから `log` を取ると、極端な値で `log(0)` によるNaNやinfが発生しうる。一体で計算すると log-sum-exp トリックにより安定する。また `pos_weight` 引数でクラス不均衡に対応できる。
- **使うとどうなるか**: 学習が安定する。**落とし穴**: モデル側で自分でシグモイドを掛けてしまうと二重適用になり、勾配がほぼ消えて学習が進まない（エラーは出ない）。推論のときだけ明示的に `torch.sigmoid()` を掛ける、と覚える。
- **初出**: 2026-08-24 / Playground S6E8（Predicting Smartphone Addiction）

### CoAtNet（畳み込み + self-attention のハイブリッドバックボーン）
- **意味**: 浅い層を深さ方向分離畳み込み、深い層を relative self-attention で構成し、両者を段階的に組み合わせた画像バックボーン。
- **いつ使うか**: 局所的なテクスチャと大域的な位置関係の両方が効くタスク。医用画像（局所の微細な信号変化 × 臓器全体の形態）は典型例。
- **なぜ使うか**: 純CNNは局所パターンに強く少データでも学習しやすいが大域関係が弱い。純ViTは大域関係に強いが大量データを要する。CoAtNetは畳み込みの帰納バイアスを保ちつつattentionの表現力を得るため、中規模データで両者の中間的な良さを狙える。
- **使うとどうなるか**: 単体性能が上がるだけでなく、**純CNN・純ViTとは違う誤りをする**ためアンサンブルの多様性源として価値が高い。計算コストは純CNNより重く、入力解像度に対してattention部分が二乗で効くので解像度設計に注意。
- **初出**: 2026-08-24 / RSNA Knee Abnormality Detection

### 所見別ブレンド重み（Per-Target Blend Weights under Macro-Averaged Metrics）
- **意味**: マルチラベル問題で、ブレンドの混合比率（α）をラベルごとに変える設計。特定のラベルだけ特定のモデルの予測をそのまま温存することも含む。
- **いつ使うか**: 評価指標がラベルごとに独立して計算され、その後に平均される場合（macro-averaged AUC / macro-F1 など）。
- **なぜ使うか**: macro平均では各ラベルが等しい重み（12ラベルなら1/12ずつ）を持つ。全ラベル一律のαは「平均的には良いが、あるラベルでは明確に悪化する」妥協になりがちで、悪化したラベルのAUC低下がそのまま1/12の損失になる。ラベルごとにαを選べば、この取りこぼしを回収できる。
- **使うとどうなるか**: 上積みが取れる一方、**自由度が跳ね上がるためpublic LBへの過剰適合が起きやすい**。αはpublic LBではなくホールドアウトのmacro指標で決めるべき。ラベル数×候補モデル数だけ選択肢があるので、探索範囲を絞る（例: α ∈ {0, 0.25, 0.5} のみ）などの規律が要る。
- **初出**: 2026-08-24 / RSNA Knee Abnormality Detection

### 保守的包絡によるキャリブレーション（Conservative Envelope of Competing Corrections）
- **意味**: 補正の仮説が複数あって（例: 加法補正 `x + c` と比例補正 `x * q`）どちらが正しいか決められないとき、片方に賭けず `max(...)` や `min(...)` で**両方と矛盾しない側**を採る手法。UMUDのnotebookでは `max(元のMT, (元のMT + 0.4) * q_mean)` という形で使われている。
- **いつ使うか**: 検証データが少なく、2つ以上のもっともらしい補正則の優劣を統計的に判定できないとき。あるいは「補正しすぎる」ことのリスクが「補正しない」ことのリスクより大きいとき。
- **なぜ使うか**: 誤った仮説を全面採用したときの損失を上限で抑えられる。ミニマックス的な考え方で、期待値を最大化するのではなく**最悪ケースを改善する**。
- **使うとどうなるか**: 平均的な改善幅は「正しいほうの補正を単独で当てた場合」より必ず小さくなる。つまり**上振れを捨てて下振れを防ぐ**トレードオフ。少数サンプルのPublic LBで機構を採否している場面では合理的だが、十分な検証データがあるなら素直にCVで優劣を決めたほうが良い。
- **初出**: 2026-08-25 / UMUD Challenge

### 解剖学的整合ゲート（Anatomical / Physical Consistency Gate）
- **意味**: 予測した複数の量の間に成り立つはずの物理・幾何関係（UMUDでは羽状筋の幾何から `FL·sin(PA) ≈ MT`）を、損失関数ではなく**後処理のブール判定**として使い、条件を満たす行だけに補正を適用する手法。
- **いつ使うか**: 複数のターゲットを同時に予測していて、それらの間にドメイン由来の関係式があるとき。特に、モデルを再学習せずに既存の予測を改善したい場面。
- **なぜ使うか**: 制約を損失項として入れるには再学習が必要だが、ゲートとしてなら**推論後に無料で適用できる**。また「関係式を満たさない行＝モデルが自信を持てていない行」という代理指標にもなり、補正の適用先を絞る判断材料になる。
- **使うとどうなるか**: 関係式が近似でしかない領域（UMUDでは筋束が湾曲する場合や腱膜が平行でない場合）では誤ってゲートが閉じ、本来効くはずの補正が当たらなくなる。原著notebook自身が「`FL*sin(PA)` は近似にすぎない」と注意している。**ゲートで何行が影響を受けたかを必ずカウントして表示する**こと（0行なら機構が死んでいる、全行なら条件が意味を成していない）。
- **初出**: 2026-08-25 / UMUD Challenge

### チャンク分割による全バッチ勾配計算（Chunked Full-Batch Gradient Accumulation）
- **意味**: 全データを使う勾配を、行方向に小分け（チャンク）して部分和を足し合わせることで計算する手法。**ミニバッチ学習とは違い、数学的には全バッチ勾配とまったく同一**で、変わるのは加算の順序に由来する浮動小数点誤差（float64なら相対1e-14程度）だけ。
- **いつ使うか**: 特徴量行列がGPU/CPUメモリに載らないが、確率的勾配法ではなく決定論的な全バッチ最適化（LBFGSなど）を使いたいとき。今回の例では 1205列 × 691,369行のfloat64行列（約6.6GB）を16GBのT4上で扱うために使われた。
- **なぜ使うか**: 「メモリが足りない」ときの選択肢は (a) 精度を落とす（float32化）、(b) データを減らす（サブサンプリング）、(c) 計算を分割する、の3つ。(a)(b)は**結果そのものを変えてしまう**が、(c)だけは結果を変えずに制約を外せる。まず(c)を検討する価値がある。
- **使うとどうなるか**: メモリ使用量はチャンクサイズに比例して下げられるが、Pythonループのオーバーヘッドで実行時間は増える。またチャンク境界での加算順序が固定でないと再現性が崩れるので、**チャンクサイズを定数として記録**しておくこと。
- **初出**: 2026-08-25 / Playground S6E8

### レジーム依存重み付け（Regime-Dependent Weighting via Interaction Terms）
- **意味**: スタッキングの2段目で「メンバーの重みは全サンプル共通」という前提を崩し、サンプルの状態を表すフラグ（欠損の有無、メンバー間の意見のばらつき等）を各メンバー予測に掛け合わせた交互作用列を追加することで、**線形モデルのまま条件付きの重みを学習させる**手法。
- **いつ使うか**: データに明確に性質の異なる部分集団があるとき（欠損の多い行と少ない行、デバイス別、期間別など）。またはモデルごとに得意な領域が違うと分かっているとき。
- **なぜ使うか**: サブグループごとに別々のスタッカを学習すると、各サブグループのサンプル数が減って推定が不安定になる。交互作用項なら**全データを使ったまま**、条件付きの効果だけを追加で推定できる。「モデル同士が割れているサンプルでだけ重み付けを変える」（`b × 予測の標準偏差`）という使い方は特に面白く、簡単なサンプルでは差がつかず難しいサンプルで勝負が決まる、という直観の実装になっている。
- **使うとどうなるか**: 列数がフラグの数だけ倍増する（205メンバー → 1205列など）ので、正則化とメモリ設計がセットで必要。またフラグの定義（例: `欠損 >= 4`）が恣意的だと、その閾値自体が過学習の入口になる。**フラグを変えてスコアが大きく動くなら、それは機構が効いているのではなく雑音を拾っている**と疑うべき。
- **初出**: 2026-08-25 / Playground S6E8

### LBFGS（記憶制限BFGS / 準ニュートン法）
- **意味**: 勾配だけでなく**曲率（ヘッセ行列）の近似**を過去数十ステップの勾配差から構築して使う最適化アルゴリズム。ヘッセ行列そのものを持たず履歴だけ保持するので「記憶制限（Limited-memory）」と呼ばれる。
- **いつ使うか**: 目的関数が凸で（ロジスティック回帰・線形回帰など）、全バッチの勾配が計算できるとき。データがGPU/メモリに載る規模で、高精度な収束が欲しいとき。
- **なぜ使うか**: SGDやAdamは学習率とエポック数の調整が必要で、収束点も実行ごとに揺れる。LBFGSは曲率情報を使うため**少ない反復で高精度に収束**し、`tolerance_grad=1e-10` のような厳しい停止条件を実際に満たせる。AUCの差が0.0001を争う場面では、この決定論性が重要。
- **使うとどうなるか**: 非凸問題（ニューラルネット）では鞍点に捕まりやすく不向き。ミニバッチとの相性も悪い（勾配のノイズが曲率近似を壊す）。また特徴量が強く相関していると条件数が悪化するので、**float64で回す**か事前に標準化するのが実務上の必須手当て。PyTorchでは `closure` を渡す独特のAPIになる点にも注意。
- **初出**: 2026-08-25 / Playground S6E8


### Rank-01 パーセンタイル正規化（Rank-01 / Percentile-Rank Normalization）
- **意味**: 予測値を順位に変換し `R(p) = (rank(p) - 1) / (N - 1)` として **[0, 1] の一様分布**に写す変換。`scipy.stats.rankdata` や `pandas.rank(pct=True)` で実装する。
- **いつ使うか**: 評価指標がAUCなど順位ベースで、かつ**出力分布の異なる複数モデルをブレンドする**とき。
- **なぜ使うか**: 生の確率のまま加重平均すると、**分布の広いモデルが結果を支配する**。たとえばモデルAが [0.01, 0.99]、モデルBが [0.45, 0.55] なら、単純平均の順位はほぼAで決まり、Bに与えた重みは名目上のものになる。Rank-01 で両者を一様分布に揃えれば、**指定した重みがそのまま影響力になる**。AUCは単調変換に不変なので、この変換で失う情報はゼロ。
- **使うとどうなるか**: 重みが意図どおり効くようになり、出力が [0,1] に収まるので値域チェックも単純になる。ただし**キャリブレーションは完全に失われる**ので、確率としての解釈が必要なタスク（期待値計算、閾値の事前設定）には使えない。また同点処理（`method='average'`）の選び方で結果が微妙に変わるため、決定論性が必要なら明示すること。
- **初出**: 2026-08-26 / Playground Series S6E8

### 相関適応型ブレンド重み（Correlation-Adaptive Blend Weighting）
- **意味**: 2つの予測系統の相関を実行時に測り、その値に応じてブレンド重みを**実績のある安全な値へ自動的に引き戻す**手法。
- **いつ使うか**: 検証で攻めた重みを決めたが、本番データでその検証が通用するか確信が持てないとき。特にCode Competitionのように、提出時にしか test を見られない状況。
- **なぜ使うか**: 高相関（例: > 0.992）は「2系統がほぼ同じ答え＝混ぜる意味がない」状態で、攻めた重みを使う根拠が消えている。低相関（例: < 0.60）は「どちらかが壊れている」疑いがあり、攻めた重みは危険。**どちらの異常でも中央へ引き戻す**ことで、検証の外挿失敗とパイプラインの故障を同時に防げる。
- **使うとどうなるか**: 最良ケースの伸びは削られるが、最悪ケースが大幅に改善する。public LBへの過学習を、コードレベルで抑制する仕組みとして機能する。**注意点**: 閾値（0.992 / 0.60 など）自体がマジックナンバーになりがちで、根拠を検証で示さないと「なんとなく安全そうな数字」に堕する。ホールドアウトでの感度分析を必ず添えること。
- **初出**: 2026-08-26 / RSNA Knee Abnormality Detection

### 密度適応型の閾値（Density-Adaptive Thresholding）
- **意味**: 距離などの閾値を全領域で固定せず、**局所的な密度に応じて自動調整**する手法。例: `GAP_CLOSE_UM = 5.8` を基準に、近傍3点の平均距離が基準値（6.5µm）より小さければ閾値を絞り、大きければ緩める。1ステップの変化量には上限（0.125µm）を設ける。
- **いつ使うか**: 対象の密度が場所によって大きく変わるデータ（細胞追跡、点群、物体検出の混雑シーン）で、距離ベースの対応付けを行うとき。
- **なぜ使うか**: 密集領域で使える閾値は疎な領域では厳しすぎ、疎な領域に合わせた閾値は密集領域で誤対応を量産する。**単一の閾値では両立できない**。密度で正規化すれば、「隣の細胞までの距離の何割か」という相対量で判断できる。
- **使うとどうなるか**: 密集/疎の両方で対応付けの精度が上がる。副作用として、**密度推定自体が誤ると閾値も連動して誤る**ため、変化量に上限（max step delta）を設けて暴走を防ぐのが定石。またパラメータが増える（基準値・ゲイン・上限・近傍数）ので、チューニング対象が増えることは覚悟が要る。
- **初出**: 2026-08-26 / Biohub - Cell Tracking During Development

### fold内順位正規化してから平均（Within-Fold Rank Normalization before Averaging）
- **意味**: k-fold の各foldの予測を、**fold内で順位化してから**fold平均を取る手法。`fold.argsort(0).argsort(0) / (n-1)` のように実装する。
- **いつ使うか**: 複数foldの予測を平均する際、指標が順位ベース（AUC等）であるとき。
- **なぜ使うか**: fold間で出力のスケールや分布がずれていると、生の値の平均では**分布の広いfoldが支配**し、実質的に一部のfoldだけを使っているのと変わらなくなる。fold内で順位化すれば、5つのfoldが**均等に**寄与する。
- **使うとどうなるか**: fold平均が設計どおりの効果を持つようになる。AUC指標下では情報損失がないため、事実上デメリットがない。ただしfoldごとにサンプル数が違う場合、順位の粒度が揃わない点には注意（`pct=True` で正規化すれば緩和される）。
- **初出**: 2026-08-26 / RSNA Knee Abnormality Detection

### モーメントマッチングによるロジット整合（Moment Matching / Standardize-then-Blend）
- **意味**: 複数モデルの出力（ロジットや生スコア）を混ぜる前に、片方の**平均と標準偏差をもう片方に揃える**変換。`aligned = (b - b.mean()) * (a.std() / b.std()) + a.mean()` の形で書き、スケール比は `clamp(0.5, 2.0)` のように上下限で挟むのが定石。
- **いつ使うか**: 学習条件（seed・エポック数・損失関数・温度）が違う複数モデルを、確率化する前のロジット段階で融合したいとき。片方が過信気味（分散が大きい）でもう片方が慎重（分散が小さい）と分かっているとき。
- **なぜ使うか**: ロジットの絶対値には**モデル間で比較可能な意味がない**。片方が −10〜+10、もう片方が −2〜+2 のレンジで出力していれば、単純に足した瞬間に前者が実質的に支配してしまい、「50:50 で混ぜたつもりが 90:10 だった」という事故が起きる。平均と分散だけでも揃えれば、宣言した重みが実際の寄与比に近づく。
- **使うとどうなるか**: 重みの解釈が意味を持つようになり、重み探索が安定する。ただし**モーメントを揃えても分布の形（歪度・裾）は揃わない**ので、片方が極端に裾の重い分布を持つ場合は不十分。その場合は順位空間（`rank(pct=True)`）に移すほうが確実で、順位化は「分布を完全に一様に潰す」という、より強いが情報を捨てる正規化にあたる。スケール比のclampを省くと、分散がほぼ0のモデル（全行ほぼ同じ値＝壊れている）を掛け算で増幅して破壊的になるため、clampは必須。
- **初出**: 2026-08-27 / Biohub - Cell Tracking（Biohub Harmonic Fusion）

### 確信度マージン適応ブレンド（Confidence-Margin-Adaptive Blending）
- **意味**: アンサンブルの混合重みを**行（サンプル）ごとに変える**手法。各モデルについて softmax 上位1位と2位の確率差＝**マージン**を確信度の代理として計算し、`local_weight = clamp(base + margin_B − margin_A, lo, hi)` のように、「Aが迷っていてBが自信を持っている行ほどBを重く採る」重みを作る。
- **いつ使うか**: 多クラス／候補選択型のタスク（追跡のリンク先選択、検索のリランキング、マッチング）で、モデルごとに得意な領域が違うと考えられるとき。固定重みのグリッドサーチが頭打ちになったとき。
- **なぜ使うか**: 固定重みのアンサンブルは「全行に同じ配合比」を強制する。しかし実際には、片方のモデルが明確に正しい行と、両方が迷っている行が混在している。**確信度は無料で手に入る局所的な信号**なので、それを配合比に反映すれば固定重みより情報を使える。追加の学習も検証データも要らないのが利点。
- **使うとどうなるか**: 両モデルの強弱がサンプルごとに入れ替わるタスクでは固定重みを上回る。一方で、**マージンは確信度としては粗い代理**であり、過信したモデル（キャリブレーションが悪い）は「マージンが大きい＝正しい」を満たさないので逆効果になり得る。安全策として (a) 重みを `clamp(0.15, 0.75)` のように両側で挟み片方に振り切らせない、(b) 両モデルのトップ候補が一致する行では重みを下げない、といったガードを併用する。前提として、混ぜる前にロジットのモーメントマッチング（上項）を済ませておく必要がある。
- **初出**: 2026-08-27 / Biohub - Cell Tracking（Biohub Harmonic Fusion）

### D4対称8視点TTA（Dihedral-4 Symmetry TTA）
- **意味**: TTA（→「Test-Time Augmentation」の項）のうち、正方形の対称群 **D4**（恒等・90°/180°/270°回転・4種の鏡映）の**全8要素**で入力を変換して推論し、逆変換して平均する構成。実装上は「反転3種＋原画像＝4視点」に、回転2種・転置・反転転置を足して8視点にする。
- **いつ使うか**: 対象が**向きに意味を持たない**画像タスク（細胞・細菌・衛星画像・組織染色像）。逆に、上下が意味を持つ画像（自然画像の風景、文字、胸部X線の左右）では鏡映を入れると害になる。
- **なぜ使うか**: 学習データの向きの偏りをモデルは必ず拾う。同じ対象を回して推論して平均すると、その**向き依存のブレ（分散成分）が打ち消される**。アンサンブルと違い追加の重みが要らず、モデルが1つでも効く。
- **使うとどうなるか**: 検出・セグメンテーションでは境界が滑らかになり、確率のキャリブレーションもわずかに改善することが多い。代償は**推論時間が視点数に比例して増える**こと（8視点なら素の8倍）。Code Competitionのように実行時間の上限がある環境では、視点ごとの寄与を測って4視点に落とす価値がある。特に3D画像でボクセルが**異方的**（例: z 1.625µm vs xy 0.40625µm）な場合、xy平面内の8視点が等価に有用とは限らないので、盲目的に8視点にせず実測すべき。
- **初出**: 2026-08-27 / Biohub - Cell Tracking（Biohub Harmonic Fusion）

### 後有糸分裂の発散テスト（Post-Mitotic Divergence Test）
- **意味**: 細胞追跡で「分裂（1つの親→2つの娘）」候補を採用するかどうかを、**t+2 以降で2つの娘が両方存続し、かつ互いの距離が増加しているか**で判定する後処理ルール。単独では使わず、(C1) 親がトラックの途中であること（開始点は親になれない）、(C2) 姉妹が**相互に**最近傍の孤児であること、と組み合わせる。
- **いつ使うか**: 距離ベースの分裂ルールが偽陽性を大量に出しているとき。より一般には、「イベント発生の直後にしか現れない動的な性質」を判別条件に使えるあらゆる場面。
- **なぜ使うか**: 分裂の瞬間だけを見ると、姉妹細胞は「近くにある2つの点」でしかなく、**たまたま隣り合った無関係な2細胞と区別がつかない**。区別できるのは時間発展のほうで、分裂直後の姉妹は互いに離れていくが、無関係な隣接細胞はその挙動を示さない。つまり**生物学的な事実を、そのまま判別器にしている**。
- **使うとどうなるか**: 実測例では、この3制約の導入により偽陽性が数百件から9件に減り、それまでゼロだった division Jaccard が初めて非ゼロ（0.0625、TP 1 / FP 9 / FN 6）になり、総合スコアが 0.8799 → 0.8845 に改善した。副作用として**t+2 まで観測できないケース（系列の末尾付近、一時的な検出漏れ）は原理的に判定できず落ちる**ので、時系列の端では再現率が下がる。また判定を t+3, t+4 まで延ばすと精度は上がるが取りこぼしがさらに増えるトレードオフがある。
- **初出**: 2026-08-27 / Biohub - Cell Tracking（Biohub Harmonic Fusion）

### 重み摂動によるタイ消去（Weight Perturbation for Tie Elimination）
- **意味**: 複数の順位ベクトルを重み付き和で融合するとき、重みをあえて `0.725` ではなく **`0.725001`** のように微小にずらして、**順位和が完全一致する行（タイ）の発生を防ぐ**手法。`assert np.unique(pred).size == len(pred)` で検証する。
- **いつ使うか**: AUC のように**順位のみで決まる指標**で、有理数の重み（0.725 = 29/40 など）を使って複数の整数順位を線形結合するとき。
- **なぜ使うか**: 重みが単純な有理数だと、`w1*r1 + w2*r2` が異なる (r1, r2) の組で**同じ値になる行が確実に生じる**。ROC-AUC では同点ペアは 0.5点（半分正解）として計上されるため、タイは**手元にある順序情報を捨てている**ことに等しい。1e-6 の摂動は予測値としては意味のない差だが、順位の一意性は完全に回復する。
- **使うとどうなるか**: 情報を捨てずに済む。ただし効果は極めて小さく（LB 0.00001〜0.00005 のオーダー）、**この差を「改善」と呼ぶかどうかは慎重に**——同じ桁のノイズが公開LBには常に乗っている。関連項目として「決定論的タイブレーク付き厳密ランク化」があり、あちらは**タイをどう壊すかを決定論的に定める**話、こちらは**そもそもタイを作らない重みを選ぶ**話。
- **初出**: 2026-08-27 / Playground S6E8（S6E8 Regime-Calibrated Rank Fusion）

### 2.5D入力（2.5D CNN / Multi-Slice Stacking）
- **意味**: 3Dボリューム（MRI・CTなど）を扱う際、3D CNNを使わず、**連続する複数スライスをチャンネル方向に積み重ねて2D CNNに入力する**手法。16スライスを16チャンネルの「1枚の画像」として扱うようなイメージ。
- **いつ使うか**: 3Dの文脈が必要だが、(a) GPUメモリや実行時間の制約が厳しい、(b) ImageNetなどで事前学習された2Dバックボーンの資産を使いたい、のいずれかに該当するとき。Kaggleの医用画像コンペではほぼ標準の第一手。
- **なぜ使うか**: 3D CNNはパラメータ数・メモリ・計算量がすべて跳ね上がるうえ、**大規模な3D事前学習済みモデルがほとんど存在しない**。2.5Dなら `timm` の強力な2Dバックボーン（CoAtNet、EfficientNet、ConvNeXt等）をそのまま使え、隣接スライス間の連続性も部分的に捉えられる。9時間制限のCode Competitionでは実質これ一択になることが多い。
- **使うとどうなるか**: 事前学習の恩恵で、少ないデータでも安定して立ち上がる。一方で**スライス方向の並進不変性が失われる**（1番目と16番目のチャンネルは対等でない）ため、スライス数や順序が変わると性能が落ちやすい。スライス順序の正規化（患者座標系での並べ替え）が前提条件になる。また真に3次元的な形状（嚢腫の球状性など）の把握は3D CNNに劣る。
- **初出**: 2026-08-28 / RSNA Knee Abnormality Detection

### ソフトラベル（Soft Labels / 確率ラベル）
- **意味**: 教師ラベルを 0/1 の二値ではなく、`0.8` `0.3` のような**0〜1の連続値**で与えること。「断裂が疑われる」という曖昧な記述を 1 ではなく 0.8 として扱う。
- **いつ使うか**: ラベルの生成過程そのものに不確実性があるとき。具体的には、(a) LLMや正規表現で読影レポート・自由記述テキストからラベルを起こしたとき、(b) 複数のアノテータの意見が割れたとき（同意率をそのままラベルにする）、(c) 知識蒸留で教師モデルの出力を使うとき。
- **なぜ使うか**: 曖昧な記述を強制的に 0/1 に丸めると、**ラベルノイズを「確信」としてモデルに教え込む**ことになる。「疑われる」を 1 にすれば、モデルは断定的な症例と同じ強さでそれを学習してしまう。ソフトラベルなら不確実性がそのまま伝わり、モデルの出力も自然と中間的な値を取る。**AUCは順序の指標なので、この「自信の度合いの差」が直接スコアに効く**。
- **使うとどうなるか**: 過信が減り、確率の較正（calibration）が改善し、AUCが上がりやすい。損失関数は `BCEWithLogitsLoss` のまま使える（ターゲットが連続値でもそのまま動く）。一方で**ソフトラベルの値そのものが恣意的**（なぜ0.8で0.7ではないのか）になりがちで、その割り当てルールが新たなハイパーパラメータになる。また、ラベル生成にLLMを使った場合、**モデルが病変ではなくレポートの書き癖を学習してしまう**リスクがあるため、少数でも存在する「真の構造化ラベル」での検証を並行させるべき。
- **初出**: 2026-08-28 / RSNA Knee Abnormality Detection

### フレーム単位ロールバックガード（Frame-Retention Guard / Per-Unit Safe Fallback）
- **意味**: アンサンブルや追加処理を適用した結果、**ある単位（フレーム・症例・グループ）で明らかに劣化の兆候が出たら、その単位だけを元のベースライン出力に巻き戻す**仕組み。Biohubの例では「アンサンブル後の検出候補数が主モデル単独より減ったフレーム」を主モデルに戻している。
- **いつ使うか**: 「平均的には改善するが、一部の単位では悪化する」処理を入れるとき。アンサンブル、TTA、後処理、キャリブレーションのすべてが該当する。特に**劣化を正解なしで検知できる代理指標**（候補数、分散、予測の極端さなど）があるときに有効。
- **なぜ使うか**: 通常のアンサンブルは全体を一括で採用するか捨てるかの二択になる。しかし実際には**利得と損失が単位ごとに分かれている**ことが多く、損失側だけを外せれば純利得が残る。特にトラッキングのように「検出の見逃しがエッジ2本分の損失になる」構造の指標では、候補が減る方向の変化は非対称に危険なので、そこだけ監視する価値が高い。
- **使うとどうなるか**: 悪化の下振れが抑えられ、スコアの分散が下がる。ただし**代理指標の選び方が誤っていると、良い変化まで巻き戻してしまう**（例: アンサンブルが正しく偽陽性を削ったのに「候補が減った」として戻す）。したがって巻き戻しの発生回数を必ずログに残し、多すぎる場合は代理指標を疑うこと。またこの判定は正解を使わないため、テスト時にもそのまま適用できるのが利点。
- **初出**: 2026-08-28 / Biohub - Cell Tracking During Development

### 距離ゲート付き二部マッチング（Gated Bipartite Matching / Big-M Gating）
- **意味**: ハンガリアン法（`scipy.optimize.linear_sum_assignment`）で1対1対応を解く際、「この距離を超えるペアは絶対に組ませない」という制約を、コスト行列の該当セルに巨大な値（例: `1e6`）を入れることで表現する手法。
- **いつ使うか**: 予測点と正解点の対応付け、フレーム間の細胞対応付けなど、**「対応する相手が存在しないかもしれない」**マッチング問題全般。
- **なぜ使うか**: ハンガリアン法は「行と列を必ず全て割り当てて総コストを最小化する」アルゴリズムなので、**放っておくと遠く離れた無関係なペアも強引に組ませてしまう**。距離閾値でフィルタしてから解こうとすると行列が矩形でなくなり実装が煩雑になるが、Big-Mを入れるだけなら1行で済む。
- **使うとどうなるか**: 「マッチしなかった」を明示的に扱えるようになり、TP/FP/FNの分解が正しくなる。注意点は、Big-Mの値が小さすぎると（他のコストの合計と同オーダーだと）**総コスト最小化の都合で禁止したはずのペアが選ばれてしまう**こと。必ず「ありうる最大の総コスト」より十分大きく取る。逆に大きすぎると数値誤差が出るので `1e6` 程度が実務的な落としどころ。
- **初出**: 2026-08-29 / Biohub - Cell Tracking（Biohub 0.928 LB）

### 強正則化メタモデル（Strong-L2 Meta-Learner / 低自由度スタッキング）
- **意味**: スタッキングのメタモデルに、`LogisticRegression(C=0.006)` のように**極端に強い正則化**をかけ、学習される重みを意図的に「ほぼ等重み平均」へ引き寄せる設計。`C` は正則化強度の逆数なので、小さいほど強い罰則。
- **いつ使うか**: 入力となるOOFが**数十本あり、かつ互いに高相関**なとき（Playground終盤の公開OOFプールが典型）。ベースモデルの数がサンプル数に比べて多いときほど必要。
- **なぜ使うか**: 高相関な入力に自由な重みを許すと、多重共線性によって「片方に大きな正、もう片方に大きな負」という**打ち消し合う係数**が生まれ、訓練OOFのノイズに合わせにいく。強い正則化は係数のノルムを罰することでこれを抑え、**単純平均に近い安定した解**に落とす。単純平均そのものより少しだけ賢く、自由な回帰よりはるかに壊れにくい、という中間点を狙う。
- **使うとどうなるか**: OOFスコアは自由な回帰より下がるが、**private/未知データでのスコアは上がりやすい**。効果の確認方法は、fold間で係数がどれだけブレるかを見ること（安定していれば正則化が十分）。逆に `C` を強くしすぎると全メンバーが同じ重みになり、弱いモデルの混入がそのまま害になるので、事前のプール枝刈りとセットで考える。
- **初出**: 2026-08-29 / Playground S6E8（S6e8 Public Ensemble）

### 臨床共起事前知識による補正（Clinical Co-occurrence Prior Correction）
- **意味**: マルチラベル予測において、ラベル同士の**実世界での共起しやすさ**を辞書として与え、あるラベルの予測が高いとき関連ラベルのスコアを引き上げる／引き下げる後処理。例: 「ACL断裂 → 骨挫傷・外側半月板損傷も上げる」「滑膜炎 ↔ 関節液貯留・Baker嚢腫」。
- **いつ使うか**: ラベルが独立でないことがドメイン知識として分かっており、かつモデルが各ラベルを（ほぼ）独立に予測しているとき。医療所見、故障モード、商品カテゴリなど。
- **なぜ使うか**: 多くのマルチラベルモデルは出力ヘッドが独立で、ラベル間の相関を**データからしか学べない**。稀なラベルは学習データが少なくその相関を学びきれないため、**人間が知っている構造を後付けで注入**する方が安上がりで確実なことがある。
- **使うとどうなるか**: 稀なラベル（骨折など）のAUCが改善しやすい一方、**補正の強さを検証データで決めると、そのデータが小さいほど過学習する**。本質的な副作用は「モデルが独立に見つけた真の異常」を共起事前分布が打ち消してしまうこと（例: 単独のACL断裂を過小評価する）。強度は複数段階（moderate / aggressive）用意し、必ず保守側も残しておく。
- **初出**: 2026-08-29 / RSNA Knee Abnormality Detection（RSNA Knee Crazy LB Tune）

### バケット内マイクロソート（Bucketed Micro-Sorting）
- **意味**: 強いモデルの予測順位をN個のバケット（等分位）に分け、**大域的な順位はそのまま維持**したうえで、バケット**内部**の並び順だけを別のモデルの予測で決め直す手法。`pd.qcut` + `np.lexsort` で数行で書ける。
- **いつ使うか**: AUCのような順位ベースの指標で、ベースラインの大域順位は信頼できるが「隣接する似たサンプル同士の細かい順位」に改善余地があると考えるとき。2つのモデルの強さに大きな差があり、単純ブレンドだと弱い方がノイズにしかならない場合の代替。
- **なぜ使うか**: 単純な加重ブレンドは「全ての順位」を同時に動かすため、強いモデルの大域構造まで壊しかねない。マイクロソートは影響範囲をバケット内に閉じ込めるので、**壊すリスクの上限が構造的に決まる**。
- **使うとどうなるか**: バケット数が粒度のつまみになる（少ない＝影響大、多い＝影響小）。**重大な注意点**: 細かいバケットでの順位入れ替えは真の信号ではなく**Public LBのサンプリングノイズ**を拾っていることが多い。必ずOOFでバケット数と効果を検証し、「符号を反転させると上がる」といった現象が出たら、それは信号ではなくノイズを追っている証拠として撤退すること。
- **初出**: 2026-08-30 / Playground Series S6E8（Predicting Smartphone Addiction）

### 負の重みブレンド（Negative-Weight Blending）
- **意味**: アンサンブルで、あるモデルに**マイナスの重み**を与えて予測から引き算すること。`pred = 1.005*A - 0.005*B` のような形。
- **いつ使うか**: モデルBが持つ系統的なバイアスをAが共有しており、それを打ち消したいとき。理論的には、OOF上での最小二乗解が負係数になる場合がある（＝Bが「Aの誤り方向」を代理表現している）。
- **なぜ使うか**: 単純な正の重みでは、弱いモデルを混ぜると必ず希釈されて悪化する。負の重みは「弱いモデルが間違える方向の情報」を使えるので、正の重みでは到達できない改善がありうる。
- **使うとどうなるか**: **極めて過剰適合しやすい**。負係数は通常、多重共線性の強い予測子群で出現し、わずかな分布シフトで符号が反転する。使うなら (1) OOFで推定、(2) NNLS（非負最小二乗）と比較して有意に良いか確認、(3) ブートストラップで係数の符号が安定しているか確認、の3点セットが必須。Public LBだけを見て負の重みを入れるのは、LB過剰適合のほぼ確定パターン。
- **初出**: 2026-08-30 / Playground Series S6E8

### 不確実性マスキング（Uncertainty Masking / 部分領域だけのブレンド）
- **意味**: ブレンドを全サンプルに一律適用せず、「ベースラインが自信を持てていない領域」（例: パーセンタイル順位0.2〜0.8の中間帯）**にだけ**第2のモデルを注入する手法。
- **いつ使うか**: 木モデルやアンサンブルのように「両端では強いが中間で迷う」性質を持つベースラインがあり、補助モデルを安全に足したいとき。マルチラベルでも、所見ごとに信頼区間の広い領域を選んで適用できる。
- **なぜ使うか**: 全域ブレンドは、ベースラインが正しく確信している領域まで揺らしてしまう。適用範囲を絞ることで**改悪の下限を守りつつ、改善の余地がある領域だけを狙える**。「どこを触るか」を明示的に設計する点で、重み1つを振るより解釈しやすい。
- **使うとどうなるか**: 補助モデルが十分強くない場合は効果ゼロ〜微減で終わる（S6E8の事例では0.97100→0.97099）。閾値（0.2/0.8）は新しいハイパーパラメータになるので、OOFで決めないと自由度が増えた分だけ過剰適合しやすくなる。「多様性」だけでなく「一定以上の単体性能」が補助モデル側に必要。
- **初出**: 2026-08-30 / Playground Series S6E8

### 撮像プロトコル別スロット融合（Multi-Protocol Slot Fusion）
- **意味**: 医用画像で、撮像面（Sagittal / Coronal / Axial）や左右などの**プロトコル軸**を明示的な「スロット」として定義し、各スロットを別入力としてモデルに与え、欠けているスロットは**他で代用せずマスク**する設計。
- **いつ使うか**: 1検査に複数シリーズ（撮像条件）が含まれ、しかも施設ごとに揃っているシリーズが違うデータ（MRI、多相CT、マルチシーケンス撮像など）。
- **なぜ使うか**: 「あるシリーズを別のシリーズで埋める」と、モデルは「このチャンネルには何が来るか分からない」という矛盾した入力を学ぶ。スロットを固定して欠損はマスクにすれば、**各チャンネルの意味が最後まで一貫**し、欠損パターンが異なるテストデータへの汎化が効く。さらにスロット識別トークンを与えれば「今どの撮像面を見ているか」をモデルが利用できる。
- **使うとどうなるか**: 欠損マスクの実装（segment softmax やラグドプーリング）が必要になり、コードは複雑化する。一方で「Axialが無いstudyでの性能」を分解して測れるようになり、デバッグ性が大きく上がる。左右（laterality）をスロット軸に含める場合は、**DICOMタグを鵜呑みにせず幾何情報から検算する**こと（左右を誤ると内側/外側の所見が丸ごと入れ替わる）。
- **初出**: 2026-08-30 / RSNA Knee Abnormality Detection

### 移植ルールの再キャリブレーション（Ported-Rule Re-calibration）
- **意味**: 他人のnotebookから発見的ルール（閾値・半径・条件式）を移植する際、値をそのまま使わず、**自分のパイプラインが生む統計に合わせて再調整**すること。
- **いつ使うか**: 公開notebookの後処理ルールやヒューリスティクスを取り込むとき。特に「距離の閾値」「候補の個数上限」など、上流の出力分布に依存する値。
- **なぜ使うか**: 同じルールでも、上流の検出器が変われば候補の密度が変わる。密度が高いパイプラインで元の半径を使うと、候補を拾いすぎてFPが激増する。**ルールの「形」は移植できるが「定数」は移植できない**。
- **使うとどうなるか**: 移植先で FP/FN を実測しながら定数を詰める作業が必要になる（例: 半径 12/15/10 → 8/11/10 で FP 14件→9件）。手間はかかるが、コピーしただけの参加者との差はここで生まれる。理想的には定数を固定値にせず、局所密度から動的に決める形（密度適応型の閾値）まで持っていくとよい。
- **初出**: 2026-08-30 / Biohub - Cell Tracking During Development


### ベイジアン平滑化つきターゲットエンコーディング（Bayesian Smoothing / Shrinkage in Target Encoding）
- **意味**: カテゴリを目的変数の平均で置き換える際、素の平均ではなく `(カテゴリ内の合計 + 全体平均 × m) / (件数 + m)` という縮小推定を使う手法。`m`（smoothing係数）は「仮想的に全体平均のサンプルを m 件足す」ことを意味する。
- **いつ使うか**: 高カーディナリティのカテゴリ列や、連続値を離散化してエンコードする場合。特に**件数の少ないカテゴリが多数存在する**とき。
- **なぜ使うか**: 件数3件のカテゴリの平均は 0.0 や 1.0 といった極端な値になりやすく、そのまま特徴量にすると過学習の温床になる。全体平均に向かって引き戻すことで、統計的に信頼できない推定を穏当にする。件数が m より十分多いカテゴリではほぼ生の平均になるため、情報を捨てすぎない。
- **使うとどうなるか**: CVとLBの乖離が縮まりやすい。ただし `m` は列ごとに最適値が違う（種類が多く1カテゴリあたりの件数が少ない列ほど大きい `m` が必要）ため、**全列共通の固定値にすると効きが半減する**。Out-of-Fold計算と必ず併用すること（自分のラベルで自分の特徴量を作るとリークする）。CatBoostの ordered target statistics は、これを時系列順に自動でやっている。
- **初出**: 2026-08-31 / Predicting Smartphone Addiction (Playground Series S6E8)

### Higher-Order Cell Tracking Transformer（HOCT、エッジ同士を相互参照する追跡）
- **意味**: 細胞追跡において、候補リンク（エッジ）を1本ずつ独立にスコアリングするのではなく、**エッジ同士を3D幾何事前分布のもとで相互にattentionさせる**エッジ中心アーキテクチャ。
- **いつ使うか**: フレーム間の対応付けで「同じノードを複数のエッジが取り合う」競合が頻繁に起きるタスク（密集した細胞、分裂イベントの多いデータ）。
- **なぜ使うか**: 従来のNode Transformer的な手法は、ペア(i, j)が同一細胞かを独立に判定し、競合の解消はILPなど後段の最適化に丸投げしている。しかしILPは与えられた確率を制約下で組み合わせるだけで、**確率そのものの質は競合を考慮していない**。エッジ間attentionを入れると「このエッジを採るなら隣は採りにくい」という情報が確率の段階で織り込まれる。
- **使うとどうなるか**: 深層の事前学習済み画像エンコーダなしでも Cell Tracking Challenge のSOTAを達成したと報告されている（計算コスト面でも有利）。一方でエッジ数の2乗のattentionになるため、候補エッジの事前枝刈りが必須。
- **初出**: 2026-08-31 / Biohub - Cell Tracking During Development（文献経由）

### [CLS]トークンによる行集約（[CLS] Token Aggregation for Tabular Rows）
- **意味**: 表形式データの1行を「フィールド=トークン」の列に変換したうえで、先頭に**学習可能なダミートークン `[CLS]`** を1つ置き、self-attention を通した後の `[CLS]` の出力だけを分類ヘッドに渡す設計。BERT の文分類と同じ機構を表データに持ち込んだもので、FT-Transformer 系のアーキテクチャで標準的に使われる。
- **いつ使うか**: 表データに Transformer を使うとき、**行ごとに重要なフィールドが変わる**と考えられる場合。例: 高所得層では所得が購買を決め、低所得層では設備の有無が決める、といった条件付きの重要度がある問題。逆に、どの行でも同じ数フィールドが支配的なら、単純な平均プーリングや concat で十分。
- **なぜ使うか**: 全トークンの平均プーリングは**全フィールドに等しい重みを与える**ため、その行にとって無関係なフィールドのノイズを必ず混ぜてしまう。`[CLS]` は attention 重みを通じて「この行では何を見るか」を**行ごとに動的に決めて**集約できる。加えて、`[CLS]` は入力に対応する実体を持たないので、特定フィールドの値に引きずられにくい。
- **使うとどうなるか**: フィールド数が多く、条件付きの重要度がある問題で平均プーリングより安定して良くなることが多い。一方で、`[CLS]` 1本に情報を絞る分だけボトルネックになりうるので、**表データでは `[CLS]` と平均プーリングを concat する**折衷がしばしば強い。注意点として、表データの Transformer は単独では GBDT に勝てないことがほとんどなので、**役割は「GBDT と誤りの傾向が違う1本を用意してブレンドで稼ぐ」ことだと最初から割り切る**のが実務的（だから "Tiny" でよい）。射影行列は全フィールドで共有するのが基本（フィールドごとに持つとパラメータがフィールド数倍になり即座に過学習する）。関連: PLR（Periodic-Linear）数値埋め込み、TabM、識別モデルと生成モデルの誤り非相関性。
- **初出**: 2026-09-02 / Playground Series S6E9

### 単一ノブ掃引（One-Variable-at-a-Time / Single-Knob Sweep）
- **意味**: 1回の実験で変更するパラメータを**厳密に1つだけ**に限り、それ以外は明示的に凍結（freeze）したうえで実行し、スコアの変化をその1つに帰属させる実験プロトコル。notebookの冒頭に「Changed / Frozen」の2列表を置き、何を変え何を凍結したかを宣言するのが実践形。
- **いつ使うか**: すでにある程度積み上がったパイプラインを、さらに改善しようとするとき。特にLBの返答回数が限られている（1日5回など）コンペで、**1回の提出から得られる情報量を最大化したい**とき。
- **なぜ使うか**: 2つ以上を同時に変えると、スコアが上がっても「両方効いた」「片方が効いて片方が足を引っ張った」「両方無効でノイズ」の区別がつかない。結果として、次に何を試すべきかの手がかりがゼロになる。1つに絞れば、上がっても下がっても**必ず1ビットの情報が得られる**。
- **使うとどうなるか**: 進捗は遅く感じるが、**掃引の履歴がそのまま知識として蓄積する**。実務上の要点が3つ: (1) 宣言だけでは守れないので、期待値をハードコードした Config-Drift ガードで機械的に強制する。(2) 1点の結果だけでなく **閾値を 0.10 / 0.12 / 0.14 / 0.16 と掃引して曲線として見る** — 単点では改善なのかノイズなのか判別できない。(3) この規律は**新しく足す部分にしか適用されないことが多い**という落とし穴がある。すでに積み上がった5個の機構については、別途 ablation を取らないと寄与が永久に不明のままになる。関連: Config-Driftガード、ノイズフロアの測定、Leave-One-Author-Out アブレーション、最小検出可能差。
- **初出**: 2026-09-03 / Biohub - Cell Tracking During Development

### ハイパーパラメータの特徴量化（Hyperparameter-as-Feature）
- **意味**: 前処理のハイパーパラメータ（ターゲットエンコーディングの平滑化強度、ビン数、乱数シードなど）の**最適値を1つ選ぶ代わりに、複数の値で作った特徴量を全部並べてモデルに渡し、選択をモデルに委ねる**手法。例: `for smooth in [10, 'auto']` で2種類のターゲットエンコード列を作り、列名に `_smooth_10` / `_smooth_auto` を付けて両方渡す。
- **いつ使うか**: 下流が GBDT のように**不要な特徴量を無視するのが得意な**モデルで、かつ前処理パラメータの最適値が列ごとに違うと予想されるとき。逆に、線形モデルやNNのように全特徴に重みが乗るモデルでは、次元だけ増えて過学習するので不向き。
- **なぜ使うか**: 「平滑化強度は10が最適」という前提そのものが疑わしい。稀なカテゴリでは強い平滑化が、頻出カテゴリでは弱い平滑化が良い——**列ごと・値ごとに最適値が違う**のに、グリッドサーチで1つに決めるのは情報を捨てている。複数渡せば、木が分割を選ぶ過程で自動的に列ごとの使い分けが起きる。
- **使うとどうなるか**: 探索コスト（グリッドサーチ）が不要になり、しばしば単一最適値より良くなる。代償は列数の増加で、`colsample_bytree` を下げる（0.3程度）などの調整が必要になる。**リークの規律は緩められない** — 複数作ろうと全て fold の学習側だけで fit すること。関連: ベイジアン平滑化つきターゲットエンコーディング、マルチスケール分位ビン・ターゲットエンコーディング、Nested Honest Cross-Validation。
- **初出**: 2026-09-03 / Playground Series S6E9

### モデル・スイッチボード（Model Switchboard with Frozen Folds）
- **意味**: 特徴量生成から評価・保存までを1本のパイプラインにまとめ、`model_type = 'lgb'` のような**1つの文字列変数だけでモデルを差し替えられる**構造にすること。あわせて fold 分割を `folds = list(StratifiedKFold(...).split(X, y))` としてリスト化・固定し、OOFとテスト予測を `{model_type}_{SEED}_{N_FOLDS}` の命名規則で `.npy` 保存する。
- **いつ使うか**: 複数モデルのアンサンブルを最終的に狙っているとき。特に GBDT 系（LightGBM / XGBoost / CatBoost）と深層系（TabM / RealMLP / FT-Transformer）を混ぜたいとき。
- **なぜ使うか**: 理由は2つ。(1) **公平な比較**: fold分割が違うと0.001程度のスコア差は簡単に出るので、分割を固定しないとモデル比較そのものが無意味になる。(2) **ブレンドの前提条件**: OOFを使って重みを最適化するには、全モデルのOOFが**同一の分割**から出ている必要がある。ここが揃っていないスタッキングは、メタモデルがfold分割の癖を学習してしまい破綻する。
- **使うとどうなるか**: モデルを1つ試すのが `model_type` の書き換え＋再実行だけになり、実験速度が上がる。TabArena（arXiv:2506.16791）の結論——**「GBDTに単体で勝つモデルを探すより、アンサンブルの中でよく効くモデルを探すほうが重要」**——を実行に移すための最小構成でもある。ただし**器を作っただけで満足しやすい**のが最大の落とし穴で、OOFを保存したのにブレンドせず単体スコアで止まっているnotebookは非常に多い。関連: TabM、Nested Honest Cross-Validation、非負制約付きブレンド重み、識別モデルと生成モデルの誤り非相関性。
- **初出**: 2026-09-03 / Playground Series S6E9

## 前処理

### DICOM幾何情報からの左右(laterality)復元
- **意味**: DICOMヘッダーの`Laterality`タグが欠落している場合に、`ImagePositionPatient`（撮像位置）などの座標タグから、左膝か右膝かを幾何学的に推定する手法。
- **いつ使うか**: 医用画像で、左右・向きなどのメタデータタグが機器ベンダーや施設によって記録されていたりされていなかったりする（欠損にばらつきがある）とき。
- **なぜ使うか**: タグに頼れない場合でも、必ず記録されている空間座標という別の情報源から間接的に同じ情報を復元できれば、欠損データを捨てずに済む。
- **使うとどうなるか**: 左右で反転させる正規化処理（例えば左膝の向きに統一する）の前提条件を満たせるようになり、モデルが「左右の違い」という本質と無関係な要因を学習してしまうリスクを減らせる。
- **初出**: 2026-08-10 / RSNA Knee Abnormality Detection

### 物理サイズ基準のクロップ（Physical-Scale Crop, FOVの実測に基づく設計）
- **意味**: 画像をピクセル数ではなく、実世界のミリメートル単位（例: 130mm四方）でクロップしてからリサイズする前処理。クロップサイズは「なんとなくキリの良い数字」ではなく、学習データ全体で実際の撮像範囲（Field of View）の分布を測定した上で決める。
- **いつ使うか**: 医用画像のように、施設・機種によって画素間隔（PixelSpacing）や撮像範囲が大きく異なるデータを扱うとき。
- **なぜ使うか**: ピクセル数だけでクロップすると、同じ「256x256」でも施設によって写っている実際の範囲（cm単位）が異なってしまい、モデルが解剖学的なスケールを一貫して学習できない。ミリメートル単位でクロップしてから統一解像度にリサイズすることで、どの画像でも同じ物理スケールになる。
- **使うとどうなるか**: モデルが解剖学的な大きさ（病変のサイズ等）を一貫した基準で学習できるようになる。クロップサイズが小さすぎると対象部位が見切れ、大きすぎると正規化の意味がなくなるため、実データの分布（パーセンタイル）を確認してから決めるべき。
- **初出**: 2026-08-11 / RSNA Knee Abnormality Detection

### スライス順序の幾何学的復元（Geometric Slice Ordering via Patient-Space Projection）
- **意味**: DICOMのファイル名（多くはランダムなUID）に頼らず、各スライスが持つ空間座標（`ImagePositionPatient`）をスライス面の法線ベクトルに投影した値で並べ替えることで、解剖学的に正しいスライス順序を復元する手法。
- **いつ使うか**: 3D医用画像のように、複数の2D断面（スライス）が1つのシリーズとして保存されており、ファイル名の順序が物理的な並びと一致する保証がないとき。
- **なぜ使うか**: ファイル名順に依存すると、実際には解剖学的に離れたスライス同士を「隣接」として扱ってしまい、隣接スライスを1つの入力（擬似3チャンネル画像など）にまとめる処理が意味をなさなくなる。
- **使うとどうなるか**: 隣接スライスを使う処理（3Dコンテキストの活用、TTAの重複ウィンドウなど）が正しく機能するようになる。座標情報が欠損しているスライスには`InstanceNumber`やファイル名の自然順ソートへのフォールバックが必要。
- **初出**: 2026-08-11 / RSNA Knee Abnormality Detection

### スロット単位の欠損マスキング（代用禁止の原則, No Cross-Slot Substitution）
- **意味**: 複数の入力源（例: 撮像シーケンスのスロット）のうち、特定の条件に合致するデータが存在しない場合、他の条件のデータで代用せず「欠損（absent）」として明示的にマスクする設計原則。
- **いつ使うか**: モデルが各スロットの「存在するかどうか」自体を手がかりにして注意を配分する（Attentionのマスクなど）設計になっているとき。
- **なぜ使うか**: 条件に合わないデータで代用してしまうと、モデルは「本来存在しないはずの情報源が存在する」と錯覚し、実際には獲得されていない証拠に基づいて注意配分や予測を行ってしまう。これは学習時と推論時で同じ代用ルールを使っていても、モデルが学習するべき「欠損というシグナル」そのものを消してしまう問題がある。
- **使うとどうなるか**: モデルの注意機構が実際に取得された情報源のみを根拠に判断するようになり、解釈可能性も上がる。ただし、欠損が多いデータでは学習に使える情報が単純に減るため、代用を許すか厳格にマスクするかはタスクごとにトレードオフを検証すべき。
- **初出**: 2026-08-12 / RSNA Knee Abnormality Detection

### 公式メタデータとの答え合わせによる判定ロジック監査（Audit Against Official Sequence Metadata）
- **意味**: 正規表現やヒューリスティックで自前推定した値（例: T1/T2/PD強調の種別、脂肪抑制の有無）を、コンペ公式が配布する構造化メタデータと突き合わせ、一致率を定量的に算出して検証する手法。
- **いつ使うか**: DICOMヘッダのテキスト情報のような表記揺れの多いデータから、正規表現ベースで属性を推定するロジックを組んだとき、かつ公式の「正解に近い」メタデータが別途配布されている場合。
- **なぜ使うか**: 正規表現ベースの推定ロジックは、パターンの網羅漏れに気づかないまま下流のモデル学習・スロット選定に使われてしまうリスクがある。答え合わせをせずに使うと、静かに劣化した前処理が最終スコアの見えない足かせになる。
- **使うとどうなるか**: 一致率が低いカテゴリが分かれば、そこだけ正規表現パターンを見直すという的を絞った改善ができる。ただし公式メタデータ自体に欠損・誤記があるケースも珍しくなく、100%一致を目指しすぎず「大きく外れていないか」の確認に留めるのが実用的。
- **初出**: 2026-08-13 / RSNA Knee Abnormality Detection

### ナイキスト制約に基づくリサイズ設計（Nyquist-Aware Resampling for MRI）
- **意味**: 画像を任意の解像度に縮小するのではなく、元のMRIのピクセル間隔（PixelSpacing）から決まる実質的な空間分解能（ナイキスト周波数に相当する最小識別可能サイズ）を下回らないよう、リサイズ後の解像度を設計する考え方。
- **いつ使うか**: 医用画像を計算コスト削減のためダウンサンプリングする際、病変のような小さな構造を見落とさない解像度を確保したいとき。
- **なぜ使うか**: 単純に「256x256に統一」のようにキリの良い解像度へ揃えると、元画像の実際の空間分解能によっては小さな病変の情報がリサイズ時点で失われてしまう（サンプリング定理上、元の解像度の半分以下の周期構造は原理的に復元不可能）。
- **使うとどうなるか**: 病変サイズの分布と元画像のPixelSpacingを踏まえて最小限必要な解像度を逆算でき、闇雲な高解像度化による計算コスト増も、過度な縮小による情報欠落も避けられる。
- **初出**: 2026-08-13 / RSNA Knee Abnormality Detection


### KS検定によるOOF↔test分布ドリフト検査（Distribution Drift Screening）
- **意味**: あるモデルのOOF予測とtest予測を順位スケールに直し、`scipy.stats.ks_2samp` でコルモゴロフ–スミルノフ統計量（2つの累積分布関数の最大差）を計算して、乖離の大きいメンバーをスタックから除外する検疫手法。
- **いつ使うか**: 他人が公開したOOF/test予測のペアを寄せ集めてスタックするとき。自作モデルでも、trainとtestで前処理や特徴量生成の経路が違う場合。
- **なぜ使うか**: メタモデルの重みは**OOF側で決まる**のに、実際に適用されるのは**test側**。OOFとtestで予測の分布が違うメンバーに大きな重みが乗ると、CVは良いのにLBだけ落ちる。原因は、trainにしか無いカテゴリ、transductiveな前処理、k近傍のように学習データ密度に依存するモデルなど。
- **使うとどうなるか**: S6E8では閾値0.05で `knn`(0.180)、`rf`(0.081)、`extratrees_support`(0.073) の3つが弾かれた。閾値はデータ量に依存する（行数が多いとKSは小さく出る）ので、**まず全メンバーのKS分布を眺めてから決める**のが安全。例外として、単体性能が無くても「他メンバーの誤差方向」を表す補正役メンバーは、この検査から除外する設計にすることがある。
- **初出**: 2026-08-17 / Playground Series S6E8

### 予測配列のハッシュ重複検出（MD5 De-duplication of Prediction Arrays）
- **意味**: スタックに入れる全予測配列をMD5等でハッシュ化し、バイト単位で同一のものを1つだけ残す。`hashlib.md5(np.ascontiguousarray(a).tobytes()).hexdigest()`。
- **いつ使うか**: 複数の公開データセットからOOF配列を `glob` でかき集めるとき。必ず最初にやる。
- **なぜ使うか**: 完全重複を残すと、**そのモデルの重みが黙って2倍になる**。メタモデルは「同じ意見が2票入った」と解釈してしまい、多様性の推定も狂う。
- **使うとどうなるか**: S6E8では実際に2組見つかった — 同じ配列が別々のデータセットに違う名前で入っていた例と、同一ライブラリ内で `xgb_d7_alt1`/`xgb_d7_alt2` がバイト単位で同一だった例。誰の落ち度でもなく、単に**ディレクトリをglobして拾ったものを全部積むと二重計上する**という話。`np.ascontiguousarray` を挟むのは、メモリレイアウトが違うだけで同じ内容の配列を確実に同一視するため。
- **初出**: 2026-08-17 / Playground Series S6E8

### パーセンタイル基準のロバスト輝度正規化（Robust Percentile Windowing）
- **意味**: 画像を0〜255に正規化する際、最小値・最大値ではなく**1〜99パーセンタイル程度でクリップしてから伸張**する手法。医用画像では「ウィンドウ処理（windowing）」とも呼ばれる。
- **いつ使うか**: MRIのように**画素値に絶対的な単位がない**モダリティ。あるいは、金属アーチファクト・ホットピクセルのような極端な外れ値が混じる画像全般。
- **なぜ使うか**: min-max正規化はたった1個の外れ値で画像全体を潰す。MRIはCTのHU値と違って装置・設定・施設ごとに輝度スケールが全く違うため、そのまま学習させると**施設の違いを病変の違いとして学んでしまう**（ショートカット学習）。
- **使うとどうなるか**: 施設間の分布差が大きく縮まり、転移しやすい表現になる。クリップした範囲外の情報は失われるので、**病変が極端に明るい/暗い場合はパーセンタイルの取り方を調整**する必要がある。前景（対象物）だけを切り出してからパーセンタイルを取ると、背景の空気に引っ張られずさらに安定する。
- **初出**: 2026-08-21 / RSNA Knee Abnormality Detection

### 多言語臨床テキストのUnicode正規化（NFKD + 言語固有の事前置換）
- **意味**: 複数言語が混在するテキストを比較可能な形に揃える処理。NFKD分解でラテン文字のアクセントを剥がしつつ、言語固有の落とし穴を事前の文字置換テーブルで潰す。
- **いつ使うか**: レポート・カルテ・レビューなど、複数言語が1つのカラムに混在するテキストから正規表現でキーワードを拾うとき。
- **なぜ使うか**: 素朴な `.lower()` は言語をまたぐと壊れる。代表例:
  - **トルコ語の ı / İ 問題** — 点なしi(ı)と点ありI(İ)があり、`"İZLENMEZ".lower()` は `"i̇zlenmez"`（結合文字付き）になって `"izlenmez"` と一致しない。事前に `str.maketrans` で潰す。
  - **ドイツ語 ß**, **クロアチア語/セルビア語 đ**, **北欧語 ø / æ** も同様に分解されないので個別対応が要る。
  - **ギリシャ語の μ** — 多くの臨床レポートで MICRO SIGN (U+00B5) が使われており、正しい GREEK SMALL LETTER MU (U+03BC) と別コードポイント。NFKDが片方をもう片方に写す。
- **使うとどうなるか**: 正規化を入れないと、特定の言語・ベンダーのレポートだけが**まるごと取りこぼされる**。しかもエラーは出ないので、「この言語のマッチ率だけ極端に低い」という形でしか気付けない。逆に言えば、**言語別のマッチ率（アノテーション不要で測れる）を監視すれば、辞書のどこが薄いかが分かる**。
- **初出**: 2026-08-22 / RSNA Knee Abnormality Detection

### 語幹正規表現によるステミング代用（Stem-Regex as Lightweight Multilingual Stemming）
- **意味**: `menisc\w*` / `μηνισκ\w*` / `мениск\w*` のように**語幹＋ワイルドカード**の正規表現を書くことで、形態素解析器を使わずに活用・語形変化を吸収する手法。
- **いつ使うか**: 対象言語が多数（今回は9言語）あり、各言語の形態素解析器を用意するのが非現実的なとき。かつ拾いたい語彙が専門用語に限定されているとき。
- **なぜ使うか**: 専門用語は語幹が安定しており（ラテン・ギリシャ語由来なら言語をまたいでも似ている）、活用語尾だけが変わる。`\w*` で語尾を吸収すれば、辞書1行で全活用形をカバーできる。9言語ぶんの解析器を導入・維持するコストと比べて桁違いに安い。
- **使うとどうなるか**: 網羅性は解析器に劣り、**語幹の切り方を誤ると過剰マッチする**（短すぎる語幹は無関係な語を拾う）。また語順が離れるケース（ギリシャ語「十字および側副靭帯」のように形容詞と名詞が分離する）には、形容詞語幹を単独でマッチさせる等の個別対応が要る。失敗モードが「誤検出」ではなく「**沈黙**（何も拾わない）」に寄るのが利点で、沈黙は正解ラベルなしでも検出できる。
- **初出**: 2026-08-22 / RSNA Knee Abnormality Detection

### 欠損インジケータの付加（Missing-Indicator Augmentation, `SimpleImputer(add_indicator=True)`）
- **意味**: 欠損値を補完すると同時に、「この値はもともと欠損していた」ことを示す0/1のフラグ列を追加する手法。
- **いつ使うか**: 欠損が「ランダムに起きた」のではなく「欠損すること自体に意味がある」と疑われるとき（例: 収入欄を空欄にした回答者には傾向がある、特定の検査を受けていない患者には理由がある）。
- **なぜ使うか**: 中央値や最頻値で埋めると、欠損だった行と本当にその値だった行が区別できなくなり、情報が失われる。フラグを残せばモデルが「欠損していたこと」を特徴として使える。木モデルは欠損を内部で扱えるが、NNは欠損を扱えないので補完が必須であり、その分この手当ての価値が高い。
- **使うとどうなるか**: MNAR（Missing Not At Random）な欠損では明確に効く。列数が増える（欠損のある列の数だけ）ため、欠損だらけの高次元データでは次元が膨らむ点に注意。全く欠損のない列にはフラグ列が作られないので、trainとtestで欠損のある列が違うと列数がずれる——`fit` は必ず学習データのみで行い、testには `transform` だけを適用すること。
- **初出**: 2026-08-24 / Playground S6E8（Predicting Smartphone Addiction）
### スライスバンド選択（Slice-Band Cropping）
- **意味**: 3Dシリーズの全スライスを使わず、**相対位置で中央帯だけを採用する**前処理。例: `SLICE_BAND = (0.12, 0.88)` なら、各シリーズのスライスを位置で正規化したうえで中央76%のみ使う。
- **いつ使うか**: MRI/CTのシリーズごとにスライス枚数がバラバラで、かつ端のスライスに対象臓器が写っていない、あるいはノイズが多いとき。多施設データではほぼ必ず該当する。
- **なぜ使うか**: 枚数の絶対値（「最初の5枚を捨てる」）で切ると、40枚のシリーズと200枚のシリーズで捨てる割合が全く変わってしまう。**相対位置で切れば、枚数に依存せず同じ解剖学的範囲に揃う**。さらに情報の薄い端を捨てることで、限られたスライス予算（例: 16枚）を意味のある領域に集中できる。
- **使うとどうなるか**: 入力の質が揃い、無駄なスライスに割く計算が減る。副作用として、**端にしか現れない所見を構造的に見落とす**（膝でいえば膝窩のベーカー嚢腫など、視野の周縁にある病変）。所見ごとのAUCを個別に確認し、特定の所見だけ極端に低い場合は、この帯の設定を疑うべき。
- **初出**: 2026-08-28 / RSNA Knee Abnormality Detection


### 小数桁特徴量（Fractional / First-Decimal-Digit Features）
- **意味**: 連続値カラム `x` から `frac_x = x - floor(x)`（小数部）や `d1_x = floor(x*10) % 10`（小数第1位の数字）を切り出して特徴量にすること。
- **いつ使うか**: 合成データ（CTGAN・拡散モデル等で生成されたKaggle Playgroundのデータ）で、生成器が残した量子化パターンを利用したいとき。
- **なぜ使うか**: 生成器は連続値を独特の刻みで出力するため、**小数部の分布がクラスによって微妙に偏る**ことがある。本来の因果とは無関係だが、Playground Seriesではスコアに直結することが多い。
- **使うとどうなるか**: LBは上がるが、これは「データ生成過程の暗記」であり**実務のモデルには一切転用できない**。さらに private split で分割方法が変わっていれば効果が消えるリスクもある。使うなら、この特徴を落としたときのCV低下幅を必ず測り、**スコアのうち何割がこの痕跡由来か**を把握しておくこと。
- **初出**: 2026-08-31 / Predicting Smartphone Addiction (Playground Series S6E8)

### 合成データのフォレンジック特徴量（Synthetic-Data Forensic Features）
- **意味**: 値そのものではなく、**その値が「どう作られたか」の痕跡**を特徴量にすること。具体的には、各小数位で丸めた値と丸め誤差（`FORENSIC_ROUND_DECIMALS = (0,1,2,3)`）、丸め値の出現頻度、そして**複数の法での剰余**（`value % 2, % 5, % 10, % 100, % 1000`）など。S6E9 の V6 パッチが実装している。
- **いつ使うか**: **Playground シリーズのような合成データ**、あるいは複数ソースを結合したデータ。生成器が内部で乱数を丸めたり、離散分布からサンプリングしたりするため、「値がちょうど100の倍数」「小数第2位が常に0」といった不自然な集中が残っていることがある。それが生成時のクラス条件と相関していれば、モデルにとって強い手がかりになる。**実データ一本のコンペではほぼ無意味かノイズ**なので使わない。
- **なぜ使うか**: 合成データでは、特徴量の意味的な関係よりも**生成過程のアーティファクト**の方が強い信号を持つことが珍しくない。剰余や丸め誤差は、木モデルにとって分割しやすい形で「値の格子構造」を露出させる。
- **使うとどうなるか**: 当たれば数千分の1〜数百分の1の AUC 向上。外れれば**数百次元のノイズが増え**、GBDT の学習が遅くなり過学習リスクが上がる。したがって**入れた場合と外した場合の OOF 差を必ず記録する**こと。効果が小さければ外して次元を減らす方が得。効果が大きければ、それ自体が「このデータの生成過程」についての知見なので Discussion で共有する価値がある。倫理的には「データの穴を突いているだけで、汎化する知識ではない」という自覚が要る。関連: 値の格子構造（Value Lattice）を持つ合成データとルックアップ的モデル、連続値の文字列化トリック、ラベル生成過程の逆算。
- **初出**: 2026-09-02 / Playground Series S6E9

### マルチスケール分位ビン・ターゲットエンコーディング（Multi-Scale Quantile-Bin Target Encoding）
- **意味**: 数値列を**単一の粒度ではなく複数の粒度**（例: 8 / 16 / 32 / 64 ビン）で同時に分位ビン化し、それぞれについてビンごとの目的変数平均（target encoding）を作ること。S6E9 の V5 パッチが `MULTISCALE_TE_BINS = (8,16,32,64)` で実装している。
- **いつ使うか**: 数値特徴と目的変数の関係が**非線形で、かつ効く粒度が事前に分からない**とき。行数が多く（数十万以上）、細かいビンでも1ビンあたりの件数が確保できる場合に特に有効。
- **なぜ使うか**: 20ビンで切ると「所得の非常に細かい帯だけ購買率が跳ねる」効果は平均に埋もれる。逆に64ビンでは1ビンあたりの件数が減って推定がノイズだらけになる。**どの粒度が正解かは列ごとに違う**ので、全部作って GBDT の分割に選ばせるのが素直。信号処理のマルチスケール解析や画像のピラミッド表現と同じ発想。
- **使うとどうなるか**: 粒度を変えた分だけ特徴量が増える（数値列 × 4粒度）。GBDT は冗長な特徴に強いので実害は小さいが、**細かい粒度ほど平滑化（shrinkage）を強くしないと過学習する**。実装上の必須事項が2つ: (1) ビン境界の両端を `-inf` / `+inf` に置き換える — そうしないと test の外挿値で `searchsorted` がビン範囲外になる。(2) 目的変数を使うので、**必ず fold の学習部分だけで fit する**。関連: ベイジアン平滑化つきターゲットエンコーディング、トランスダクティブ前処理。
- **初出**: 2026-09-02 / Playground Series S6E9

### 上三角マスクによる完全相関列の除去（Upper-Triangular Duplicate-Column Pruning）
- **意味**: 相関行列の絶対値を取り、`np.triu(..., k=1)` で**上三角部分だけ**を残したうえで、相関がちょうど 1.0 の列を片方だけ落とす手法。あわせて `nunique() == 1` の定数列も落とす。
```python
corr = X.corr().abs()
upper = corr.where(np.triu(np.ones(corr.shape), k=1).astype(bool))
to_drop = [c for c in upper.columns if any(upper[c] == 1.0)]
```
- **いつ使うか**: 機械的な特徴量生成（桁分解、ビン化、交互作用の総当たりなど）で列数が数倍〜数十倍に膨らんだ直後。手作りの特徴量では滅多に完全重複は出ないが、生成則で作ると必ず出る。
- **なぜ使うか**: **上三角に限定することが必須**で、ここが要点。全体を見ると列AとB、列BとAの両方が閾値に引っかかり、**両方とも削除してしまう**（＝情報を丸ごと失う）。上三角だけを見れば各ペアが1回しか評価されず、必ず片方が残る。閾値を 1.0 ちょうどにしているのは、0.95 などにすると「相関は高いが情報が違う」列まで落としてしまうため。**完全重複の除去は情報を1ビットも失わない**が、高相関の除去は失う。
- **使うとどうなるか**: 列数が減り、学習時間とメモリが減る。GBDTは冗長列に強いので精度への直接の影響は小さいが、**特徴量重要度が読めるようになる**（完全重複列があると重要度が2つに割れて過小評価される）副次効果が大きい。注意点として、相関がちょうど 1.0 でも**符号が逆**（−1.0）の列は `abs()` により同一視されるので、片方が消える。木モデルでは問題ないが、係数の符号を解釈したい線形モデルでは注意。関連: 敵対的検証、条件付き特徴量重要度、合成データのフォレンジック特徴量。
- **初出**: 2026-09-03 / Playground Series S6E9

## 概念・その他

### 提出専用notebook（Submission-Sharing Notebook）
- **意味**: 特徴量エンジニアリング・モデル学習のコードは非公開のまま、学習済みモデルのOOF予測とtest予測（submission）だけをCSVとして公開する形式のKaggle notebook。
- **いつ使うか**: 自分の手法・ノウハウは守りたいが、コミュニティに予測結果として貢献したい、あるいは他者の予測とブレンドして自分のスコアも伸ばしたいとき。
- **なぜ使うか**: 完全なコード公開は競争上の不利になりうるが、予測結果の共有はモデル自体を明かさずに済み、双方にメリットがある。
- **使うとどうなるか**: コミュニティ全体でOOFライブラリが蓄積され、後発の参加者がそれらを組み合わせるだけで高スコアに到達しやすくなる。一方、公開されたOOFのfold整合性が検証されていないと、知らずにリークを含んだブレンドをしてしまうリスクがある（Honest OOF参照）。
- **初出**: 2026-08-06 / Playground Series S6E8

### アンサンブルのアンサンブル（Ensemble of Ensembles / Meta-Blend）
- **意味**: 既に複数モデルをブレンド・スタッキングして作られた「アンサンブル済みの予測」を、さらに別のアンサンブル済み予測と組み合わせる、階層的なアンサンブル構成。
- **いつ使うか**: コミュニティで既に高品質な公開ブレンド・スタッキング結果が複数存在し、それらをさらに組み合わせて短時間でスコアを伸ばしたいとき。
- **なぜ使うか**: 各層のアンサンブルが異なるモデル群・着眼点で作られていれば、層を重ねるごとに多様性が増し、個々の誤りが打ち消し合う余地が広がる。
- **使うとどうなるか**: 短時間で高いLBスコアに到達しやすいが、各層のfold整合性・リークの有無を全て追跡するのが難しくなり、「なぜ効いているか」の説明可能性が下がる。
- **初出**: 2026-08-06 / Playground Series S6E8

### フェイルセーフ推論API設計（Fail-safe Inference Serving）
- **意味**: 本番の逐次推論サーバー（Code Competitionの`predict`関数など）で、想定外のエラーが発生した場合にサーバー全体を停止させず、安全側のデフォルト値（例: 「投資しない＝配分0」）を返して処理を継続させる設計。
- **いつ使うか**: 1回のエラーで全体の推論が停止すると、それ以降のすべての予測が採点不能になってしまうような、逐次・API形式の評価の仕組みで実装するとき。
- **なぜ使うか**: 機械学習コードは実運用データの想定外パターン（欠損の組み合わせ、型の違いなど）で例外を投げることがある。`try/except`で例外を握りつぶし安全なデフォルト値を返すことで、1件のエラーが全体に波及するのを防げる。
- **使うとどうなるか**: 一部の日・一部のサンプルで予測が「安全策（無配分など）」に倒れる代わりに、システム全体の可用性が保たれる。ただし例外を握りつぶしすぎるとエラーの発生に気づきにくくなるため、`logger.warning`等でログには残す、というバランスが実務でも重要。
- **初出**: 2026-08-05 / Hull Tactical - Market Prediction

### トランスダクティブ前処理（Transductive Preprocessing）
- **意味**: 目的変数（ターゲット）を一切使わず、trainとtestのデータを合わせて学習・変換する前処理（例: 欠損値補完モデルをtrain+testの特徴量だけで学習する）。
- **いつ使うか**: 欠損値補完や次元圧縮など、目的変数を必要としない前処理をtrain/testの両方に一貫した方法で適用したいとき。
- **なぜ使うか**: 目的変数を使わないため、通常の意味でのターゲットリークにはならない。trainだけで学習した変換をtestに適用するより、testの分布も考慮した変換ができる。
- **使うとどうなるか**: testのデータ分布に前処理が影響されるため、本番運用時にtestデータが未知の場合（Kaggleのように後から新しいtestが来る、または逐次評価される場合）は、都度前処理をやり直す必要がある点に注意。
- **初出**: 2026-08-04 / Predicting Smartphone Addiction (Playground Series S6E8)

### メトリックハック（Metric Hacking）
- **意味**: 評価指標の計算方法の境界条件や抜け穴を突いて、タスク本来の目的（良いモデルを作ること）を達成せずにスコアだけを不自然に高くする行為・その結果生まれたnotebook群。
- **いつ見られるか**: Kaggleのコンペで、Codeタブを「Best Score」順にソートすると、同一の（あるいは酷似した）スコアに大量のnotebookが集中しているとき。多くの場合、notebookのタイトルに「metric hack」等の言葉が使われていることもある。
- **なぜ注意すべきか**: 表面的なスコアの高さだけでnotebookを選んでしまうと、実際には汎用性のない・学びの少ないコードを参考にしてしまう。
- **見分け方・対処**: 「Best Score」だけでなく「Most Votes」も確認し、複数の観点を突き合わせる。同一スコアへの不自然な集中、極端に短い/単純すぎるコードで高スコア、といったパターンに注意する。
- **初出**: 2026-07-31 / Hull Tactical - Market Prediction（2026-08-03のBiohub・Hull Tacticalでも同様の兆候を確認。2026-08-04のBiohubでは著者自身が"METRIC EXPLOIT"と明記したコードセルを発見: ボリューム範囲外・時刻が負の偽ノード/エッジを追加するだけでスコアが実測+0.08向上したと報告されており、指標が座標・時刻の妥当性を検証していない実例として非常に分かりやすいケースだった）

### Config-Driftガード（Configuration Drift Guard）
- **意味**: パイプラインの設定値（環境変数・ハイパーパラメータ等）を実行開始時に記録・検証し、想定外の値に変わっていないかをコード側でチェックする仕組み。
- **いつ使うか**: 環境変数や設定ファイルで挙動を切り替えるパイプラインで、意図しない設定変更（コピー時の値の写し忘れ、他実験の設定の混入等）による事故を防ぎたいとき。
- **なぜ使うか**: 設定ミスはエラーにならず「なんとなく違う結果」として現れることが多く、気づかれにくい。実行前に想定される設定値との一致をアサーションで確認しておけば、早期に検知できる。
- **使うとどうなるか**: 設定ミスに起因する再現性の欠如や、意図しないスコア変動の原因調査コストを減らせる。一方、チェック項目を都度メンテナンスする手間が発生する。
- **初出**: 2026-08-07 / Biohub - Cell Tracking During Development

### ダミーノード/エッジによる提出フォーマットパディング（Synthetic Hub/Fork Padding for Submission Schema）
- **意味**: 提出データの構造的な要件（例: 一定数以上の分岐＝細胞分裂構造を含むこと）を満たすために、実際の予測内容とは無関係なダミーのノード・エッジ（座標を範囲外の値にするなど明確に無効と分かる値にする）を人工的に追加する後処理。
- **いつ使うか**: 評価システムや後段の処理パイプラインが、提出データに一定の構造的パターン（分岐の数など）を暗黙に期待しており、それを満たさないと処理が失敗したり不当に低評価されたりする場合。
- **なぜ使うか**: 本物の予測内容を歪めてまで構造要件を満たそうとするより、明確に「ダミーである」と分かる形で最小限の要件だけを人工的に追加する方が、予測の質を落とさずに済む。
- **使うとどうなるか**: 提出エラーやフォーマット起因の減点を防げる一方、ダミーデータの座標・IDの設計を誤ると、本物のデータと混同されて評価がおかしくなるリスクがあるため、範囲外の値（負の座標や時刻など）を使うといった明確な区別が重要。
- **初出**: 2026-08-11 / Biohub - Cell Tracking During Development

### 値の格子構造（Value Lattice）を持つ合成データとルックアップ的モデル
- **意味**: 合成生成された表形式データにおいて、ある特徴量の「値そのもの」がラベルと強く紐づいている（生成器が値→ラベルの対応関係を小さな元データセットから記憶しているような）構造。単変量AUCではほぼ無相関に見えても、値ごとの残差同士は独立なデータ分割間で高い相関を持つ、という形で検出できる。
- **いつ使うか**: Kaggle Playgroundシリーズのような合成生成データセットで、単純な線形・単調な関係を仮定したモデル（線形回帰など）の性能が伸び悩み、値ごとに細かく異なる挙動が疑われるとき。
- **なぜ使うか**: 生成器が小規模な実データから合成データを作る過程で、値の組み合わせパターンをそのまま（意図せず）ラベルに焼き付けてしまうことがある。この構造に気づかず連続値として扱うモデル（線形モデルなど）だけを使うと、木モデルやFM・ルックアップ系モデルが拾える情報を取りこぼす。
- **使うとどうなるか**: 値そのものをキーとして扱うモデル（Lookup Transformer、因子分解機など）が、通常の連続値モデルより高い性能を出すことがある。ただし本番の実データではこの種の「値の記憶」構造は存在しないことが多く、合成データ特有のショートカットである点に注意（実務のモデリングにそのまま応用できるとは限らない）。
- **初出**: 2026-08-12 / Predicting Smartphone Addiction (Playground Series S6E8)

### 順位保存型のローカル補正（Rank-Preserving Local Correction / Band Adjustment）
- **意味**: 予測全体の値の集合（multiset）とグローバルな相対順位を変えず、特定のサブグループ（例えば特徴量のある値域）内部の順位だけを、その部分に特化したモデルの予測で再構成する補正手法。
- **いつ使うか**: グローバルなアンサンブルの精度が、特定のサブグループ（値域・欠損パターンなど）だけ弱いことが分かっており、そのサブグループに特化したモデルの追加情報が使えるとき。
- **なぜ使うか**: サブグループ特化のモデルをそのままグローバルなブレンドに混ぜると、そのサブグループの一部が想定外に他グループの順位帯に食い込んでしまい、意図しない副作用が起こりうる。値の集合を変えずサブグループ内の順序だけを入れ替えることで、ローカルな改善がグローバルな順位構造を壊さないことを保証できる。
- **使うとどうなるか**: サブグループの局所的な精度が向上する一方、「サブグループのモデルとブレンドの差（gap）」が大きすぎる場合は逆効果になりうるため、適用前に効果測定（OOFでの検証）が必須。
- **初出**: 2026-08-12 / Predicting Smartphone Addiction (Playground Series S6E8)

### ライセンスゲート（Licence Gate for Public Datasets）
- **意味**: 公開されているモデル予測・データセットをアンサンブルやパイプラインに取り込む前に、各ソースのライセンス表記を確認し、再配布条件が不明・不適合なものを機械的に除外するチェック工程。
- **いつ使うか**: Kaggleのコミュニティ公開データセット（OOF予測のライブラリなど）のように、多数の異なる作者・ライセンスのファイルを1つのパイプラインに集約するとき。
- **なぜ使うか**: 上流のデータセットのライセンス表記が「unknown」等の場合、それを元に作った下流の成果物（今回のようなアンサンブル）の再配布条件も不明確になってしまう。取り込む前に機械的にフィルタしておけば、後から法的にグレーな成果物だと気づいて作り直す事態を避けられる。
- **使うとどうなるか**: 除外によるスコアの悪化（今回の実測では約1e-05）を許容してでも「クリーンな成果物」を優先する、という設計判断を明示的に下せる。除外前後のスコア差を測定して記録しておくことで、後から「本当に安全側に倒す価値があったか」を検証できる。
- **初出**: 2026-08-14 / Predicting Smartphone Addiction (Playground Series S6E8)


### フェイルクローズド提出検証（Fail-Closed Submission Validation）
- **意味**: 提出ファイルを書き出す直前に、列名・行数・**行順**・値域・IDの一意性を検査し、1つでも違えば例外を投げて提出させない設計。異常時に「止まる」のがfail-closed、「続ける」のがfail-open。
- **いつ使うか**: Code Competitionのように**隠しテストで再実行され、失敗しても詳細なログが見られない**環境。複数アームを順位ブレンドするなど、途中でマージや並べ替えが発生するパイプライン。
- **なぜ使うか**: Kaggleの提出は1日数回に制限されており、壊れた提出は回数を無駄にするだけでなく、最終選択の候補を汚す。特に**行順のズレ**は、ファイル形式が完全に正しく見えるのにAUCだけ不可解に低くなる、最も発見しにくいバグ。
- **使うとどうなるか**: 「動いたけど点が低い」という原因不明の状態が消える。**「回復してよい失敗」との区別が肝心**で、推論中の一般例外（GPU不足、DICOM破損）は無情報の提出（全部0.5）を残して生き延びるべき（0点よりAUC 0.5相当のほうがマシ）。一方、ラベル読み出しの失敗のような**前提の崩壊**と、出来上がった提出物の契約違反は、握りつぶしてはいけない。レイヤーごとに何を守るかを決めるのがコツ。
- **初出**: 2026-08-17 / RSNA Knee Abnormality Detection

### メタモデルの収束アサーション（Convergence Assertion）
- **意味**: `LogisticRegression` などの反復ソルバの実際の反復回数を確認し、上限に達していたら例外を出す。`assert max(model.n_iter_) < max_iter`。
- **いつ使うか**: 特徴量が数百本あるスタックのメタモデル、特徴のスケールが揃っていない線形モデル、正則化が弱い設定のとき。
- **なぜ使うか**: lbfgsが収束していない場合、scikit-learnは警告を出すだけで**動きます**。そして厄介なことに、**非収束のフィットはスコアが真の値より「高く」出ます**（正則化が効き切っていない重みで採点されるため）。つまり気づかないまま「良い結果が出た」と誤認する。
- **使うとどうなるか**: 「なぜかCVだけ良くてLBが伴わない」という現象の原因の1つが潰れる。合わせて `StandardScaler` を必ず挟むこと — スケールが揃っていないと、現実的な `max_iter` では収束しない。警告を見逃さないための仕組みを**コードに埋め込む**（目視に頼らない）のが要点。
- **初出**: 2026-08-17 / Playground Series S6E8

### ブートストラップ信頼区間（Bootstrap Confidence Interval）
- **意味**: 元データから**復元抽出**で同サイズの疑似データセットを多数（例: 2000個）作り、それぞれで統計量を計算して分布を得て、その2.5%〜97.5%点を95%信頼区間とする手法。
- **いつ使うか**: AUCのように理論的な分散の式が複雑または存在しない指標について、推定の不確かさを知りたいとき。特に**陽性が数十件しかない稀なラベル**の性能を評価するとき。
- **なぜ使うか**: 点推定だけでは「AUC 0.85」がたまたまなのか本物なのか区別できない。区間が [0.55, 0.98] なら「サンプルが少なすぎて何も言えない」、[0.83, 0.87] なら本物、と判断できる。**理論式を導出せずどんな指標にも機械的に適用できる**のが最大の強み。
- **使うとどうなるか**: 「このラベルのルールベースは信用できる/できない」といった判断が定量的に下せる。実装上の注意として、リサンプリング標本がたまたま単一クラスになるとAUCが計算できないため `if len(set(y)) < 2: continue` のガードが要る（稀なラベルでは実際に起こる）。反復数が少ないと区間自体がぶれるので、2000回程度は取りたい。
- **初出**: 2026-08-18 / RSNA Knee Abnormality Detection

### 弱いベースライン効果（Weak Baseline Inflation）
- **意味**: ベースラインのモデルが弱いと、その上に載せた特徴量エンジニアリングやテクニックの効果が**実際より大きく見える**現象。強いベースラインの上では同じ工夫がほとんど何も足さないことが多い。
- **いつ使うか**: 自分や他人の「この特徴量で+0.002改善しました」という報告を評価するとき。あるいは自分の実験計画を立てるとき。
- **なぜ使うか**: 強いモデルは、その特徴量が持っていた情報を**自力で見つけられる**。弱いモデルは見つけられないので、外から与えると大きく改善する。つまり改善幅は特徴量の価値ではなく、**ベースラインの弱さ**を測っている。
- **使うとどうなるか**: 検証方法は明快で、**同じ工夫を「弱い設定」と「強い設定」の両方の上で試し、改善幅を比較する**。実測すると、ハイパーパラメータをまともにするだけで（例: +0.0046）どの特徴量エンジニアリングより大きい、という結果になることがある。教訓は「特徴量を作る前にベースラインを強くする」「他人の改善報告を見たら、まずベースラインの強さを疑う」。
- **初出**: 2026-08-19 / Predicting Smartphone Addiction (Playground Series S6E8)

### ラベル生成過程の逆算（Label-Generating-Process Recovery）
- **意味**: 合成データのコンペで、元になった公開データセットを調べ、**ターゲットラベルがどう作られたか**（人手のアノテーションか、閾値ルールか、別モデルの出力か）を突き止めること。
- **いつ使うか**: Playground Series のように「実在のデータセットを元に生成モデルで合成した」と明示されているコンペ。Kaggleは元データセットを公式に案内することが多い。
- **なぜ使うか**: ラベルが機械的なルールで作られている場合（例: `daily > 8 OR social > 4` で違反ゼロ件）、**どこが簡単でどこが勝負どころかが構造的に分かる**。ルールの内側/外側は決定的に決まり、境界の帯だけが本当の予測問題になる。
- **使うとどうなるか**: 3つの実利がある。(1) **AUCの理論上限が計算できる** — 帯の中の何%が本当にランダムかが分かれば上限が出るので、「あと0.001上げる努力に意味があるか」を判断できる。(2) **労力の配分が決まる** — 境界帯だけに絞ったモデリングに集中できる。(3) **合成データの歪みが見える** — 元データで内訳の合計が総量を超えているなら、生成モデルもその歪みを引き継いでいるので、「なぜこの特徴量は単独では効くのに条件付けると消えるのか」の答えになる。なお、これはテストラベルを覗く行為ではなく**公開情報からデータ生成過程を理解する作業**であり、上位陣が必ずやっていること。
- **初出**: 2026-08-19 / Predicting Smartphone Addiction (Playground Series S6E8)

### 勝者の呪い／選択バイアス（Winner's Curse, Selection-Induced Optimism）

- **意味**: 多数の候補の中から「測定値が最も良かったもの」を選ぶと、その測定値は**真の実力より必ず上振れしている**という統計的な現象。候補が多いほど、また測定のノイズが大きいほど、上振れ幅は大きくなる。
- **いつ意識するか**: 公開LBで提出を選ぶとき、CVスコアで特徴量やハイパーパラメータを選ぶとき、多数のシード・ブレンド比率から最良を選ぶとき。要するに「たくさん試して一番良いのを採る」場面すべて。
- **なぜ重要か**: 「選んだから良く見える」のか「良いから選ばれた」のかは、**選択に使ったデータの上では区別できない**。この区別ができないまま最終提出を決めると、privateで順位が崩れる（shake-up）。
- **どう対処するか**: (1) **選択に使っていないデータ**で再評価する（nested CV、ホールドアウト）。(2) 候補数を意識的に絞る。(3) 選択ルールを**事前に文書化**して、結果を見た後の判断バイアスを排除する。(4) 改善の**方向が複数の独立した検証で一致するか**を見る。S6E8の実測では、公開LB相当のサイズで選ぶと未使用データで平均マイナスになった。
- **初出**: 2026-08-20 / Playground S6E8 (Predicting Smartphone Addiction)

### 先端でのみ壊れる順位（High Correlation, Broken Frontier）

- **意味**: 公開LBと非公開LBの**全体の順位相関は高い**のに、**上位10チームだけは総入れ替えになる**という現象。Playground Season 6 では7エピソード中3回、公開top10のprivate top10生存者がゼロだった（S6E7では公開1位がprivate 440位）。
- **いつ意識するか**: 「順位相関が0.99だから公開LBは信用できる」と言いたくなったとき。上位が僅差でひしめいているコンペ全般。
- **なぜ起きるか**: Spearman相関は全チームを対象にするので、実力差の大きい中位〜下位が正しく並べば高い値になる。一方**先頭の数チームは互いに実力がほぼ同じ**で、順位を決めているのは実力ではなくノイズ。**平均的な指標が良いことは、先端での信頼性を何ら保証しない**。
- **使うとどうなるか（教訓）**: 「全体としては信頼できる」という統計を、「自分の順位も信頼できる」と読み替えないこと。自分が上位の僅差帯にいるほど、公開LBの情報価値はゼロに近づく。
- **初出**: 2026-08-20 / Playground S6E8 (Predicting Smartphone Addiction)

### 挑戦者モデルの採用ゲート（Challenger Admission Gate）

- **意味**: 新しく作ったモデル・特徴量・後処理を、**スコアが上がったという理由だけでは採用しない**設計。ブートストラップによる改善の再現率、fold間の一貫性、ラベル別のAUCなど複数の条件を通過したものだけが最終ブレンドに重みを得る。
- **いつ使うか**: パイプラインに何世代も改良を積んでいて、どれが本当に効いているか分からなくなってきたとき。特に上位が僅差のコンペ終盤。
- **なぜ使うか**: 「試して上がったら入れる」を繰り返すと、**上がったように見えただけのもの**が蓄積する（勝者の呪い）。ゲートを設けることで、改善の主張に統計的な裏づけを要求できる。
- **使うとどうなるか**: RSNA Knee DINOsaur V3 では `_v10_bootstrap_positive_fraction(repeats=280)`（280回リサンプルして改善が正だった割合）と `_v11_fold_consistency`（fold間で一貫して改善しているか）を採用条件にしている。副作用として、**本当は効く改善もゲートで落ちる**（検出力とのトレードオフ）ため、閾値自体をチューニングの対象と考えるべき。
- **初出**: 2026-08-20 / RSNA Knee Abnormality Detection

### Ladderメカニズム（再利用可能ホールドアウト）

- **意味**: 公開リーダーボードのスコアを、**前回のベストより統計的に有意に良いときだけ更新する**アルゴリズム（Blum & Hardt, ICML 2015）。参加者が何回submitしても公開LBが過学習しないことが理論的に保証される。
- **いつ使うか**: コンペ主催者としてLBを設計するとき。参加者としては、**自分の提出履歴に事後的に適用**して「自分がいつからノイズを追いかけ始めたか」を可視化する用途に使える。
- **なぜ有効か**: 過学習の原因は「LBに何ビットの情報を漏らしたか」なので、更新を有意な改善だけに制限すれば漏れる情報量が抑えられる。
- **使うとどうなるか**: 参加者側で真似すると、5桁目の微小な改善はすべて「更新なし」と判定され、**手元のCVを信じる以外の選択肢がなくなる**。それが正しい状態。
- **初出**: 2026-08-20 / Playground S6E8（関連文献: https://proceedings.mlr.press/v37/blum15.pdf ）

### モンキーパッチによる上流スクリプト改変（Monkey-Patching, and its Silent-Failure Risk）
- **意味**: 外部リポジトリのソースコードを**テキストとして読み込み、特定の文字列を置換して書き戻す**ことで、forkせずに挙動だけ変更する手法。
- **いつ使うか**: 上流の実装をほぼそのまま使いたいが、1〜2箇所だけ変えたい場合（例: TTAを4方向から8視点に拡張する）。forkして自分で保守するコストを避けたいとき。
- **なぜ使うか**: 差分が明示的で、上流の更新に追随しやすい。notebook上で完結するため、外部リポジトリを別途管理する必要もない。
- **使うとどうなるか**: 便利だが**危険**。置換対象の文字列が上流で1文字でも変われば、`str.replace` は**何もせず静かに成功を返す**。結果として「TTAを拡張したつもりが素のまま走っている」という状態に気づけない。**必ず `if old not in src: raise RuntimeError(...)` を添えること**。設定ドリフト・ガードを実装するほど再現性に気を遣っているコードでも、ここだけ穴が空いている例が実際にある。
- **初出**: 2026-08-21 / Biohub - Cell Tracking During Development

### スレッドのオーバーサブスクリプション（Thread Oversubscription）
- **意味**: 外側で並列化（ThreadPool / ProcessPool）しているのに、内側のライブラリ（NumPy/OpenBLAS/OpenCV）も既定で全コアを使おうとして、**総スレッド数がコア数を大幅に超え、かえって遅くなる**現象。
- **いつ使うか（対処するか）**: DICOM読み込み・画像デコード・特徴抽出などを自前のワーカーで並列化するとき。ほぼすべての前処理パイプラインが該当する。
- **なぜ対処するか**: スレッドが増えすぎるとコンテキストスイッチとキャッシュの奪い合いが支配的になり、**並列化したのに逐次より遅い**という結果になる。しかもCPU使用率は100%に張り付くので、一見「働いている」ように見えて原因に気づきにくい。
- **使うとどうなるか（対処すると）**: `os.environ.setdefault('OMP_NUM_THREADS','4')`（`OPENBLAS_NUM_THREADS`, `MKL_NUM_THREADS` も同様）や `cv2.setNumThreads(1)` を、**ライブラリをimportする前に**設定する。原則は「**並列化は1階層に集約する**」。外で並列化するなら内は1に、内に任せるなら外は逐次にする。実行時間制限のあるCode Competitionでは、これだけで数十分単位の差が出ることがある。
- **初出**: 2026-08-21 / RSNA Knee Abnormality Detection

### 多重共線性による打ち消し合い係数（Cancelling Coefficients under Multicollinearity）
- **意味**: 相関の非常に高い予測配列を線形回帰で混ぜたとき、`+1.303` と `-1.014` のように**大きさが1を超え、互いに打ち消し合う**係数ペアが現れる現象。
- **いつ使うか（＝いつ疑うか）**: 数十本以上のOOF配列をリッジ回帰でブレンドし、係数を目視したとき。単純平均なら重みは合計1・各項が小さいはずなのに、個々の絶対値が1を超えているのは危険信号。
- **なぜ起きるか**: ほぼ同じ情報を持つ2つの説明変数がある場合、回帰の解は一意に定まりにくくなる。回帰は残差を減らすために「大きな正 − 大きな負」を割り当て、実質的に**2モデルの差分**（片方が他方より高く評価した部分）を新しい特徴量として使う。その差分はしばしばOOFのノイズそのもの。
- **使うとどうなるか**: OOFスコアは上がるが、テスト分布がわずかにずれると**符号ごと逆に効く**。Public LBで僅差の上位に群がっている解が、Privateで大きくシャッフルされる典型的な原因。対策は①非負制約(NNLS)/hill climbing、②相関の高い配列を事前に刈り込む、③複数シードで重みを推定して**係数の分散**を見る、④OOF−LBギャップを採否基準にする。
- **初出**: 2026-08-22 / Playground Series S6E8

### 解像度の二乗則によるキャッシュ予算設計（Resolution-Squared Cache Budgeting）
- **意味**: 画像キャッシュのメモリ量が $N_\text{sample} \times N_\text{slot} \times S \times P^2$ で決まる（$S$=スライス数、$P$=解像度）という関係から、**どの軸に予算を割くか**を決める設計判断。
- **いつ使うか**: 3D医用画像や動画のように、1サンプルあたり百枚単位のファイルを読む必要があり、かつ毎エポック同じピクセルを使う（ファインチューニング）とき。
- **なぜ使うか**: このパイプラインのコストは**演算ではなくI/O**が支配する。1回だけなら許容できる読み込みが、エポックごとでは破綻するので、一度デコードして `uint8` でメモリに置く。そのうえで重要なのは指数 — **キャッシュは解像度の二乗で増え、スライス数には線形でしか増えない**。つまり「カバレッジは安い軸、解像度は高い軸」。ただしナイキスト制約により解像度には下限があるので、下限を割らない範囲でスライス数を増やすのが合理的。
- **使うとどうなるか**: 予算を空きメモリの100%に設定してはいけない。エンコーダ・活性値・転送中バッファが同じプールから取られるうえ、**これは超過すると遅くなるのではなくランが死ぬ唯一のアロケーション**だから、意図的に低めに設定する。`uint8` で持つことで float32 の1/4に抑え、正規化は使用直前にGPU上で行う。
- **初出**: 2026-08-22 / RSNA Knee Abnormality Detection

### グローバル名前空間の接頭辞分離（Global-Namespace Prefixing in Notebooks）
- **意味**: 1つのnotebookに複数の独立したパイプラインを同居させるとき、各パイプラインの変数名に `_rad_` / `_A5_` のような接頭辞を付けて衝突を防ぐ運用。
- **いつ使うか**: 他人の複数のnotebookを1本に統合するとき（ブレンド系のCode Competition提出で頻出）。
- **なぜ使うか**: Jupyter/Kaggleのnotebookは**全セルが同じグローバル名前空間を共有する**。セル3が `Path` や `np`、あるいは `CONFIG` のような一般的な名前を別の意味で再代入すると、セル1が作った状態が黙って壊れる。関数内に閉じ込めるのが本来は正しいが、他人のコードを大きく改変せずに統合したい場面では接頭辞が現実的な妥協案になる。
- **使うとどうなるか**: 統合時のバグを大幅に減らせる。補助として `_SAVED = dict(globals())` でスナップショットを取り、必要なら復元する手法も併用される。**限界**: 接頭辞は規約にすぎず強制力がないため、import した名前（`np`, `Path` など）の上書きは防げない。根本的には各パイプラインを関数やモジュールに閉じ込めるべき。
- **初出**: 2026-08-24 / RSNA Knee Abnormality Detection

### 寄与ゼロのタイトル詐称（Title–Contribution Mismatch in Public Notebooks）
- **意味**: notebookのタイトルや説明が特定の手法を掲げているのに、最終出力へのその手法の寄与が実質ゼロである状態。例: 「NN Residual Network」と題しながら、最終ブレンドでNNの重みが `1e-4`。
- **いつ気をつけるか**: 公開notebookのスコアを「その手法の実力」として引用しようとするとき。特にLB上位帯のブレンド系notebook。
- **なぜ重要か**: 高スコアの公開notebookは、しばしば①他人の提出CSVの再ブレンド、②他人の学習済み重みの読み込み、で構成される。パイプラインの実装品質と、スコアの出どころは**別物**。混同すると「この手法は効く」という誤った学習をしてしまう。
- **確認方法**: ①最終ブレンドの重みを読む、②入力データセット（Input）に他者の submission.csv や weights が含まれていないか見る、③実行時間を見る（数分なら学習していない）、④「自分の成分だけ」でのスコアが報告されているか探す。
- **初出**: 2026-08-24 / Playground S6E8・RSNA Knee Abnormality Detection

### 十進固定精度による出力の再現（Decimal + ROUND_HALF_EVEN）
- **意味**: 浮動小数点（float, 2進数）ではなく `decimal.Decimal`（十進、任意精度）で計算し、丸めに**偶数丸め（ROUND_HALF_EVEN, 銀行家丸め）**を使うことで、出力のCSV文字列をビット単位で再現可能にする手法。
- **いつ使うか**: 出力ファイルのハッシュ一致が要件のとき（監査・提出物の凍結・法規制のある金額計算など）。また、丸め処理を大量の行に繰り返し適用するとき。
- **なぜ使うか**: float は `0.4` や `0.1` を正確に表現できないため、演算順序が変わると最下位桁がずれ、CSVの文字列表現が変わってSHA-256が一致しなくなる。また、0.5をつねに切り上げる通常の丸めは**誤差が正方向に蓄積**するが、偶数丸めなら多数行にわたって平均的に打ち消される（MAEを競う場面では地味に効く）。
- **使うとどうなるか**: floatより桁違いに遅いので、ホットループ全体に使うのは非現実的。「最終出力の整形・丸めだけDecimal」という使い分けが実際的。なお**桁数の多さは精度の高さではない**——309行のテストに対して有効数字60桁の校正係数を書いても、それは再現性のための実装都合であって統計的な精度の主張ではない。
- **初出**: 2026-08-25 / UMUD Challenge

### アンカー再生型提出（Anchor-Replay Submission / 学習なしルックアップ提出）
- **意味**: 過去に採点済みの提出CSVをソース内（base64+gzip等）に埋め込み、それに決定論的な後処理を適用して提出を再構築する形式のnotebook。学習も推論も行わない。
- **いつ使うか**: **手法としては推奨しない**。ただし「後処理・キャリブレーションだけでスコアがどこまで動くか」を実験的に切り分ける目的や、再現可能な形でスコアを凍結・配布する目的では意味がある。
- **なぜ危ういか**: (1) 新しい入力に一切適用できない（モデルではなくルックアップテーブル）。(2) 補正パラメータがPublic LBのフィードバックで選ばれている場合、**多重比較による過学習**であり、Private LBで崩れる。(3) スコアの高さが手法の良さを示さないため、公開notebookの序列を歪める。
- **見分け方**: 「No training」「LB 0.xxxxx」を掲げ、実行時間が数十秒、GPUなし、入力データセットが1ファイルだけ、ソースに巨大なbase64文字列がある、といった特徴。**Kaggleでnotebookを選ぶときは、スコアではなく「新しいデータに適用できるか」を最初に確認する**こと。
- **参考にすべき点**: この形式のnotebookでも、否定的な実験結果（「広範なFL補正は0.82498と大幅悪化」など）を表で残している点や、入力のハッシュ・行数・列名を検証してから処理する点は、そのまま自分の実験に取り入れる価値がある。
- **初出**: 2026-08-25 / UMUD Challenge


### globals()スナップショットによる擬似セルスコープ（Global-Namespace Snapshot & Restore）
- **意味**: 巨大なセルの先頭で `_SAVED = dict(globals())` として名前空間を退避し、末尾で書き戻すことで、そのセル内の変数定義を外に漏らさない技法。
- **いつ使うか**: 独立に開発された複数のパイプラインを1つのnotebookに連結するとき（Kaggleの提出notebookで頻出）。同名だが値の違う定数（`SLICE_BAND` がセル1では `(0.2,0.8)`、セル2では `(0.12,0.88)`）が衝突する場面。
- **なぜ使うか**: notebookのセルはすべて同じグローバル名前空間を共有するため、後から連結したコードが前のコードの定数を**静かに上書き**する。エラーは出ず、スコアだけが落ちる。関数やモジュールに包むのが正攻法だが、他人のコードを丸ごと貼り付ける場面ではコストが高すぎる。
- **使うとどうなるか**: セル単位の擬似的なスコープが得られ、連結の事故が減る。**副作用**: 退避と復元の間で例外が出ると名前空間が中途半端な状態で残る（`try/finally` にすべき）。また、意図的にセル間で共有したい変数まで巻き戻されるため、共有したいものは別名で明示的に外へ出す必要がある。あくまで**リファクタリングまでのつなぎ**と割り切ること。
- **初出**: 2026-08-26 / RSNA Knee Abnormality Detection

### ブートストラップ勝率（Bootstrap Win-Rate）
- **意味**: 2つのモデルAとBを比べるとき、「平均スコアが上がった」ではなく、**検証セットをN回リサンプリング（復元抽出）して、そのうち何%でAがBに勝ったか**を報告する方法。例:「+0.0131、2000回のブートストラップのうち92.7%で勝利」。
- **いつ使うか**: 検証セットが小さく（数十〜数百件）、スコア差が小さい（AUCで0.01程度）とき。要するに**Kaggleでの改善判断のほぼ全部**。
- **なぜ使うか**: 単一の数字の比較（0.8923 → 0.9054）は、その差が実力なのか標本のゆらぎなのかを区別できない。信頼区間を出すという手もあるが（→「ブートストラップ信頼区間」の項）、**勝率のほうが直感的に読める**——「92.7%」は「10回に9回は勝つ」と即座に翻訳でき、実験を採用するかの判断に直結する。区間が重なるかどうかを目視する必要もない。
- **使うとどうなるか**: 「上がったから採用」を「どのくらい確からしく上がったか」に置き換えられる。目安として、**勝率が 90% を切る改善は追わない**という運用にすると、偶然の上振れを実力と誤認して複雑さを積む失敗が大きく減る。注意点は、ブートストラップは**手元の検証セットが母集団を代表している**という仮定の上に成り立つこと。45症例のgold setをいくらリサンプリングしても、そのgold set自体が偏っていれば偏った結論しか出ない（実際にRSNA Kneeの事例では、45症例で +0.010 と見えたアンサンブル効果が、実LBでは +0.001 しかなかった）。**サンプルサイズそのものを増やすことの代替にはならない**。
- **初出**: 2026-08-27 / RSNA Knee Abnormality Detection（RSNA Knee | DINOsaur V4）

### 厳格なno-op設計によるアンカー保護（Strict No-Op Optional Stage / Anchor Preservation）
- **意味**: パイプラインの追加要素（オプションのアンサンブルアーム、外部重み、2枚目のGPUを使う処理）を、**成功したときだけ効果を持ち、少しでも怪しければ完全に何もしない（no-op）**ように書く設計。基準となるスコア（アンカー）を絶対に割らないことを保証する。
- **いつ使うか**: Code Competition のように、**隠れテストセット上での実行時エラーが即0点**になる環境。あるいは、既に良いスコアが出ている提出に対して実験的な要素を足すとき。
- **なぜ使うか**: 「動くはず」のコードは本番で動かない。重みファイルが無い、2枚目のGPUが割り当てられない、特定のstudyだけDICOMが壊れている——これらは全部起こる。try/except で握りつぶすだけでは**部分的に適用された中途半端な状態**が残り、かえって危険。「全部成功したか、完全に無かったことにするか」の二択にするのが安全。
- **使うとどうなるか**: 実装例としては、(a) 成功率のしきい値を置く（`legacy_fraction >= 0.98` を満たさなければ legacy の予測を丸ごと捨てる、`swin_fraction >= 0.97`）、(b) チェックポイントが見つからなければブロック全体をスキップ、(c) サンプル単位で例外が出たら 0.5（＝情報なし）で埋めて続行、(d) 2枚目のGPUが無ければそのアームは起動しない。副作用として、**「効いているはずの要素が実は全部no-opになっていた」ことに気づけない**リスクがあるので、各段階が実際に適用されたかを必ずログに出す（例:「legacy complement: MCL=w0.160,corr=0.981; ...」のように、適用した重みと観測した相関を印字する）。ログの無いno-op設計は、静かな失敗を静かにしただけになる。
- **初出**: 2026-08-27 / RSNA Knee Abnormality Detection（RSNA Knee | DINOsaur V4）

### ラベル不要の出力監査（Label-Free Output Audit）
- **意味**: 正解ラベルを一切使わずに、生成した提出ファイルの正しさを検証すること。検査は3層に分けられる。(1) **スキーマ**（列名・列順・行数・IDの連番性・値域）、(2) **カバレッジ**（テストの全対象に行が存在するか）、(3) **ドメイン制約**（時間的に逆行するエッジが無いか、1つの親から3つ以上に分岐していないか、確率が単調性制約を破っていないか）。
- **いつ使うか**: 提出ファイルを書き出した直後、必ず。特に**提出回数が1日数回に制限されるCode Competition**では必須。長時間の推論の後にフォーマット違反で0点になるのが最も高くつく失敗。
- **なぜ使うか**: 正解が無くても検出できる誤りは想像以上に多い。そして重要なのは、これが**モデルの静かな故障（silent failure）を捕まえる唯一の手段**である点。モデルがクラッシュせずに変な出力を出し続けている状態は、スコアを見るまで気づけないが、ドメイン制約違反としてなら即座に検出できる。
- **使うとどうなるか**: 提出枠の浪費が無くなり、後処理のバグが早期に露見する。実装上の注意として、**`assert` ではなく `raise RuntimeError(...)` を使うこと** — `assert` は最適化フラグ（`python -O`）で消えるため、本番で必ず効かせたい検査には不適切。また検査が厳しすぎると正当な出力まで弾くので、制約は「仕様上ありえない」ものに限る。
- **初出**: 2026-08-28 / Biohub - Cell Tracking During Development

### 効率トラック評価（Efficiency Score / 精度と実行時間のトレードオフ）
- **意味**: 予測精度だけでなく**推論の実行時間も同時に評価する**副次的な順位づけ。RSNA Knee 2026の例では `Efficiency = AUC/(Benchmark - maxAUC) + RuntimeSeconds/32400` を**最小化**する形で定義され、独立した賞金枠（$18,000）が用意されている。
- **いつ使うか**: コンペに効率賞トラックがあるとき。また実務では常に意識すべき軸で、「精度が0.5%高いが推論に10倍かかるモデル」は臨床・production環境ではまず採用されない。
- **なぜ使うか（なぜ狙う価値があるか）**: 本線のリーダーボードは巨大アンサンブルに支配されるため上位が極めて厚い。一方**効率トラックは公開notebookが薄く、競争が緩い**ことが多い。単一モデルとアンサンブルの精度差がわずか（例: 0.924 vs 0.936）なら、実行時間で割られる効率スコアでは単一モデルのほうが期待値が高くなりうる。**同じ資産で狙える別の賞金**という発想。
- **使うとどうなるか**: 「アンサンブルを1本削ると精度がどれだけ落ち、時間がどれだけ縮むか」を測る習慣がつき、パイプラインのどこが本当に効いているかが見えるようになる。副作用として、効率を追うと本線の順位は当然下がるので、**どちらを狙うかは提出の選択段階で明確に分けて設計する**必要がある（多くのコンペで両方に同じ提出をエントリーできる）。
- **初出**: 2026-08-28 / RSNA Knee Abnormality Detection

### イベント認識バリデーション標本設計（Event-Aware Validation Sampling）
- **意味**: 検証セットを作るとき、単純なランダム抽出やアルファベット順ではなく、**評価指標に効く稀なイベントが必ず含まれるように**標本を選ぶこと。例: Biohubで「細胞分裂を含む動画」を優先して検証セットに入れる（分裂は全動画の約44%にしか存在しない）。
- **いつ使うか**: 指標が複数の成分の加重和で、そのうち一部が**稀なイベントに依存**しているとき。検証セットが小さいときほど重要。
- **なぜ使うか**: 機械的に選ぶと、たまたま稀イベントがゼロの検証セットができあがる。するとその成分のスコアは常に0で**動かず、改善しても劣化しても検知できない**。指標の15%が丸ごと見えない検証セットは、その15%について何の情報も与えない。
- **使うとどうなるか**: 検証セットが公式指標の全成分に反応するようになり、LB提出を消費せずに判断できる範囲が広がる。副作用は、稀イベントを意図的に濃縮するぶん**検証セットが母集団より難しく（あるいは易しく）なり、絶対値がLBとズレる**こと。使い方は「絶対値の推定」ではなく「変更前後の差分の符号を見る」と割り切るのが正しい。
- **初出**: 2026-08-29 / Biohub - Cell Tracking（Biohub 0.928 LB）

### LBプロービングと shake-up リスク（Leaderboard Probing）
- **意味**: 少数の公開ラベルや public LB のフィードバックに合わせてブレンド重み・後処理を調整し、**public スコアだけを押し上げる**行為。汎化性能の改善ではなく、公開スコアという「もう1つの検証セット」への過学習である。
- **いつ使うか**: 意図的にやるなら「情報収集」として。ただし**最終提出に選ぶかどうかは別の判断**にすべき。
- **なぜ危険か**: public LB は test の一部（しばしば数%〜数十%）でしかない。そこに合わせて調整するほど private では崩れ、順位が大きく入れ替わる（**shake-up**）。特にラベルが数十件しかない状況での所見別重み推定は、統計的にほぼノイズを拾っている。
- **実務的な扱い方**: (1) プロファイルを**リスク別に分類して名前を付ける**（低分散 / public狙い / 高リスク）、(2) 全プロファイルを成果物として書き出し、選択は1か所のスイッチに閉じ込める、(3) 元の未調整版を対照として必ず保存する、(4) 最終2提出のうち1つは必ず保守側に充てる。RSNA Knee の "Crazy LB Tune" はこの4点をすべて実践しており、**手法そのものより「自分がやっているのは指標へのハックだと自覚し明示する姿勢」が学ぶべき点**。
- **初出**: 2026-08-29 / RSNA Knee Abnormality Detection（RSNA Knee Crazy LB Tune）

### モデル重みのチェックサム検証（Weight Integrity Verification）
- **意味**: 推論前に、読み込むモデル重みファイルの SHA-256 を計算し、コードにハードコードした期待値と照合して不一致なら停止する仕組み。
- **いつ使うか**: 他人の公開データセットとして配布された重みを使うとき、複数の重みを組み合わせるアンサンブル、そして再現性を主張したい公開notebook。
- **なぜ使うか**: 重みファイルが差し替わったり破損したりしても、モデルは例外を出さず「それらしい」出力を返す。**エラーは出ないのにスコアだけが静かに落ちる**という、最も気づきにくい壊れ方をする。ハッシュ照合はこれを実行開始直後に検出できる唯一の確実な方法。
- **使うとどうなるか**: 「なぜか再現しない」の調査時間が劇的に減る。同じ思想の兄弟として、設定値を照合する Config-Drift ガード、実際にロードされた実体を出力する Pipeline Manifest がある。**設定・重み・実体の3層すべてを検証して初めて再現性が機械的に保証される**。
- **初出**: 2026-08-29 / Biohub - Cell Tracking / RSNA Knee（両notebookで採用）

### 形式に寛容な予測ローダ（Tolerant Prediction Loader）
- **意味**: 他人が作った予測ファイルを大量に取り込む際、拡張子（CSV/Parquet/npy）・列名・値のスケール（確率かロジットか）を**推定して吸収し、読めなかったものは黙ってスキップして続行する**ローダ。
- **いつ使うか**: 公開OOFライブラリを数十本まとめて読むスタッキング、複数チーム由来の予測を集約する場面。
- **なぜ使うか**: 数十人が別々に作ったファイルに統一仕様を要求すると、そもそも集まらない。また添付データセットの1つがパス変更されただけで全体が落ちるのは、パイプラインとして脆すぎる。**「緩く推測し、失敗は局所化する」**方が全体の可用性が高い。
- **使うとどうなるか**: 頑健になる一方、**推測が外れたことに気づけない**という代償を負う。典型的な失敗は「`|値| <= 1.1` なら確率」というヒューリスティックが、予測が全て0付近に集中したロジット出力を誤判定して二重変換すること。対策は、読み込み時に各ファイルの min/max/平均/件数を必ず印字し、`SKIP` されたファイル数も集計して目視できるようにすること。**寛容さと可観測性はセットで導入する**。
- **初出**: 2026-08-29 / Playground S6E8（S6e8 Public Ensemble）

### フェイルオープン / フェイルクローズドの使い分け（Fail-Open vs Fail-Closed）
- **意味**: 処理が例外を投げたときに、**続行する（フェイルオープン）** か **停止する（フェイルクローズド）** かの設計判断。
- **いつ使うか**: 長時間の推論パイプラインを組むとき、各ステップについて「これが壊れたらどうするか」を決める場面。
- **なぜ使い分けるか**: 判断基準は **「そのステップが無くても提出が成立するか」** の一点。
  - **提出形式の検証・重みのハッシュ照合・入力データの網羅性チェック** → **フェイルクローズド**（例外で停止）。壊れた提出をそのまま出しても0点なので、止めた方が確実に得。しかもnotebook内で落とせば原因が特定できる。
  - **キャリブレーション・任意の後処理・診断出力** → **フェイルオープン**（`try/except` でスキップして続行）。無くても提出は成立するので、ここで止めるのは損失しかない。
- **使うとどうなるか**: 「全部try/exceptで囲む」は最悪の選択になる（形式エラーが黙って通る）。逆に「全部assertで落とす」も、5時間の推論が診断コードのバグで飛ぶ。**例外を握りつぶすときは必ずログを出す**（`print('calibration skipped safely: ...')`）ことで、事後に「何が最終提出になったのか」が追える状態を保つのが要点。
- **初出**: 2026-08-30 / RSNA Knee Abnormality Detection（Biohubの提出監査がフェイルクローズド側の対比例）


### 検証セット過学習（Validation Overfitting / 適応的過学習）
- **意味**: 検証データのスコアを見ながらモデル選択・アンサンブル重み決定・ハイパーパラメータ調整を繰り返すことで、**検証スコアだけが上がり、真の汎化性能は上がっていない**状態になること。
- **いつ気をつけるか**: 検証セットが小さいとき（数十〜数百件）、かつ選択・調整の試行回数が多いとき。特に**貪欲前向き選択やhill climbingでアンサンブルを組む工程**は、実質的に検証セットに対する数十回のフィッティングになっている。
- **なぜ問題か**: 検証セットは「1回だけ使う」前提でこそ不偏推定になる。同じデータを繰り返し参照すると、ノイズに合致した選択が採用され、選択された時点のスコアは**必ず上振れ**する（勝者の呪い）。RSNA Knee のように gold ラベルが58件しかない場合、稀少所見の陽性は数件で、macro AUCは ±0.02 程度平然と動く。
- **対策と効果**: (1) 選択用と最終評価用でデータを分ける（nested CV）、(2) bootstrapで信頼区間を出し、区間が重なる改善は採用しない「採用ゲート」を設ける、(3) 大きな弱教師ラベル側でCVを組み、小さなgoldは最終確認だけに使う。適用するとCVの見た目のスコアは下がるが、**LB/privateとの乖離が縮む**。
- **初出**: 2026-08-31 / RSNA Knee Abnormality Detection

### スペクトルバイアス（Spectral Bias / Frequency Principle）
- **意味**: ニューラルネットワークが学習の初期段階で**低周波（滑らかな）成分を先に、高周波（細かい凹凸）成分を後から**学ぶという経験的・理論的性質。裏返すと、生の数値を1次元のまま入力した MLP や Transformer は、**入力に対する急峻な変化を表現するのが苦手**ということ。
- **いつ使うか（この概念を思い出すべき場面）**: 表形式データに深層モデルを使っていて、GBDT に勝てないとき。GBDT は分割で任意の階段関数を作れるので高周波成分に強く、この差がそのまま性能差になっていることが多い。また、座標や時刻を直接入力する回帰問題（NeRF、暗黙関数表現、時系列）でも同じ問題が出る。
- **なぜ重要か**: 対処法が明確だから。**入力を高周波基底に展開してから渡せば、線形結合だけで細かい凹凸を表現できる**。具体的には (1) フーリエ基底 `sin(π·x·f), cos(π·x·f)`（f = 1,2,4,8 …）、(2) 局所ガウス基底／RBF、(3) 区分線形（分位ベース）埋め込み。Gorishniy らの "On Embeddings for Numerical Features in Tabular Deep Learning" は、これらの埋め込みを与えるだけで**深層表形式モデルと GBDT の差がほぼ埋まる**ことを実験的に示している。
- **使うとどうなるか**: 学習が速く収束し、非線形な応答曲線を持つ列で明確に効く。注意点が2つ: (1) 周波数を上げすぎると**ノイズまで表現してしまい過学習**する（f=1,2,4,8 程度が実務的な上限）。(2) フーリエ基底は**大域的**なので局所的な効果の表現は苦手 — 特定位置に山を持つ局所基底を併用して大域と局所の両方をカバーするのが定石。(3) 欠損値では基底成分を `active = 1 − missing` で丸ごと 0 にし、代わりに `missing` フラグを別次元で渡すこと。`sin(0)=0, cos(0)=1` を「値が0の人」と混同させないため。関連: PLR（Periodic-Linear）数値埋め込み、[CLS]トークンによる行集約。
- **初出**: 2026-09-02 / Playground Series S6E9

### 上限バインド診断（Cap-Binding Diagnostics）
- **意味**: パイプラインに設けた上限値（救済するノード数の上限、追加してよい分裂の個数上限、リトライ回数の上限など）が、実行時に**実際に張り付いた（binding）のか、余裕があった（slack）のか**をログに出すこと。最適化理論でいう制約のアクティブ／非アクティブ判定を、実装の安全弁に対して行う。
- **いつ使うか**: `MAX_NODES_FRAC = 0.012` / `MAX_NODES_ABS = 120` のような**魔法の定数**をコードに置いたとき、必ずセットで。特にその上限をチューニング候補にしようとする前に。
- **なぜ使うか**: 上限に**張り付いているなら、その上限がスコアを制限している**＝チューニングすべき対象である。**余裕があるなら上限は無関係**で、実際に効いているのは別の条件（例: 平均エッジ確率0.88という品質フィルタ）のほうである。この区別なしに上限値を掃引すると、効かない変数を延々と動かすことになる。
- **使うとどうなるか**: 「どのノブを回すべきか」が1回の実行で分かる。実装は `print(f"rescued={n} / cap={cap} ({'BINDING' if n>=cap else 'slack'})")` の1行で足りる。より一般化すると、**閾値・上限・タイムアウトのたぐいは、設定値ではなく到達値をログに出す**という原則になる（パイプライン・マニフェストの考え方と同じ）。関連: パイプライン・マニフェスト、Config-Driftガード、単一ノブ掃引。
- **初出**: 2026-09-03 / Biohub - Cell Tracking During Development

### 候補保持率の下限（Minimum Candidate Retention Floor）
- **意味**: フィルタや合意ゲートを掛けたあと、**元の候補のうち最低でも何割は残さなければならない**という下限を設けること（例: `BIOHUB_DUAL_SEED_MIN_CANDIDATE_RETENTION = 0.90` ＝ 融合後も候補の90%以上を保持する）。下限を割り込む場合はフィルタを緩めるか、その単位（フレーム・症例）だけフィルタ前の状態にロールバックする。
- **いつ使うか**: 「複数モデルの合意を取る」「低確信の予測を落とす」といった**間引き系の処理**を入れるとき。特にその処理が偽陽性の削減を狙っている場合。
- **なぜ使うか**: 間引きは常に**偽陽性と真陽性の両方を削る**。平均的には得でも、たまたま2つの検出器の意見が食い違ったフレームでは候補が壊滅し、そこから先の軌跡が全部途切れる——という**局所的な破滅**が起きうる。全体スコアの平均では見えないが、そのフレーム以降の全ノードを失うタイプの指標では致命的になる。下限は「間引きが暴走した単位だけを安全側に倒す」ためのガードレール。
- **使うとどうなるか**: 平均性能をわずかに犠牲にして、最悪ケースを大きく改善する。設計上の注意が2つ: (1) 下限は**単位ごと（フレームごと・症例ごと）に判定する**こと。全体で90%を満たしていても、特定フレームだけ30%になっている状況を見逃す。(2) 下限が発動した回数を必ずログに出す — 毎回発動しているならフィルタ自体が強すぎるし、一度も発動しないなら下限は不要（＝上限バインド診断と同じ話）。関連: フレーム単位ロールバックガード、上限バインド診断、no-regressionゲート、双方向調和平均マッチング。
- **初出**: 2026-09-03 / Biohub - Cell Tracking During Development

## ライブラリ・ツール

### Optuna
- **意味**: Pythonのハイパーパラメータ自動探索（ベイズ最適化の一種であるTPEアルゴリズムなど）ライブラリ。
- **いつ使うか**: グリッドサーチやランダムサーチより効率的にハイパーパラメータを探索したいとき。特に1回の学習に時間がかかるモデル（LightGBM, XGBoost等）で試行回数を節約したいとき。
- **なぜ使うか**: 過去の試行結果を踏まえて次に試すパラメータを賢く選ぶため、同じ試行回数でもグリッドサーチより良い解に到達しやすい。目的関数を自由に定義できるため、評価指標（balanced accuracyやカスタムスコアなど）を直接最適化できる。
- **使うとどうなるか**: 手動チューニングより高いスコアに到達しやすい一方、目的関数の中で交差検証をしっかり行わないと、探索空間に過学習してしまう（ハイパーパラメータのoverfitting）リスクがある。
- **初出**: 2026-07-31 / Predicting Student Health Risk (Playground Series S6E7)

### Zarr（チャンク化配列ストレージ形式）
- **意味**: 大きな多次元配列（3D顕微鏡ボリュームなど）を、圧縮された小さな「チャンク」に分割してディスクに保存するファイル形式・ライブラリ。必要な部分だけを読み込める。
- **いつ使うか**: メモリに乗り切らないほど大きな3D/4D配列データ（顕微鏡タイムラプス画像、地理空間データなど）を扱うとき。
- **なぜ使うか**: 配列全体を一度にメモリへ読み込む必要がなく、特定のフレーム・領域だけを高速に読み書きできる。並列処理・クラウドストレージとの相性も良い。
- **使うとどうなるか**: チャンクサイズの選び方次第でアクセスパターンごとの速度が変わる（今回のBiohubコンペのコードでは、Zarr読み込みに失敗した場合の下位互換フォールバックとしてblosc2による生チャンク解凍も併用されていた）。
- **初出**: 2026-08-04 / Biohub - Cell Tracking During Development

### オフラインパッケージインストール（`pip install --no-index --find-links`）
- **意味**: PyPI（インターネット上のパッケージサーバー）に接続せず、あらかじめダウンロード・添付しておいた`.whl`ファイル一式のフォルダだけを見てパッケージをインストールする手法。
- **いつ使うか**: Code Competitionの推論環境のように、インターネットアクセスが無効化されている（かつ実行時間制限もある）環境で、標準では入っていないライブラリを使いたいとき。
- **なぜ使うか**: `pip install`は通常PyPIへの接続が必要だが、事前にwheelファイルをKaggle Datasetとして添付しておけば、オフラインでも同じコマンド体系でインストールできる。`--no-deps`と組み合わせることで、依存関係の自動解決によって既存のNumPy/PyTorch等のバージョンが意図せず変わってしまう事故も防げる。
- **使うとどうなるか**: 環境構築の再現性が高まる一方、必要なパッケージ・依存パッケージをすべて事前に洗い出してwheelを用意しておく手間がかかる。バージョン違いのOSやPythonでビルドされたwheelだと動かないため、実行環境と同じ条件でビルドしたwheelを用意する必要がある。
- **初出**: 2026-08-05 / Biohub - Cell Tracking During Development

### TabM（Parameter-Efficient Ensembling for Tabular Deep Learning）
- **意味**: 表形式データ向けの深層学習モデルで、1つのネットワーク内で複数のMLP（多層パーセプトロン）が重みの大部分を共有しながら、少しだけ異なるパラメータを持つことで「MLPアンサンブル」相当の効果を効率的に得るアーキテクチャ（ICLR 2025）。
- **いつ使うか**: 表形式データに対して、複数モデルのアンサンブルによる精度向上の恩恵を受けたいが、フルサイズのモデルを何個も学習・保持するコストは避けたいとき。
- **なぜ使うか**: 通常のアンサンブルはモデルをN個独立に学習・保存する必要があり計算コストが線形に増えるが、TabMは重み共有によりアンサンブルの多様性の恩恵の多くを1つのモデルサイズに近いコストで得られる。
- **使うとどうなるか**: 単体の勾配ブースティング木モデルと組み合わせてブレンドすると、木モデル同士では出しにくい「異なる間違え方」をするモデルとして多様性に貢献しやすい（今回のPlaygroundコンペのOOFブレンドnotebookでも上位スコアのメンバーとして採用されていた）。
- **初出**: 2026-08-05 / Predicting Smartphone Addiction (Playground Series S6E8)


### RadImageNet（医用画像で事前学習されたCNN重み）
- **意味**: CT・MRI・超音波の医用画像 約130万枚で学習された ResNet-50 等の事前学習重み。ImageNet（自然画像）の医用版という位置づけ。
- **いつ使うか**: 医用画像の分類・検出タスクで、学習データが少ないとき。特にDINOv2/v3のような自己教師あり基盤モデルと**組み合わせてアンサンブル**する用途。
- **なぜ使うか**: 事前学習ドメインが違えば、獲得する特徴も違う。ImageNetは犬・車・家具のエッジとテクスチャを学ぶが、RadImageNetは組織のコントラストや解剖学的構造を学んでいる。医用タスクではImageNet事前学習を上回る報告がある。
- **使うとどうなるか**: 単体で強いだけでなく、**DINOv2系とは別の見方を持ち込むのでアンサンブルとして価値が高い**（「強さより、ばらけ方」）。注意点は、(a) グレースケール前提の設計なので3ch入力への詰め方に注意、(b) 入力の正規化定数がImageNetと異なる場合がある、(c) ライセンス（研究用途に限定される場合がある）をコンペのルールと照らして確認すること。
- **初出**: 2026-08-17 / RSNA Knee Abnormality Detection

### bfloat16による混合精度推論（AMP with bfloat16）
- **意味**: 16ビット浮動小数点のうち、**指数部をfloat32と同じ幅にした**形式。仮数部が短い代わりに、float16より値域がはるかに広い。Ampere世代（compute capability 8.0）以降のGPUで利用可能。
- **いつ使うか**: 大きなVision Transformerやボリューム画像の推論で、メモリと速度を稼ぎたいとき。学習でも使える。
- **なぜ使うか**: float16は値域が狭く、オーバーフロー/アンダーフローを避けるために `GradScaler` などの対策が要る。bfloat16は指数部がfloat32と同じなので、**基本的にそのまま置き換えられる**。
- **使うとどうなるか**: メモリがおよそ半減し、行列演算が高速化する。仮数部が短いので数値精度は落ちるが、推論では実害が出にくい。注意点は、(a) 古いGPU（T4はTuring世代）ではbf16のネイティブサポートが無く、float16へのフォールバックが必要 — `torch.cuda.get_device_capability()` で分岐する、(b) 出力を後段でブレンドする場合、精度差が指紋照合の許容誤差（例: 0.002）に収まるかを確認する。
- **初出**: 2026-08-17 / RSNA Knee Abnormality Detection

### taxonomy-agnostic なレポート分類（ラベル体系をプロンプトで差し替える設計）
- **意味**: 分類したいラベルの一覧（taxonomy）をモデルの出力層に固定クラス数として焼き込むのではなく、**プロンプトの中に所見リストと出力スキーマとして記述する**設計。ラベル体系が変わってもモデル本体は変えずに済む。
- **いつ使うか**: 同じレポート集合に対して所見の定義や粒度がまだ動いているとき。あるいは、あるコンペで作った抽出パイプラインを別のラベル体系のタスクに流用したいとき。
- **なぜ使うか**: 従来の教師あり分類器は「12クラスのヘッド」を持つため、13個目の所見を足すには再学習が必要になる。プロンプト側にリストを置けば、所見の追加・統合・言い換えが設定ファイルの編集で済む。稀少ラベルのために専用ヘッドを用意する必要もなくなる。
- **使うとどうなるか**: 実験のイテレーションが劇的に速くなる一方、出力が自由テキスト（JSON等）になるためパース失敗の処理が必須になる。また、固定ヘッドのモデルのように「全クラスのlogitを一度に取る」ことができないので、確率値を使ったキャリブレーションやアンサンブルは工夫が要る。
- **初出**: 2026-08-19 / MOSAIC（放射線レポート分類, RSNA Knee Abnormality Detection 文脈）

### (+)F1 / (−)F1 の分離報告（陽性検出と否定検出を別々に測る）
- **意味**: 「所見あり（definitely present）」を当てる性能と、「所見なし（definitely absent、＝レポート中で明示的に否定されている）」を当てる性能を、1つのF1にまとめず**別々のマクロF1として報告する**評価の作法。
- **いつ使うか**: 自然言語（放射線レポート、問診票、レビュー文など）から弱教師ラベルを作るとき。特に否定表現・不確実表現が頻出するドメイン。
- **なぜ使うか**: 「断裂を認める」と「断裂は認めない」は文字列としてはほぼ同じで、**否定語を1つ見落とすだけで陽性と陰性が反転する**。全体の精度だけを見ていると、この反転が「そこそこの精度」に紛れて見えなくなる。分けて測れば、辞書のカバレッジ不足（陽性の取りこぼし）と否定処理の欠陥（陰性を陽性と誤る）のどちらがボトルネックかが即座に分かる。
- **使うとどうなるか**: 弱教師ラベルの誤りが「どちら向きの誤りか」で切り分けられ、修正の優先順位が決まる。注意点は、否定文の評価セットを作るには**否定文だけを意図的にサンプリングする**必要があること（自然なサンプリングでは否定文の割合が低く、統計的に検出できない）。
- **初出**: 2026-08-19 / MOSAIC（放射線レポート分類, RSNA Knee Abnormality Detection 文脈）

### 多重仮説追跡（MHT / Multi-Hypothesis Tracking）
- **意味**: 物体追跡において、各フレームで「1つの正解」を確定させず、**複数の割当候補（仮説）を同時に保持したまま先へ進み**、後のフレームで得た情報を使って過去の仮説の尤度を事後的に再評価する枠組み。最も尤もらしい仮説を最後に選ぶ。1979年 Reid の提案が原型で、確率的な実装として Multi-Bernoulli Mixture（MBM）などがある。
- **いつ使うか**: 「その場では判断できないが、少し先まで見れば明らかに分かる」種類の誤りが支配的なとき。細胞追跡なら、見た目がそっくりな細胞の乗り移り、過分割を分裂と誤認する、など。長い系列ほど価値が出る。
- **なぜ使うか**: 逐次的な貪欲マッチング（ハンガリー法を1フレームずつ適用する等）は、**一度下した判断を取り消せない**。frame 2 の誤りが frame 5 の情報から明白でも、修正する経路がない。MHTは仮説を残しているので、後から「こちらの枝のほうが尤もらしかった」と乗り換えられる。
- **使うとどうなるか**: 長期的な一貫性（系統樹の正しさ、細胞周期の分布）が大きく改善する。一方で仮説数が指数的に増えるため、**枝刈り（`H_max` で上限、同一状態の仮説をマージ）が必須**。注意点は、(a) 仮説を1本に制限すると実質ただの逐次割当に退化するので、その場合はコスト設計側の工夫だけが効く、(b) カルマンフィルタのような線形運動モデルを前提にしがちだが、細胞のようなランダムウォーク的な動きには合わないので、画像から回帰した動き推定で置き換えるほうが良い。
- **初出**: 2026-08-22 / Cell Tracking according to Biological Needs（細胞追跡, Biohub 文脈）

### 分裂許容コスト行列（列を複製して1対多の割当を作る）
- **意味**: ハンガリー法などの**1対1割当アルゴリズムを一切変更せずに**、「1つの親に2つの子」という割当を表現するテクニック。コスト行列 `[C | Diag_∞(c_unassigned)]` の右側に、**左ブロックの複製 `C'` をもう1枚連結する**だけ。複製列は同じ物体を指すので、検出 j₁ が元の列に、j₂ が複製列に入れば、1対1制約を守ったまま両方が同じ親に割り当たる。
- **いつ使うか**: 細胞分裂、物体の分割・合流、1つのトラックが2つに分かれうるあらゆる追跡問題。既存のハンガリー法ベースのパイプラインに最小の変更で分裂を導入したいとき。
- **なぜ使うか**: ハンガリー法は各行・各列に高々1つしか割当を許さないので、**分裂は定式化上そもそも表現不可能**。多くの実装がこれを「割当のあとに分裂だけ別ロジックで救済する」後処理で回避しているが、後処理は割当のコスト情報を捨てているうえ、増やす処理と抑えるブレーキを別々にチューニングする羽目になる。列複製なら、分裂の判断が**元のコスト（＝距離・エッジ確率・見た目の類似度）の中で他の候補と直接比較される**。
- **使うとどうなるか**: 分裂が割当本体で拾えるようになり、後処理の総量規制のようなヒューリスティックが不要になる。意外な副産物として、**分裂を許す設定ではハンガリー法がむしろ高速化する**（最適化問題が緩くなり、ヒューリスティックな初期解がそのまま最適解になる頻度が上がるため）。注意点は、**複製列だけを足すと分裂が出すぎる**こと。必ず分裂側にペナルティ（下記のErlang寿命コスト等）を併せて入れる。
- **初出**: 2026-08-22 / Cell Tracking according to Biological Needs（細胞追跡, Biohub 文脈）

### Erlang寿命事前分布による分裂ペナルティ
- **意味**: 「その細胞は今何フレーム生きているか（Age）」を使って、分裂の割当コストに `−log(ErlangCDF_{α,β}(Age))` を加算する仕組み。Erlang分布は細胞寿命のモデルとして生物学で使われる分布で、そのCDF（累積分布）は「この年齢までに分裂しているはずの確率」を表す。若い細胞ほどCDFが小さく、`−log` が大きくなる ＝ **分裂コストが高くなる**。
- **いつ使うか**: 分裂・出芽・生成イベントの誤検出を抑えたいとき。特に、後処理側で「分裂の総数を全体の N% までに制限する」のような**文脈を持たない上限**でごまかしている場合の置き換えとして。
- **なぜ使うか**: 「生まれたばかりの細胞がもう分裂するのはおかしい」という**ドメイン知識をコスト関数に直接埋め込める**。総量規制と違い、細胞ごとに強さが変わる。さらに多重仮説追跡と組み合わせると、「frame 2 の分裂を認めると frame 5 で寿命が短すぎる矛盾が出る」という**時間をまたいだ矛盾の検出**として機能し、過去の誤りを事後的に棄却できる。
- **使うとどうなるか**: アブレーション上、この項を外すと他の貢献を全部入れていても指標が崩壊する（CHOTA 76.75 → 48.31）ほど効きが大きい。`α, β` は学習データの系統樹から分裂間隔のヒストグラムを取って当てはめる。注意点は、**分裂がまれで推定が不安定な短い系列**では当てはめを諦め、系列長 K を使って `α = K, β = 1/K` と置いて一律に高いペナルティにするのが実務的（論文著者自身がこの逃げ道を明記している）。
- **初出**: 2026-08-22 / Cell Tracking according to Biological Needs（細胞追跡, Biohub 文脈）

### 生物学的指標 vs 技術的指標（CT / TF / BC(i) / CCA / CHOTA）
- **意味**: 細胞追跡の評価を2層に分ける考え方。**技術的指標**はフレーム単位の検出精度やリンクの正誤（一般の多物体追跡と同じ発想）。**生物学的指標**は系統樹の再構成としての正しさを測る。Cell Tracking Challenge が報告する主なものは、**CT**（1つも誤りなく完全に再構成できたトラックの割合）、**TF**（トラックが連続して正しかった区間の平均割合）、**BC(i)**（分裂イベントの検出正解率）、**CCA**（予測と正解の細胞周期長の分布の重なり）。**CHOTA** は各リンクを「追跡結果全体への影響」で重み付けする総合指標。
- **いつ使うか**: 追跡結果を「軌跡・系統樹として使う」下流タスクがあるとき。手法の改善が本物か判断したいとき。
- **なぜ使うか**: この2層は**しばしば乖離する**。技術的指標は既存手法群でほぼ飽和しており、差はノイズに埋もれる。一方、生物学的指標で見ると同じ結果に最大5.7倍の差がつく。理由は、**系統樹の中で1回の分裂を間違えると下流の枝が丸ごと壊れる**のに、技術的指標はそれを普通のリンク1本の誤りと同じ重さで数えるから。**同じ結果を別の指標で測ると順位が全く変わる**という、Kaggleの「何を測っているのか」問題そのもの。
- **使うとどうなるか**: 「技術的指標は動かないのに生物学的指標だけ大きく改善する」改善策が見えるようになる。逆に、技術的指標だけを見ていると、そういう改善は「効果なし」として捨てられてしまう。注意点は、コンペの公式スコアがどちら寄りかを必ず確認すること（Biohubの公式スコアは検出＋リンクJaccard＋**分裂イベントの正誤**の複合なので、CTCの純粋な技術的指標よりは生物学寄りに位置する）。
- **初出**: 2026-08-22 / Cell Tracking according to Biological Needs（細胞追跡, Biohub 文脈）

### プロヴェナンス階層化監査（Provenance Tiering of an OOF Pool）
- **意味**: 他人が公開した out-of-fold 予測配列をスタッキングのプールに取り込むとき、各メンバーを「**自作 / 検証済み（訓練コードかfold ID が公開されていて実際に照合できた） / 著者申告（notebookに書いてあるが配列生成コードは非公開） / 検証不能（公開資料から分割方式を特定できない） / 別スキームと判明**」の階層に分類し、その一覧を明示的に開示する運用。
- **いつ使うか**: 外部由来の予測を混ぜるすべての場面。Kaggleの公開OOFライブラリを使うとき、実務なら外部ベンダーのスコアや別チームのモデル出力をアンサンブルに入れるとき。
- **なぜ使うか**: スタックの前提は「全メンバーのOOFが同じ分割から作られていること」。この前提が成り立たないメンバーは**捨てるか、重みを割り引くか**の判断が要るが、そもそも**どれが該当するかを可視化しない限り判断すらできない**。「全部同じ分割で作られている」と書かれた文章は、多くの場合検証されていない願望にすぎない。
- **使うとどうなるか**: 「一番スコアの高いプール」と「一番信頼できるプール」が別物だと分かるようになる。実測例では、検証不能メンバーを全部落とすと nested OOF が 0.9700832 → 0.9698843 に下がるが、その差は自作配列429本を足すのとほぼ同じ大きさで、**検証不能メンバーの寄与が「巨大な追加努力1回分」に相当する**ことが定量化できた。注意点として、**都合の悪いメンバーを黙って落とすと監査の意味が消える** — 落とすなら落とした事実ごと開示する。
- **初出**: 2026-08-23 / Playground Series S6E8（S6E8 the score I would not pick）

### 外来foldによるOOF楽観バイアス（Foreign-Fold OOF Optimism）
- **意味**: 5-fold前提のスタックに、10-foldなど**別の分割で作られたOOF配列**を混ぜたときに起きる歪み。自分の検証fold行に対して、そのメンバーは訓練データの約90%（5-foldなら80%）を見た状態で予測しているため、**OOF値が実力より良く見える**。
- **いつ使うか（＝いつ疑うか）**: 他人のOOFを取り込むとき、複数プロジェクトの予測を横断的に混ぜるとき、過去に作ったOOFを分割を変えた後に使い回すとき。
- **なぜ重要か**: これは**リークではない**。テストにラベルは無いので、そのメンバーのテスト予測自体は健全。壊れるのは**メタモデルの重み推定だけ**。したがって害は「LBが下がる」という分かりやすい形では出ず、**LBは上がるのにCVとLBの乖離だけが静かに広がる**という、最も気づきにくい形で現れる（強いモデルは過大な重みでもまだ役に立つため）。
- **使うとどうなるか**: 対処は3択 —(a) 除外する、(b) 残すが重みを割り引く、(c) 残して開示だけする。少なくとも「見なかったことにする」以外を選べるようになる。検算のコツは、**同一手法を5-foldと10-foldで作った配列が両方プールにあるなら、その差を実測する**こと（引用ではなく手元の数字にできる）。関連: 「Honest OOF」「Nested Honest Cross-Validation」。
- **初出**: 2026-08-23 / Playground Series S6E8（S6E8 the score I would not pick）

### 識別モデルと生成モデルの誤り非相関性（Discriminative × Generative Diversity in Stacking）
- **意味**: 決定境界 `P(y|x)` を直接学習する**識別モデル**（LightGBM/CatBoost/XGBoost/NN）だけのプールに、特徴量の分布 `P(x|y)` を学習してベイズ則で反転する**生成モデル**を混ぜる多様化戦略。
- **いつ使うか**: プールが十分大きいのにアンサンブルの改善が頭打ちになったとき。特にメンバーがほぼ全部GBDTで、シード違い・ハイパラ違いしか多様性が無いとき。
- **なぜ使うか**: **スタックが報酬を受け取るのは「精度」ではなく「誤りの非相関性」**。同じ枠組みの中で強いモデルを足しても、同じ場所で同じように間違うので加算的な情報が少ない。生成モデルは**そもそも違う質問に答えている**ので、単体では明確に弱くても誤りの構造が根本的に違う。
- **使うとどうなるか**: solo AUCが最下位帯の50本を足しただけで nested OOF が有意に上がる、という直感に反する結果が出る。**検証方法が重要**で、「弱い」だけでなく「プールの合意（ベースメンバーのランク平均）との順位相関が低い」ことを両方示さないと主張が成立しない。実測では自作50本の合意との相関中央値が、プールメンバー同士の相関中央値より明確に低かった。注意点は、生成モデルは表形式データでは実装コストが高く、画像・テキストではさらに難しいこと。
- **初出**: 2026-08-23 / Playground Series S6E8（S6E8 the score I would not pick）

### レベル2メンバーの排除原則（Level-2 Member Exclusion in Stacking Pools）
- **意味**: スタックの入力プールから、**それ自体がプールのブレンドであるメンバー**（他人が公開した blend / stack の出力）を除外する原則。除外対象の判別は手書きリストではなく、**命名規約や構造から計算**する。
- **いつ使うか**: 公開OOFライブラリを集めるとき。ライブラリには単体モデルとブレンドが混在しており、名前を見ないと区別できないことが多い。
- **なぜ使うか**: プールのブレンドをプールの中に入れると、そのメンバーは**隣にいる他メンバーの重み付け直し**になり、メタモデルの重み推定が循環する。さらに、そのブレンドは「データのモデル」ではなく「プールのモデル」なので、solo AUCがプール最強に見えても**新しい情報を1ビットも持っていない**可能性がある。
- **使うとどうなるか**: プール最強のsolo AUCが「ベースモデル最強」に置き換わり、**何と比較して改善したのかが正しく言えるようになる**。実測ではブレンドを除外してもスコアはほぼ動かなかった（-0.000001程度）が、**動かないこと自体が「情報を足していなかった」証拠**。手書きリストは必ず腐るので、`startswith("naji_blend")` のような規約ベースの判定にしておくと、プール構成が変わっても壊れない。
- **初出**: 2026-08-23 / Playground Series S6E8（S6E8 the score I would not pick）

### 再現ゲート（Reproduction Gate before Novel Claims）
- **意味**: 他人の手法を独立再実装したとき、**新しい主張をする前に、先行notebookが公開している数値を再現できるかを最初の関門にする**設計。再現しなければ以降のすべての結論が無効なので、そこで止める。
- **いつ使うか**: 論文やnotebookのパイプラインをコピーではなく「読んで書き直す」形で取り込むとき。実験の比較の土台を他人の実装に置くとき。
- **なぜ使うか**: 「Aさんの手法に自分の工夫を足したら上がった」という主張は、**Aさんの手法を正しく再現できていて初めて成立する**。再現できていないなら、上がったのは工夫のせいか実装ミスの偶然か区別がつかない。コピーせず書き直す場合、ローダの細部（例: `oof`→`test` の置換をbasenameのみに適用するか、パス全体に適用するか）で**メンバーが黙って何十本も消える**ような差が生まれ得る。
- **使うとどうなるか**: 実験の腕（arm）が1つ増えるが、それは「比較専用の腕」であって提出には使わない。実測例では、先行notebookが公開した2つの値（177メンバーで0.970094、ブレンド除外で0.970083）の両方を再現できるかを最初に確認していた。**2つ以上の独立な数字で照合する**のがコツ（1つだけだと偶然一致し得る）。
- **初出**: 2026-08-23 / Playground Series S6E8（S6E8 the score I would not pick）

### パイプライン・マニフェスト（Resolved-State Pipeline Manifest）
- **意味**: 実行の最後（または推論直前）に、**設定した値ではなく「条件分岐が解決された後の実際の状態」**を1箇所にまとめて印字する仕組み。「dual-seedアンサンブル: 要求=True, 重みファイル発見=False ← 単一seedにフォールバック中」のように、要求と実現の両方を並べる。
- **いつ使うか**: オプション機構が多いパイプライン、外部成果物（学習済み重み・キャッシュ）の有無で挙動が変わるパイプライン、複数人やLLMが継ぎ足しで書いたコード。
- **なぜ使うか**: 設定ファイルに `USE_X=1` と書いてあっても、重みファイルが無ければフォールバックして X は動いていない。**「設定した値」と「実際に有効になった機構」は別物**であり、多くの静かなバグ（機構が一度もロードされていなかった、重複した処理セルが黙って勝っていた、検証の比較が死んでいた）は**この1つの印字だけで見えていたはず**のものである。
- **使うとどうなるか**: スコアが動いた／動かなかった理由の切り分けが劇的に速くなる。「Config-Driftガード」が**設定値のズレ**を検出するのに対し、こちらは**設定と実現のズレ**を検出するので、両方あって初めて完全になる。実装コストはprint文数行と極めて低く、機械学習パイプラインのデバッグで最も費用対効果が高い工夫のひとつ。
- **初出**: 2026-08-23 / Biohub - Cell Tracking（Biohub Cell Tracking）

### セグメントsoftmax / raggedプーリング（Segment-wise Softmax over Ragged Batches）
- **意味**: サンプルごとに要素数が違うデータ（検査ごとにスライス数が違う、系列ごとに長さが違う等）を、**パディングして固定長テンソルにするのではなく、全要素を1本のフラットなテンソルに詰め、「どのサンプルに属するか」のインデックス配列で区切って** softmax / mean / max を計算する手法。`scatter_reduce` と `index_add_` で実装する。
- **いつ使うか**: Multiple Instance Learning、可変長スライスの医用画像、点群、グラフのノード集約、可変長系列。要素数の分散が大きいほど有利。
- **なぜ使うか**: ゼロパディングは(a) **パディング部分が平均やsoftmaxを汚染する**（マスク処理を忘れると静かにバグる）、(b) 最長サンプルに合わせるので**メモリが最大長 × バッチサイズ**になる、という2つの問題を持つ。ragged表現ならどちらも起きない。
- **使うとどうなるか**: メモリ効率が上がり、マスク忘れ由来のバグが構造的に発生しなくなる。実装上の注意は、**softmaxの数値安定化**（サンプルごとの最大値を引いてから exp する）を自分で書く必要があること。`scatter_reduce(..., reduce='amax')` でサンプルごとの最大値を取り、`index_add_` で合計する、という2段構えが定型パターン。
- **初出**: 2026-08-23 / RSNA Knee Abnormality Detection（RSNA Knee）

### ソース埋め込みバイナリペイロード（In-Source Embedded Binary Payload）
- **意味**: 較正パラメータや小さな重み・ルックアップテーブルを、外部ファイルではなく **base64 + zlib で圧縮した文字列としてソースコードに直接埋め込む**手法。実行時に `zlib.decompress(base64.b64decode(...))` で復元する。
- **いつ使うか**: インターネット接続が切られるCode Competition、単一ファイルでの提出が求められる環境、添付データセットの数を減らして依存関係を単純にしたいとき。数十KB〜数百KB程度の数値データが対象。
- **なぜ使うか**: 添付データセットは**マウント忘れ・名前変更・権限変更で静かに壊れる**。ソースに埋め込めば「ファイルはあるがロードされていない」という失敗モードが消える。zlibを噛ませるとbase64の膨張（4/3倍）を相殺できる。
- **使うとどうなるか**: 依存が減る代わりに、**可読性と検証可能性が完全に失われる**。中身が何であるかはコードから読み取れず、外部の目からは監査不能で、diffも意味を持たない。これは「プロヴェナンス階層化監査」で言うところの**検証不能メンバーをパイプラインに埋め込む行為**そのものであり、後で「何が効いているのか分からない」状態を作る。**速さ・堅牢さと再現性・監査可能性のトレードオフとして意識的に選ぶべき**で、公開・共有するコードでは生成スクリプトを必ず添える。
- **初出**: 2026-08-23 / RSNA Knee Abnormality Detection（RSNA Knee）

### JEPA / 潜在空間予測型の自己教師あり学習（Joint-Embedding Predictive Architecture）
- **意味**: 画像の一部を隠して学習する自己教師あり学習のうち、隠した領域の**ピクセルを復元させる**のではなく、隠した領域の**特徴ベクトル（潜在表現）を予測させる**方式。context encoder（生徒）が見えている部分だけを見て、predictorが「隠された場所の表現はこうなっているはず」を出力し、target encoder（教師）が画像全体から出した実際の表現との Smooth L1 距離を最小化する。
- **いつ使うか**: ノイズが多い・SN比が低い画像ドメイン（超音波、低線量CT、顕微鏡画像など）で、ラベルなし画像が大量にあるとき。逆に、自然画像のように「局所のピクセル強度＝下にある構造」がほぼ成り立つドメインでは MAE 等のピクセル復元でも十分に機能する。
- **なぜ使うか**: ピクセル復元を目的にすると、モデルは**復元しても意味のない高周波成分**（超音波のスペックル、装置ゲイン、音響陰影）の再現に表現能力を費やしてしまう。これらは装置・術者・施設で変わるため、そこに合わせた表現は本番環境（OOD）で崩れる。潜在表現を予測目標にすれば、この「意味のない成分」は目標から自然に落ちる。
- **使うとどうなるか**: ラベルが少ない領域での性能劣化が明確に緩やかになり（ラベル10%未満で macro F1 +18pt の報告）、**画質劣化に対する頑健性が大きく上がる**（テスト側にだけスペックルノイズを乗せる実験で、ピクセル復元系のURFMが -44.6% 劣化する条件で -0.6% に留まる）。一方で、下流タスクが分類なのか密なセグメンテーションなのかで転移のしやすさは変わり、後者での有効性は別途検証が要る。関連する変種に、教師をEMA更新ではなく**凍結した既存モデル**に置き換える **SALT (Static-teacher Asymmetric Latent Training)** があり、EMAのハイパーパラメータ調整が不要になり計算も軽くなる。
- **初出**: 2026-08-26 / Paper Digest（US-JEPA, arXiv:2602.19322） — UMUD Challenge（筋超音波）のように、ラベルが少なく画質のばらつきが大きいコンペの事前学習方針として。

### 線形プローブ（Linear Probing）
- **意味**: 事前学習済みモデルのバックボーンを**完全に凍結**し、その出力特徴の上に**線形分類器（または線形回帰）を1枚だけ**学習させて下流タスクの性能を測る評価手法。fine-tuning が「モデルを作り変えたときの性能」を測るのに対し、線形プローブは「**すでに獲得されている表現そのものの質**」を測る。
- **いつ使うか**: (a) 複数の事前学習済みバックボーンのどれを採用するか選ぶとき、(b) 自分の事前学習手法が表現を改善したかを検証するとき、(c) 下流ラベルが極端に少なく、全体をfine-tuneすると過学習するとき。
- **なぜ使うか**: fine-tuning での比較は、学習率・エポック数・凍結範囲といったチューニングの差で結果が簡単にひっくり返るため、**表現の質と調整の巧拙が分離できない**。線形プローブは学習可能パラメータが極小なので、差がほぼそのまま表現の差として読める。加えて、1本あたりの学習が数分で終わるため**探索コストが1桁以上下がる**。
- **使うとどうなるか**: バックボーン選定を「全部fine-tuneして比べる」から「凍結特徴を線形で比べて絞ってから、上位だけfine-tuneする」という2段構えにできる。US-JEPAの論文自体が、凍結教師にどのモデルを使うかをこの手順で決めている（既存の超音波基盤モデル6本を線形プローブで比較し、最良のURFMを教師に採用）。注意点は、線形プローブの順位が必ずしもfine-tuning後の順位と一致しないこと（線形分離可能性と、勾配で適応できる余地は別物）なので、**最終判断ではなく足切りに使う**のが正しい。
- **初出**: 2026-08-26 / Paper Digest（US-JEPA, arXiv:2602.19322）

### LDAM-DRW（ラベル分布考慮マージン損失 + 遅延再重み付け）
- **意味**: クラス不均衡（ロングテール）に対処する損失設計。**LDAM** はクラス j の決定境界に `Δⱼ = C / nⱼ^(1/4)`（nⱼ = そのクラスのサンプル数）というマージンを課し、ロジット zⱼ から Δⱼ を引いてから損失を取る。サンプル数が少ないクラスほどマージンが大きくなる＝「稀なクラスは、より余裕をもって正しく分類しろ」と要求する。**DRW** は再重み付けの運用で、学習の前半は一様重みで回し、全エポックの60%程度を過ぎてから初めてクラス頻度の逆数重みに切り替える。
- **いつ使うか**: マルチラベルまたは多クラス分類で、クラスの出現頻度が桁違いに偏っており、かつ評価指標が macro 平均（macro-AUC / macro-mAP）で稀なクラスも等価に効くとき。
- **なぜ使うか**: 通常の BCE では勾配の大半を頻出クラスが占め、稀なクラスは「ほぼ全部を陰性と予測する」ことで損失が下がってしまう。マージンを課すと、稀なクラスは「ギリギリ正解」では損失が減らなくなる。DRW を挟むのは、**学習の最初から再重み付けすると少数サンプルに過適合した歪んだ特徴表現を学んでしまう**ため。まず汎用的な表現を学ばせ、それができてから tail に集中させる、という2段構え。
- **使うとどうなるか**: CXR-LT 2026（30ラベル）では ResNet-50 固定で mAP が 0.3248 → 0.4241（相対+30%）と大きく動き、試した全アーキテクチャで一貫して BCE を上回った。**注意点が2つ**。(a) LDAM は本来 single-label softmax 用の定式化で、マルチラベルに使うには「各クラスを独立した二値分類とみなし、クラスごとのマージンを BCE の枠内に入れる」という適応が要る。(b) **nⱼ の数え方が結果を左右する**。ラベルが LLM によるレポート解釈のように不完全な場合、「本当は稀ではないのに抽出漏れで稀に見えるクラス」に過大なマージンを課してしまう。nⱼ を何から数えるかを先に決めること。なお同じ不均衡対策でも **Asymmetric Loss はデフォルトの (γ₊=0, γ₋=4) だと極端な不均衡下で正例の勾配まで潰し、mAP 0.0667 と壊滅的に失敗した**という報告があり、万能ではない。
- **初出**: 2026-08-29 / Paper Digest（arXiv:2603.02294） — RSNA Knee の12所見 macro-AUC で、稀な所見（骨折・ベーカー嚢腫）を押し上げる手段として。

### cRT（分類器再学習 / 表現学習と分類器の分離）
- **意味**: ロングテール分類の2段階学習。第1段でネットワーク全体を end-to-end で学習したあと、第2段で**バックボーンを凍結し、分類ヘッドだけを初期化し直して、クラス均衡サンプリングで再学習**する（Kang et al., ICLR 2020 の decoupling）。
- **いつ使うか**: すでに学習済みのモデルがあり、稀なクラスの識別力だけを底上げしたいとき。特に**評価指標がランキング系（AUC）で、再学習の GPU 予算が乏しい**とき。
- **なぜ使うか**: ロングテールでは「特徴表現」と「分類境界」で最適な学習方針が食い違う。表現学習には自然な（不均衡なままの）データ分布が良く、分類境界にはクラス均衡なサンプリングが良い。両者を同時に満たそうとするより、**分けて順にやる**ほうが素直。
- **使うとどうなるか**: **AUC が明確に改善する**（ResNet-50 で 0.8435 → 0.8828）。バックボーンを再学習しないので GPU コストが安く、推論時のコストはゼロ。一方で **F1 には一貫して波及せず、ECE（キャリブレーション誤差）はむしろ悪化する**（0.5575 → 0.8300）。つまり cRT が改善しているのは**クラスの分離可能性であって、確率としての質ではない**。ここから実務的な判断が導ける — 指標が AUC のコンペでは副作用が評価されないので**ほぼノーリスクで入れられる**が、確率値を使って融合重みや閾値を決めている下流工程がある場合、その前提が崩れるので **cRT 後に融合重み・閾値を再最適化する必要がある**。TTA も同じ性質（AP/AUC は微増、キャリブレーションは悪化）を持つ。
- **初出**: 2026-08-29 / Paper Digest（arXiv:2603.02294 / Kang et al., ICLR 2020）

### 開発セット→テストの落差を報告する（Development-to-Test Gap の明示）
- **意味**: 論文やソリューションで、内部検証（開発セット）のスコアと公式テスト（LB）のスコアを**両方並べて報告し、その差の原因を分解する**こと。arXiv:2603.02294 は開発 mAP 0.5220 に対しテスト mAP 0.3950（-0.127）を隠さず示し、原因を「内部分割への過適合」「確率キャリブレーションと閾値選択の不備」「アンサンブル重みをランキング指標に寄せすぎた」の3つに分けている。
- **いつ使うか**: 論文・公開notebookを読んで手法を輸入するかどうか判断するとき、および自分の実験ログを書くとき。
- **なぜ使うか**: 開発セットのスコアだけが書かれた手法は、**その数字のどれだけが汎化でどれだけが分割への適合か分からない**。落差が明示されていれば、輸入時に何を疑うべきかが分かる。同論文では CAM 上は病変を正しく局在化しているのに F1 が 0.0945 しか出ておらず、「**ランキングは正しいが、しきい値が正しくない**」という状態が特定できている。この切り分けは落差を報告して初めて可能になる。
- **使うとどうなるか**: 手法の**指標依存の移植可能性**が読めるようになる。上記の例では、失敗しているのは F1 と ECE で、成功しているのは AUC。したがって **AUC で評価されるコンペ（RSNA Knee 等）には成功部分だけが転移し、報告された弱点はそもそも評価されない**。逆に F1 や instance-level が効くコンペには、この手法をそのまま持ち込むと同じ失敗を再現する。「良い論文か」ではなく「**自分の指標に対して、この論文のどの部分が転移するか**」で読むための道具。
- **初出**: 2026-08-29 / Paper Digest（arXiv:2603.02294）

### SLSQP（Sequential Least SQuares Programming、逐次二次計画法）
- **意味**: 等式・不等式制約つきの非線形最適化を解くアルゴリズム。`scipy.optimize.minimize(fun, x0, method='SLSQP', bounds=..., constraints=...)` で使える。
- **いつ使うか**: アンサンブルのブレンド重みを「合計1、各重み0以上1以下」といった制約のもとで最適化したいとき。変数が数個〜数十個の小規模問題向け。
- **なぜ使うか**: 単純な等重み平均やグリッドサーチと違い、**「重みの合計＝1」のような等式制約を直接扱える**。目的関数に `-roc_auc_score(y, blend)` のような**評価指標そのもの**を置けるため、代理損失を経由せずに本番指標を最適化できる。NNLS（非負最小二乗）は二乗誤差専用だが、SLSQPは任意の目的関数が使える点で自由度が高い。
- **使うとどうなるか**: 少ないモデル数では素直に最適重みに収束する。ただし (1) AUCは階段関数なので勾配が定義されず、数値微分に頼るため**局所解や初期値依存**が出る、(2) OOFに対して最適化するので**OOFへの過学習**が起きる（モデル数が多いほど深刻）、という2点に注意。モデルが10本を超えるなら、正則化つきの線形スタッキングか hill climbing のほうが安定することが多い。
- **初出**: 2026-08-31 / Predicting Smartphone Addiction (Playground Series S6E8)

### CTGAN（Conditional Tabular GAN）
- **意味**: 表形式データ専用に設計された敵対的生成ネットワーク。カテゴリ列のモード不均衡と連続列の多峰性を扱うため、条件付きサンプリングとmode-specific正規化を使う。Kaggle Playground Series の合成データ生成に長年使われてきた。
- **いつ使うか**: プライバシー保護のためのデータ合成、少数クラスの水増し、公開できない実データの代替物を作るとき。
- **なぜ使うか**: 元データの相関構造をある程度保ったまま、実在の個人に対応しないレコードを生成できる。Kaggleでは「実データを模した無限のコンペ用データ」を作る目的で使われる。
- **使うとどうなるか（重要）**: 元の分布を完全には再現できず、**必ずアーティファクト（癖）が残る**。連続値の小数部が特定の格子に乗る、値の出現頻度が元データの再サンプリング構造を反映する、といった痕跡がスコアに直結してしまい、コンペが「生成器の癖当てゲーム」になりやすい。また研究上は「合成データの有用性が高いほど元データへの過学習（＝痕跡の残存）が起きやすい」というプライバシーとのトレードオフが指摘されている。Playgroundで学んだ特徴量が実データで効かない主因はここにある。
- **初出**: 2026-08-31 / Predicting Smartphone Addiction (Playground Series S6E8)

### 最小検出可能差（Minimum Detectable Difference / リーダーボードの分解能）
- **意味**: ある検証セット（またはpublic LB）のサイズで、「2つの提出の差が偶然でない」と言える最小の差。相関の高い2提出の場合、差の標準誤差はサンプルサイズに対しておおよそ 1/√n で縮む。S6E8 の public LB（59,260行）では、相関0.99の2提出は**ノイズだけで約0.00007**の差が出る。
- **いつ使うか**: 順位表の上位に張り付いてスコアが 4桁目・5桁目で争いになったとき。何かを変更してスコアが動いたとき、その動きを解釈する前に必ず。
- **なぜ使うか**: **自分が追いかけている差が、測定系が区別できる差より小さい可能性がある**。S6E8上位帯（0.97125〜0.97130）の差は 0.00005 で、分解能 0.00007 より小さい。この状態で「改善した/しなかった」を判断するのは、ノイズを最適化しているのと同じ。実験を止める判断基準を、感覚ではなく数値で持てる。
- **使うとどうなるか**: 「差が分解能以下なら、その変更は採用も却下もしない（＝現状維持）」という規律が持てる。副作用として、**上位帯での順位そのものが無意味に見えてくる**が、これは正しい認識である。注意点は、これは「publicボードが常に無意味」という主張ではないこと。2つの候補が本当に大きく違えば public は検出できる。危険なのは **public を使って重みやハイパーパラメータを選ぶ**行為の方。関連: Wilson スコア区間、Hanley-McNeil によるAUCの標準誤差、Ladderメカニズム。
- **初出**: 2026-09-01 / Predicting Smartphone Addiction (Playground Series S6E8)

### ノード数ペナルティ付きJaccard（Adjusted Jaccard with Node-Count Penalty）
- **意味**: 通常の Jaccard `TP/(TP+FP+FN)` に加えて、**予測したノード数が主催者の想定値（`estimated_number_of_nodes`）を超えた分**に比例した減点を課す指標。Biohub Cell Tracking で使われている。
- **いつ使うか**: 正解ラベルが sparse（一部のオブジェクトにしか付いていない）で、「検出漏れは分かるが、余計な検出が本当に間違いかは判定できない」タスクの評価を設計・理解するとき。
- **なぜ使うか**: ラベルがsparseだと、余計に出した予測の多くは「FPと判定されず、単に評価対象外」になってしまう。すると **「迷ったら全部出す」が最適戦略になり、指標が壊れる**。想定ノード数との比較でペナルティを入れると、その抜け道が塞がる。
- **使うとどうなるか**: 検出しきい値を高く保つ（＝保守的にする）ことが直接得になる。実際 Biohub の上位notebookは `DET_THRESHOLD=0.965` のような非常に高いしきい値を使い、その代わり取りこぼし（FN）は gap closing や消失ペナルティで別ルートから埋めている。**「FPを抑える機構」と「FNを埋める機構」を別々に設計する**という非対称なパイプラインになるのはこの指標の帰結。ローカル検証を組むときはこのペナルティ項を必ず再現すること。省略すると手元だけスコアが高く出る。
- **初出**: 2026-09-01 / Biohub - Cell Tracking During Development

### PhotometricInterpretation と MONOCHROME1 の反転（DICOMの輝度極性）
- **意味**: DICOMのタグ `PhotometricInterpretation` は、画素値と明るさの対応を示す。`MONOCHROME2` は「値が大きい＝明るい」（一般的な画像と同じ）だが、**`MONOCHROME1` は「値が大きい＝暗い」**で意味が反転している。読み込み時に `a = a.max() - a` のような反転処理が必要。
- **いつ使うか**: DICOMを直接読んで学習・推論に使うとき。**必ず全症例について確認する**（データセット内に両方が混在しうる）。
- **なぜ使うか**: これを見落とすと、**一部の症例だけ白黒が反転した画像**でモデルを学習・推論することになる。例外は出ず、警告も出ない。単に精度が下がるだけなので、原因究明が極めて難しい。
- **使うとどうなるか（対処すると）**: 症例間の輝度極性が揃い、正規化や窓処理が意図通りに働く。これは医用画像を扱う際の「メタデータを読まないと静かに壊れる」3点セット（(1) スライス順序＝ ImageOrientationPatient の法線への ImagePositionPatient の射影で決める、(2) PhotometricInterpretation、(3) PixelSpacing による物理スケール）の1つ。3つとも**ファイル名や画素値だけを見ていては絶対に気づけない**。関連: スライス順序の幾何学的復元、物理サイズ基準のクロップ。
- **初出**: 2026-09-01 / RSNA Knee Abnormality Detection

### アームの逐次実行によるピークRAM抑制（Sequential Arm Execution / DataParallel回避）
- **意味**: 複数モデル（アーム）のアンサンブル推論において、全モデルを同時にメモリへ載せるのではなく、**1本ずつロード → 全症例を推論 → 明示的に解放（`del model; gc.collect(); torch.cuda.empty_cache()`）** を繰り返す実行方式。あわせて `nn.DataParallel` を使わない選択も含む。
- **いつ使うか**: Kaggle の Code Competition のように、**メモリ上限が固定で、本番の隠しテストが手元の検証データより大幅に大きい**環境で複数モデル推論を行うとき。
- **なぜ使うか**: 理由が2つある。(1) 同時保持するとピークRAMがモデル数に比例し、**手元では通るのに本番テストの規模でOOMする**。これはスコアが下がるのではなく**提出が丸ごと失敗する**タイプの失敗。(2) `nn.DataParallel` は forward のたびにモジュールをGPUごとに複製するため、症例数が多いとシステムRAMが膨らむ（`DistributedDataParallel` と違い、単一プロセス・複数スレッド方式のため）。
- **使うとどうなるか**: ピークRAMが「モデル1本分」に固定され、アーム数を増やしてもメモリ的には破綻しない。代償として前処理をアームごとに再実行するが、**前処理は推論に比べて安い**ので通常は割に合う。結果は完全に同一（並列を直列にしただけ）。注意点として、この方式は「アーム間で入力が共有できる」場合にのみ再前処理のコストが問題になるので、前処理が重いパイプラインでは前処理結果をディスクにキャッシュする折衷案を検討する。
- **初出**: 2026-09-01 / RSNA Knee Abnormality Detection

### エラーの成分分解（Error Decomposition）
- **意味**: 単一のスコア（例: Edge Jaccard 0.92）を、**失点の原因カテゴリごとに分解**して集計すること。Biohub のローカル検証では `decompose_errors` が失点を「GTノードの取りこぼし」「余分な予測ノード」「復元されたエッジ」「断片化したエッジ」「検出漏れ由来の失点」「誤接続」に分類している。
- **いつ使うか**: 検証スコアが出た**直後**、次の実験を決める前。特に指標が複合的（Jaccard、macro-AUC、複数成分の重み付き和など）なとき。
- **なぜ使うか**: 「スコアが0.92だった」という情報からは**次に何をすべきかが決まらない**。しかし「失点の60%が検出漏れ由来、25%が誤接続」と分解できれば、手を入れるべき場所が確定し、改善が仮説駆動になる。分解せずにハイパーパラメータを振るのは、暗闇で探すのと同じ。
- **使うとどうなるか**: 実験の当たり率が上がり、無駄な探索が減る。さらに**分解結果を目的関数に接続できる**（例: FN由来が支配的なら消失ペナルティを上げる方向に自動探索する）。注意点は、分解の定義自体が設計判断であること — カテゴリの切り方によって「何が問題か」の見え方が変わるので、**カテゴリは互いに排他かつ網羅的**（合計が全失点と一致する）になるよう設計し、実際に合計を検算すること。関連: 開発セット→テストの落差を報告する。
- **初出**: 2026-09-01 / Biohub - Cell Tracking During Development

### 競技データツリーの再帰globの禁止（I/Oパターンが実行時間制限を食い潰す）
- **意味**: Code Competition において、`glob.glob(path, recursive=True)` を**競技本体のデータディレクトリ**（数千症例分のDICOM等、ファイル数が膨大なツリー）に対して実行しないこと。重みファイル等を探すときは、**マウント済みデータセットの直接パスを優先し、競技ディレクトリは明示的に除外する**（`if "competition" in d.lower(): continue`）。
- **いつ使うか**: `/kaggle/input` 以下からファイルを動的に探索するコードを書くとき。特に「重みファイルの場所がデータセットのバージョンで変わりうる」ので探索したい、という場面。
- **なぜ使うか**: 隠しテストセットは手元のサンプルより桁違いに大きいことがある。ファイルシステムの走査だけで実行時間制限（多くは9〜12時間、効率トラックではさらに短い）を消費してしまい、**推論に入る前にタイムアウトする**。手元では一瞬で終わるので、本番でしか顕在化しない。
- **使うとどうなるか**: 探索コストが定数時間に近づく。実装の型は「(1) 既知の直接パスを列挙して先に試す → (2) それでも見つからなければ、競技ディレクトリを除外した上でデータセット単位に再帰glob」。これは Code Competition における「**精度以前に、まず完走すること**」という要件の一部で、同じカテゴリに fp16失敗時のfp32フォールバック、症例単位の例外捕捉、アームの逐次実行が並ぶ。
- **初出**: 2026-09-01 / RSNA Knee Abnormality Detection

### 微分可能最適化層（Differentiable Optimization Layer, DOL）
- **意味**: **数理計画問題（線形計画・二次計画など）そのものをニューラルネットの1つの層として組み込む**技術。目的関数の係数行列を層への入力、その最適解を層の出力とする。逆伝播は、KKT条件と陰関数定理を使って「最適解の、係数に関する微分」を解析的に求めることで通す（Amos & Kolter 2017）。普通の層（全結合・畳み込み等）と決定的に違うのは、**出力にハード制約を強制できる**こと。
- **いつ使うか**: (1) 出力が満たすべき制約が**ドメイン知識として確実に分かっている**とき（例: 細胞は最大2つにしか分裂しない、割当は1対1、確率は和が1）。かつ (2) **その制約を学習で獲得できるほどデータがない**とき。逆に、データが潤沢で制約も曖昧なら普通の層に学ばせた方がよい。
- **なぜ使うか**: 制約を「後処理」として最後に課すだけでは、**上流のモデルは制約の存在を一度も学習しない**。DOLを挟むと制約を通した勾配が上流に流れるので、上流の特徴抽出器が「制約と整合する表現」を学ぶよう誘導される。GNN-DOL論文の著者はこれを "the use of DOL affected the upstream parameters of GNN in a favorable manner" と表現している。特に効くのは**訓練データに稀にしか現れないケース**（同一フレーム内の多重分裂など）で、データ駆動では原理的に学べない領域を制約が埋める。
- **使うとどうなるか**: GNN-DOL論文では、分裂検出のF1が制約なしSOTA比で 0.65→0.87 に改善し、**その差はほぼ全て recall から来た**（precision は横並び）。つまり「制約を入れると保守的になる」のではなく、逆に**取りこぼしが減る**。代償は計算コストで、二次計画を解くぶん推論が約3倍遅い。実務では、DOLをそのまま入れる代わりに**同じ制約をソフトなペナルティ損失（ラグランジュ緩和）として足す**という安価な代用が有力で、追加コストはほぼゼロで済む。関連: ハンガリアン法のBig-M制約（後処理で制約を課す側の代表例）、微分可能サロゲート目的関数。
- **初出**: 2026-09-02 / Paper Digest: Differentiable optimization layers enhance GNN-based mitosis detection
